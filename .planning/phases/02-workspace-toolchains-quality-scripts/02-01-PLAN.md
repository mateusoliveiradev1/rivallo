---
phase: 02-workspace-toolchains-quality-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - turbo.json
  - Cargo.toml
  - rust-toolchain.toml
  - scripts/verify-cargo-workspace.mjs
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - pnpm, Turborepo, and Cargo recognize valid root workspaces while no application, package, or crate exists.
    - The Node/pnpm and Rust/Cargo minimum-version sources are versioned, stable, and unambiguous to a validator.
  artifacts:
    - package.json
    - pnpm-workspace.yaml
    - turbo.json
    - Cargo.toml
    - rust-toolchain.toml
    - pnpm-lock.yaml
  key_links:
    - package.json declares the root package, locally resolved quality-tool dependencies, and minimum Node/pnpm policy consumed by the Phase 2 validator.
    - pnpm-lock.yaml resolves the declared local tools so clean-checkout installation and local Turbo execution are deterministic.
    - rust-toolchain.toml selects a numbered stable Rust release that Cargo.toml's virtual workspace can inspect without permitting rustup to fetch it implicitly.
---

<objective>
Create only the root workspace metadata and an explicit, inspectable minimum-version policy.

Purpose: make Phase 2's later validators and quality scripts operate on real pnpm/Turborepo/Cargo structures instead of future-component stand-ins.
Output: valid root manifests with no applications, packages, or Rust member crates.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-SPEC.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-CONTEXT.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-RESEARCH.md
@docs/adr/ADR-0001-monorepo-workspaces.md
@docs/adr/ADR-0003-independent-domain-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish root pnpm metadata, locally declared tools, and the committed lockfile</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, pnpm-lock.yaml</files>
  <action>Create the root-only pnpm package and workspace manifest, with only a root package present and location globs that do not require creating directories. Set native Node and pnpm minimum policy in package.json per D-03, D-04, and D-07; do not use an exact package-manager pin as enforcement. Declare the actual Phase 2 local development dependencies needed by subsequent configuration and checks, including Turbo, TypeScript, ESLint, Prettier, Vitest, and their required integration packages; do not declare product, app, or future-service dependencies. Generate and commit pnpm-lock.yaml from those declarations, then prove a documented clean dependency install succeeds with pnpm install --frozen-lockfile. This installs repository dependencies only and never installs or upgrades Node, pnpm, Rust, or Cargo under D-05 and D-15. Add a valid turbo.json that declares no task before an executable root quality command exists, with no app build, dev, API, desktop, database, CI, or package task. Preserve cross-platform orchestration: scripts must delegate to Node/Rust commands rather than contain shell conditionals per D-01 and D-02. Invoke Turbo only through the locally resolved dependency after the lockfile install, and confirm workspace discovery succeeds without fabricating workspace members.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile; pnpm exec turbo --version; pnpm list --depth -1</automated>
  </verify>
  <done>The root is a valid pnpm workspace and Turborepo configuration with a committed dependency lockfile and locally resolved Turbo, but no created apps, packages, build/dev pipelines, task declarations, or fictitious scripts; its native Node/pnpm policy is ready for a validator.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the virtual Cargo root and resolve Rust minimum encoding</name>
  <files>Cargo.toml, rust-toolchain.toml, scripts/verify-cargo-workspace.mjs</files>
  <action>Create a virtual Cargo workspace with an explicit empty members list and an appropriate resolver, never a dummy crate. In rust-toolchain.toml select a numbered stable release channel and required formatter/linter components. Document the representation inline in the native files only where Cargo/Rustup conventions warrant it: the numeric stable channel is the Rust/Cargo minimum source, so later validation accepts stable installed releases at or above that number, rejects prereleases, and treats the channel neither as an exact lock nor as an unbounded stable alias. This explicitly resolves the research risk under D-03, D-04, D-06, and D-07. Create a portable Node structural-probe entry point that invokes Cargo metadata through argument-array process APIs with a child environment that merges the parent environment and sets RUSTUP_AUTO_INSTALL=0. Use this entry point for every Phase 2 Cargo metadata proof, so a missing selected channel fails rather than causing rustup to download it; its diagnostic must tell the developer to install the required toolchain manually. Prove the virtual root through that metadata boundary only; do not add domain, application, axum, Tauri, persistence, or contract crates.</action>
  <verify>
    <automated>node scripts/verify-cargo-workspace.mjs</automated>
  </verify>
  <done>Cargo metadata succeeds for a zero-member virtual workspace, and rust-toolchain.toml provides a stable numeric minimum that a cross-platform validator can read consistently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Developer PATH to workspace commands | Locally resolved Node, pnpm, rustc, and Cargo binaries can be absent, stale, or substituted. |
| Root manifests to later scripts | Manifest version policy must not be silently contradicted by future script behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | package.json and rust-toolchain.toml | mitigate | Keep native policy sources versioned and make Plan 02 consume them directly. |
| T-02-02 | Denial of service | Empty Cargo workspace / rustup override | mitigate | Use a metadata structural probe with RUSTUP_AUTO_INSTALL=0, actionable manual remediation, and an empirical Clippy-semantics gate in Plan 04. |
| T-02-SC | Tampering | Dependency installation | mitigate | Later checks use committed manifests and never auto-install or auto-update toolchains. |
</threat_model>

<verification>
Run pnpm workspace discovery, Turbo configuration inspection, and the Node Cargo structural probe (`node scripts/verify-cargo-workspace.mjs`), never a direct Cargo invocation. Inspect the repository tree to confirm no application/package/crate directory was created.
</verification>

<success_criteria>
- Root workspace metadata is valid with zero product members.
- The stable Rust minimum representation is explicit and usable by the planned validator.
- No Phase 3 or Phase 4 responsibility is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-toolchains-quality-scripts/02-01-SUMMARY.md` when done.
</output>
