---
phase: 02-workspace-toolchains-quality-scripts
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - package.json
  - scripts/check-rust-quality.mjs
  - scripts/run-quality.mjs
  - .gitignore
  - README.md
  - docs/operations/local-development.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - rustfmt, Clippy, cargo-nextest, and Cargo workspace checks succeed meaningfully for the valid zero-member virtual workspace without a fabricated crate or an implicit rustup download.
    - In a zero-member workspace, rust:clippy reports that no members are available, validates the selected Clippy component and Cargo metadata, and does not represent a Cargo no-op as source-lint success; once members exist, it runs Clippy with warnings denied.
    - pnpm check validates toolchains first, then runs each available quality layer fail-fast through a reproducible atomic command.
    - A clean checkout can install declared dependencies and run all documented commands without credentials, services, Docker, or cloud resources.
    - Repeated commands leave tracked files unchanged and standard local artifacts ignored.
  artifacts:
    - scripts/check-rust-quality.mjs
    - .gitignore
    - README.md
    - docs/operations/local-development.md
  key_links:
    - package.json exposes rust:fmt, rust:clippy, rust:test, smoke, and check through actual validators.
    - README.md and local-development.md document only the commands and phase boundaries that exist in the repository.
---

<objective>
Resolve the empty-Cargo quality semantics, complete the root command contract, and document repeatable clean-checkout use.

Purpose: prevent a nominal Rust quality script from silently succeeding while no crate exists, while retaining the planned no-crate boundary until Phase 3.
Output: meaningful Rust/tooling smoke checks, final aggregate command, cache ignores, and exact developer instructions.
</objective>

<execution_context>
@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-SPEC.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-CONTEXT.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-RESEARCH.md
@docs/testing/strategy.md
@docs/operations/local-development.md
@docs/adr/ADR-0001-monorepo-workspaces.md
@docs/adr/ADR-0003-independent-domain-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empirically establish meaningful Rust quality behavior for an empty virtual workspace</name>
  <files>scripts/check-rust-quality.mjs, package.json</files>
  <action>First run and record in the implementation summary the selected toolchain's formatter check, formatter-component version, warnings-denied Clippy, nextest-version, and workspace-metadata behavior against the Plan 01 virtual workspace. Invoke each observation only as a subcommand of the portable Node Rust-quality adapter, which merges its child environment with RUSTUP_AUTO_INSTALL=0; this applies to exploratory probes, metadata checks, formatter, Clippy, and nextest. An unavailable selected toolchain must fail non-zero without a fetch and tell the developer how to install it manually. Resolve both formatter and Clippy zero-member semantics from that evidence rather than assuming a successful Cargo subcommand checked source. Use that evidence to implement the adapter and atomic `rust:fmt`, `rust:clippy`, and `rust:test` commands. `rust:fmt` must use formatter check mode only when actual Rust inputs are present; when the virtual workspace has zero members, it must instead prove the selected formatter component is available and the zero-member workspace is structurally valid, otherwise fail non-zero with an actionable explanation. `rust:test` must prove cargo-nextest availability plus virtual-workspace integration through the adapter's nextest-version and metadata subcommands per D-11. For `rust:clippy`, branch on the adapter's metadata member count. With zero members, the command must succeed only after it reports the zero-member state, proves the selected Clippy component is available, and validates the virtual workspace through that adapter; this is a component-and-structure contract, not a source-lint result. With one or more real members, the adapter must invoke Clippy for the workspace and all targets with warnings denied, then propagate failure. Fail non-zero with actionable diagnostics if metadata is invalid, the Clippy component is unavailable, the member state is unexpected, a selected toolchain is unavailable, or a non-empty workspace has Clippy findings. Do not suppress an error, fabricate a crate, permit an automatic rustup fetch, or treat a Cargo no-op as lint success. This task is the required resolution of the research risk; do not proceed by assuming empty-workspace semantics.</action>
  <verify>
    <automated>pnpm rust:fmt; pnpm rust:clippy; pnpm rust:test; node scripts/verify-cargo-workspace.mjs</automated>
  </verify>
  <done>Each Rust atomic command has observed, documented Phase 2 behavior. The valid zero-member `rust:clippy` path succeeds only as an explicit Clippy-component plus Cargo-metadata validation and reports that no source members exist; a non-empty workspace invokes Clippy with warnings denied. No command hides an empty-workspace no-op, creates a fake crate, installs tools, or downgrades warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Complete check and smoke scripts with clean-tree enforcement</name>
  <files>package.json, scripts/run-quality.mjs, .gitignore</files>
  <action>Extend the portable aggregate orchestration to include `rust:fmt`, `rust:clippy`, and `rust:test` after toolchain validation and JavaScript checks, preserving fail-fast order and one atomic reproduction command for every layer under D-12 through D-14. Make `smoke` validate workspace metadata/configuration and the Rust availability boundary that exists now; it must not invoke an absent application, API, crate, database, desktop, Docker, CI, or Playwright workflow. Add narrow ignore entries for node_modules, .turbo, target, test coverage/report artifacts created by the declared tooling, and temporary test fixtures, without masking tracked source/configuration. Exercise the full suite twice from a clean tracked tree and assert `git status --porcelain` remains empty in the verification procedure, per D-09 and D-16.</action>
  <verify>
    <automated>pnpm check; pnpm check; git status --porcelain</automated>
  </verify>
  <done>`pnpm check` is the real primary aggregate, begins with toolchains, fails fast by reproducible atomic layer, has no silent future script, and repeated runs preserve a clean tracked tree.</done>
</task>

<task type="auto">
  <name>Task 3: Publish exact clean-checkout commands and ownership boundaries</name>
  <files>README.md, docs/operations/local-development.md</files>
  <action>Rewrite the local-development guidance and add a concise README development section that names supported operating systems, minimum stable Node/pnpm/Rust/Cargo policy, how to inspect a validator failure, and the exact clean-checkout sequence: obtain supported tools independently, run `pnpm install --frozen-lockfile`, then the declared atomic commands, `pnpm smoke`, and `pnpm check`. Document that every Phase 2 Rust/Cargo probe sets RUSTUP_AUTO_INSTALL=0 and that an unavailable selected channel must be installed manually using the validator's stated remediation; it must never be downloaded by a quality command. State plainly that dependency installation does not install or update Node, pnpm, Rust, or Cargo per D-15. Record root configuration ownership, Phase 3 Rust-crate/contract ownership, Phase 4 desktop/API/persistence/container/CI ownership, and that app, crate, Tauri, axum/API, persistence, integration, Docker, and CI-specific commands are intentionally absent per D-07, D-13, and D-16. Do not document Playwright because the locked SPEC does not include it; do not require credentials, Neon, Docker, or product services.</action>
  <verify>
    <automated>pnpm check; git diff --check; git status --porcelain</automated>
  </verify>
  <done>A contributor can reproduce every Phase 2 validation command from documentation with no secrets or infrastructure, and the documentation accurately lists both present commands and deliberate phase boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust toolchain to virtual workspace checks | Cargo subcommands may have zero-member behavior that is not a useful validation. |
| Local caches to repository status | Tooling may produce artifacts that must not conceal tracked changes. |
| Documentation to developer environment | Instructions must not imply credentials, automatic upgrades, or unavailable infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09 | Repudiation | Empty-workspace Clippy | mitigate | Empirically observe behavior; for the expected zero-member state, report it and require Clippy-component plus Cargo-metadata evidence, while non-empty workspaces run Clippy with warnings denied. |
| T-02-10 | Tampering | Local caches | mitigate | Narrow ignores plus twice-run clean-tree verification. |
| T-02-11 | Denial of service | Aggregate checks | mitigate | Fail fast by atomic layer with a reproduction command. |
| T-02-12 | Elevation of privilege | Toolchain setup guidance and Rust/Cargo children | mitigate | Document manual prerequisite remediation, prohibit installers/updaters in validation, and require RUSTUP_AUTO_INSTALL=0 for every Rust/Cargo child. |
| T-02-SC | Tampering | Dependencies | mitigate | Use the committed lockfile installation command and do not provision external services. |
</threat_model>

<verification>
From a clean tracked checkout, run each atomic command, the Node Cargo structural probe (`node scripts/verify-cargo-workspace.mjs`), `pnpm smoke`, and `pnpm check` twice. Confirm all expected local artifacts are ignored, `git diff --check` succeeds, and `git status --porcelain` remains empty. Inspect script inventory and documentation for absence of future-component command claims. The structural probe and every Rust/Cargo command must cross the same Node adapter boundary that sets `RUSTUP_AUTO_INSTALL=0`.
</verification>

<success_criteria>
- Rust quality behavior is empirically proven: the expected zero-member Clippy path succeeds through an explicit component-and-structure contract, while any non-empty workspace uses warnings-as-errors Clippy; no dummy crate or ignored failure is introduced.
- `pnpm check` is a non-mutating, fail-fast aggregate of genuine Phase 2 checks.
- Documentation is enough to reproduce Phase 2 on supported systems without secrets or infrastructure.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-toolchains-quality-scripts/02-04-SUMMARY.md` when done.

## Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible pnpm/Turborepo/Cargo root with real quality commands | 01–04 | COVERED | Workspace roots, validator, quality configuration, and reproducibility are sequenced. |
| REQ | FOUND-01 | Workspace boundaries and repeatable local tooling | 01–04 | COVERED | All workspace, toolchain, script, ignore, and documentation work is in scope. |
| REQ | FOUND-02 (quality-script portion) | Independent quality-tool verification base | 03–04 | COVERED | CI itself remains owned by Phase 4. |
| RESEARCH | — | Empty but valid pnpm/Turbo/Cargo roots | 01 | COVERED | Explicit empty Cargo members and no invented workspace packages. |
| RESEARCH | — | Native minimum-policy sources and cross-platform validator | 01–02 | COVERED | Numeric Rust channel representation and manifest-driven process validator. |
| RESEARCH | — | Real TypeScript/ESLint/Prettier/Vitest checks | 03 | COVERED | Existing tooling files are explicit targets; Vitest has a real smoke test. |
| RESEARCH | — | Empty-workspace Clippy behavior must be proven | 04 | COVERED | Empirical task with fail-closed, no-dummy-crate resolution. |
| RESEARCH | — | cargo-nextest availability/workspace smoke | 04 | COVERED | Version plus Cargo metadata validation. |
| RESEARCH | — | Cache ignores and repeated clean-tree checks | 04 | COVERED | Narrow ignores and twice-run verification. |
| CONTEXT | D-01 | Windows/macOS/Linux and no shell-specific quality workflow | 01–04 | COVERED | Node process APIs and portable commands are required in every relevant action. |
| CONTEXT | D-02 | Node/Rust validation logic, scripts only orchestrate | 01–04 | COVERED | Validator and aggregate are Node programs. |
| CONTEXT | D-03 | Native versioned sources | 01–02 | COVERED | package.json and rust-toolchain.toml drive validation. |
| CONTEXT | D-04 | Stable compatible minima, no prereleases | 01–02 | COVERED | Numeric channel and comparator tests. |
| CONTEXT | D-05 | Actionable non-zero failure and no automatic update | 02, 04 | COVERED | TDD diagnostics plus docs/aggregate behavior. |
| CONTEXT | D-06 | Rust/Cargo mismatch invalid | 02 | COVERED | Explicit behavior test and validator branch. |
| CONTEXT | D-07 | Native source, validator, docs updated together | 01, 02, 04 | COVERED | Policy link is designed and documented. |
| CONTEXT | D-08 | ESLint/Clippy warnings block | 03–04 | COVERED | ESLint maximum warnings and Clippy warnings-as-errors. |
| CONTEXT | D-09 | Aggregates do not mutate | 03–04 | COVERED | Check modes plus clean-tree evidence. |
| CONTEXT | D-10 | Vitest tooling smoke only | 02–03 | COVERED | Validator and configuration tests contain no product behavior. |
| CONTEXT | D-11 | nextest availability/workspace smoke, no crate | 04 | COVERED | Version plus metadata, no Rust member creation. |
| CONTEXT | D-12 | check validates toolchains first | 02–04 | COVERED | Validator precedes final aggregate layers. |
| CONTEXT | D-13 | Explicit atomic scripts | 03–04 | COVERED | JavaScript then Rust atomic command wiring. |
| CONTEXT | D-14 | Fail-fast and atomic reproduction | 03–04 | COVERED | Node orchestrator preserves first failure. |
| CONTEXT | D-15 | Frozen lockfile docs, never toolchain setup | 04 | COVERED | Exact documented installation boundary. |
| CONTEXT | D-16 | Repetition leaves tracked tree clean | 02–04 | COVERED | Cleanup, ignores, and twice-run verification. |

No source-audit item is unplanned. The research-only Playwright discrepancy is excluded because the locked SPEC and CONTEXT do not include Playwright; adding it would exceed the accepted Phase 2 scope.
</output>
