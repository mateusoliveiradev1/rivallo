---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/domain/Cargo.toml
  - crates/domain/src/lib.rs
  - crates/application/Cargo.toml
  - crates/application/src/lib.rs
  - crates/contracts/Cargo.toml
  - crates/contracts/src/lib.rs
  - crates/platform/Cargo.toml
  - crates/platform/src/lib.rs
  - scripts/check-rust-quality.mjs
autonomous: true
requirements: [FOUND-03, DATA-02]
must_haves:
  truths:
    - "Cargo metadata reports exactly the four exercised Phase 3 crates with the locked D-01 production edge direction."
    - "Domain provides neutral module identity only, while application exposes a real contract-preparation service without a transport entrypoint or a contracts dependency."
  artifacts:
    - "crates/domain/src/lib.rs"
    - "crates/application/src/lib.rs"
    - "crates/contracts/src/lib.rs"
    - "crates/platform/src/lib.rs"
  key_links:
    - "application -> domain only for contract preparation"
    - "platform -> application and contracts only as the outer composition boundary"
  prohibitions:
    - statement: "The domain crate must not gain a direct or transitive framework, platform, network, persistence, frontend, or database dependency."
      status: planned
      verification: "Plan 03-02 real-metadata traversal and controlled transitive-denylist fixture reject the prohibited closure."
    - statement: "Phase 3 must not introduce runtime product endpoints, desktop/UI behavior, persistence, infrastructure, authentication, multiplayer, or football-product concepts."
      status: planned
      verification: "Task 1 crate-responsibility acceptance plus Plan 03-06 scope inventory keep this foundation-only boundary enforceable."
---

<objective>
Create the first exercised Rust crate boundaries and enforce their real resolved dependency graph.

Purpose: establish the independent core required by FOUND-03 before adding contract-generation tooling.
Output: four narrowly scoped crates with executable D-01 topology.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-CONTEXT.md
@scripts/check-rust-quality.mjs
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Establish the exercised D-01 crate topology</name>
<files>Cargo.toml, crates/domain/Cargo.toml, crates/domain/src/lib.rs, crates/application/Cargo.toml, crates/application/src/lib.rs, crates/contracts/Cargo.toml, crates/contracts/src/lib.rs, crates/platform/Cargo.toml, crates/platform/src/lib.rs, scripts/check-rust-quality.mjs</files>
<read_first>
- `03-SPEC.md` requirements 1, 2, and 8 plus its constraints/prohibitions: crate responsibility and forbidden scope are locked.
- `03-CONTEXT.md` D-01 through D-05: permitted edges, real crate duties, neutral domain identity, and contracts ownership.
- `03-RESEARCH.md` Architectural Responsibility Map and Recommended Project Structure: exact tier ownership and crate names.
- `03-VALIDATION.md` row 03-01-01: focused command and Wave 0 test expectation.
- Root `Cargo.toml` and `scripts/check-rust-quality.mjs`: Phase 2 zero-member workspace and no-auto-install process pattern being replaced.
</read_first>
<behavior>
- Cargo metadata lists domain, application, contracts, and platform as workspace members.
- Domain exports a neutral ModuleId or equivalent primitive and no football entity.
- Application's ContractPreparationService accepts domain-owned `ModuleId` and produces a neutral, domain-owned `PreparedContractInput` without importing contracts.
</behavior>
<action>Per D-01 through D-05, replace the zero-member workspace with only `rivallo-domain`, `rivallo-application`, `rivallo-contracts`, and `rivallo-platform`. Enforce the locked production graph in manifests: domain has no Phase-3 crate dependency; application depends only on domain; contracts has no Phase-3 crate dependency unless an explicitly approved neutral core type is later required; platform alone depends on application and contracts. Put `ModuleId` and a neutral `PreparedContractInput` in domain. Make application’s ContractPreparationService consume ModuleId and return PreparedContractInput only; it must neither import nor name contracts types. Contracts owns semantic-version/transport metadata, while platform translates/composes the neutral preparation information with contracts metadata for export. Extend the established cross-platform `scripts/check-rust-quality.mjs` Node wrapper with a non-mutating `check` mode that invokes `cargo check --workspace` under its existing `RUSTUP_AUTO_INSTALL=0` environment. Do not introduce product domain concepts, runtime listeners, adapters, or desktop behavior.</action>
<verify><automated>pnpm toolchains &amp;&amp; node scripts/check-rust-quality.mjs check &amp;&amp; cargo metadata --format-version=1 --no-deps</automated></verify>
<done>The workspace contains exactly four responsible Phase-3 members; cargo compiles their libraries; application preparation is callable without a route or server.</done>
</task>

</tasks>

<verification>
Run `pnpm toolchains`, the established `node scripts/check-rust-quality.mjs check` wrapper, and `cargo metadata --format-version=1 --no-deps`.
</verification>

<success_criteria>
The independent core compiles with D-01’s application-to-domain-only boundary.
</success_criteria>

## Artifacts this phase produces

- Four exercised Rust boundary crates under `crates/`.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-01-SUMMARY.md` when done.
</output>
