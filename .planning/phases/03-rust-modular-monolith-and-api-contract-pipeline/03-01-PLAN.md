---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/domain/Cargo.toml
  - crates/domain/src/lib.rs
  - crates/application/Cargo.toml
  - crates/application/src/lib.rs
  - crates/contracts/Cargo.toml
  - crates/contracts/src/lib.rs
  - crates/platform/Cargo.toml
  - crates/platform/src/lib.rs
  - scripts/verify-cargo-architecture.mjs
  - tooling-tests/verify-cargo-architecture.test.mjs
autonomous: true
requirements: [FOUND-03, DATA-02]
must_haves:
  truths:
    - "Cargo metadata reports exactly the four exercised Phase 3 crates and accepts only inward crate edges."
    - "The domain dependency closure contains only a small approved core set and no prohibited framework, frontend, network, persistence, or database package family."
    - "Domain provides neutral module identity only, while application exposes a real contract-preparation service without a transport entrypoint."
  artifacts:
    - "crates/domain/src/lib.rs"
    - "crates/application/src/lib.rs"
    - "crates/contracts/src/lib.rs"
    - "crates/platform/src/lib.rs"
    - "scripts/verify-cargo-architecture.mjs"
  key_links:
    - "application -> domain and contracts only for contract preparation"
    - "platform -> application and contracts only as the outer composition boundary"
    - "architecture script -> cargo metadata resolve graph"
---

<objective>
Create the first exercised Rust crate boundaries and enforce their real resolved dependency graph.

Purpose: establish the independent core required by FOUND-03 before adding contract-generation tooling.
Output: four narrowly scoped crates plus metadata-based architecture policy and tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-CONTEXT.md
@scripts/check-rust-quality.mjs
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Establish the exercised inward crate topology</name>
<files>Cargo.toml, crates/domain/Cargo.toml, crates/domain/src/lib.rs, crates/application/Cargo.toml, crates/application/src/lib.rs, crates/contracts/Cargo.toml, crates/contracts/src/lib.rs, crates/platform/Cargo.toml, crates/platform/src/lib.rs</files>
<behavior>
- Cargo metadata lists domain, application, contracts, and platform as workspace members.
- Domain exports a neutral ModuleId or equivalent primitive and no football entity.
- Application's ContractPreparationService consumes domain identity and contracts metadata without an HTTP entrypoint.
</behavior>
<action>Per D-01 through D-05, replace the zero-member workspace with only `rivallo-domain`, `rivallo-application`, `rivallo-contracts`, and `rivallo-platform`. Keep dependencies strictly inward: domain has no Phase-3 crate dependency; application uses domain and the permitted contracts core surface for its real preparation result; contracts has no Phase-3 crate dependency except an explicitly justified permitted core type; platform is the sole outer composer of application and contracts. Give every crate exercised source responsibility now: neutral module identity/primitives in domain, ContractPreparationService in application, semantic version/transport metadata ownership surface in contracts, and an exporter-composition entrypoint in platform. Do not introduce product domain concepts, runtime listeners, adapters, or desktop behavior.</action>
<verify><automated>pnpm toolchains &amp;&amp; cargo metadata --format-version=1 --no-deps</automated></verify>
<done>The workspace contains exactly four responsible Phase-3 members; cargo compiles their libraries; application preparation is callable without a route or server.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement resolved-graph architecture and domain-closure policy</name>
<files>scripts/verify-cargo-architecture.mjs, tooling-tests/verify-cargo-architecture.test.mjs</files>
<behavior>
- A real cargo metadata graph with the approved four member edges passes.
- A controlled graph with an inverted Phase-3 edge fails with the offending path.
- A controlled transitive forbidden dependency reachable from domain fails with the dependency path.
</behavior>
<action>Implement a cross-platform Node audit that invokes Cargo with `RUSTUP_AUTO_INSTALL=0`, parses `cargo metadata --format-version=1`, and traverses the resolved graph rather than inspecting manifests. Encode the narrow allowed workspace-edge matrix from D-01 and a version-controlled small domain allowlist plus package-family denylist covering platform/framework, HTTP/network, frontend, persistence, and database drivers named by the Phase 3 specification. Produce actionable diagnostics naming the violating dependency path and writer-free remediation. Unit-test graph traversal with controlled metadata fixtures for an inverted edge and a transitive domain closure violation, in addition to the live approved workspace graph.</action>
<verify><automated>pnpm test -- tooling-tests/verify-cargo-architecture.test.mjs &amp;&amp; node scripts/verify-cargo-architecture.mjs</automated></verify>
<done>Architecture validation uses actual metadata in normal operation, rejects both edge inversion and transitive domain-policy violations, and leaves the repository unchanged.</done>
</task>

</tasks>

<verification>
Run `pnpm toolchains`, `cargo metadata --format-version=1 --no-deps`, `pnpm test -- tooling-tests/verify-cargo-architecture.test.mjs`, and `node scripts/verify-cargo-architecture.mjs`.
</verification>

<success_criteria>
The independent core compiles and its graph policy makes prohibited dependency direction and transitive domain contamination actionable.
</success_criteria>

## Artifacts this phase produces

- Four exercised Rust boundary crates under `crates/`.
- A real Cargo metadata architecture/domain-closure verifier and focused tests.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-01-SUMMARY.md` when done.
</output>
