---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/check-rust-quality.mjs
  - scripts/verify-cargo-architecture.mjs
  - tooling-tests/verify-cargo-architecture.test.mjs
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "Real Cargo metadata accepts only the D-01 crate edge matrix and rejects each forbidden Phase-3 edge."
    - "The domain dependency closure contains only a small approved core set and no prohibited framework, frontend, network, persistence, or database package family."
  artifacts:
    - "scripts/verify-cargo-architecture.mjs"
    - "tooling-tests/verify-cargo-architecture.test.mjs"
  key_links:
    - "architecture script -> cargo metadata resolve graph"
    - "policy matrix -> application -> domain only; platform -> application/contracts only"
  prohibitions:
    - statement: "The D-01 application crate must not depend on contracts; domain and contracts must not depend on other Phase-3 crates; platform alone may depend on application and contracts."
      status: planned
      verification: "Task 1 validates the matrix from real cargo metadata and controlled forbidden-edge graphs, including application-to-contracts."
    - statement: "This approval plan must not introduce generated artifacts, runtime/product endpoints, or any UI, Tauri, persistence, Docker, CI, authentication, or multiplayer implementation."
      status: planned
      verification: "Task 2 traverses the live resolved graph and controlled transitive fixtures without adding product behavior."
---

<objective>
Make the D-01 dependency policy and domain-closure boundary executable against resolved Cargo metadata.

Purpose: prevent a manifest or implementation change from reintroducing an illegal architecture edge.
Output: metadata-based policy verifier and focused controlled-graph tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@scripts/check-rust-quality.mjs
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Enforce D-01 with a resolved-metadata edge matrix</name>
<files>scripts/check-rust-quality.mjs, scripts/verify-cargo-architecture.mjs, tooling-tests/verify-cargo-architecture.test.mjs</files>
<read_first>
- `03-SPEC.md` requirements 1, 2, and 8: metadata-derived direction and domain-closure acceptance.
- `03-CONTEXT.md` D-01 through D-04: locked dependency directions and neutral boundaries.
- `03-RESEARCH.md` Pattern 3, Security Considerations, and Verification Checklist: resolved-graph traversal and denylist families.
- `03-VALIDATION.md` row 03-02-01 and Wave 0 Requirements: focused test contract.
- `03-01-SUMMARY.md`: audit the completed crate manifests and exports.
- `scripts/check-rust-quality.mjs`: preserve its established cross-platform Node process handling and `RUSTUP_AUTO_INSTALL=0` environment while replacing the zero-member quality behavior.
</read_first>
<behavior>
- The live workspace passes with domain -> none, application -> domain only, contracts -> none, and platform -> application/contracts.
- A controlled application-to-contracts edge fails with its metadata path.
- Controlled inverted and transitive forbidden domain graphs fail with the offending path.
</behavior>
<action>Extend the established cross-platform `scripts/check-rust-quality.mjs` Node wrapper with a non-mutating `check` mode that invokes `cargo check --workspace` under its existing `RUSTUP_AUTO_INSTALL=0` environment; this dependent policy plan owns the wrapper update after Plan 03-01 creates the members. Implement the metadata audit in the same no-auto-install convention: invoke `cargo metadata --format-version=1`, parse its resolved graph rather than manifests, and traverse it in `scripts/verify-cargo-architecture.mjs`. Encode D-01 as an exact workspace-edge matrix: domain none; application domain only; contracts none unless an expressly approved neutral core type is represented in the policy; platform application and contracts only. Explicitly reject application-to-contracts. Add the small domain allowlist and denylist for platform/framework, HTTP/network, frontend, persistence, and database families. Produce actionable dependency paths and test live approved metadata plus controlled application-to-contracts, inverted-edge, and transitive-domain-denylist fixtures.</action>
<verify><automated>node scripts/check-rust-quality.mjs check &amp;&amp; pnpm test -- tooling-tests/verify-cargo-architecture.test.mjs &amp;&amp; node scripts/verify-cargo-architecture.mjs</automated></verify>
<done>Real metadata and controlled fixtures enforce application-to-domain-only architecture and domain purity without mutating the repository.</done>
</task>

</tasks>

<verification>
Run the focused tests and live architecture verifier after the topology plan.
</verification>

<success_criteria>
The resolved Cargo graph mechanically enforces every D-01 crate edge and domain-closure rule.
</success_criteria>

## Artifacts this phase produces

- A metadata-based D-01 policy verifier and controlled graph fixtures.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-02-SUMMARY.md` when done.
</output>
