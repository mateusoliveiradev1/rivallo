---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "04"
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - Cargo.toml
  - crates/contracts/Cargo.toml
  - crates/contracts/src/lib.rs
  - crates/platform/Cargo.toml
  - crates/platform/src/lib.rs
  - crates/platform/src/bin/export_openapi.rs
  - contracts/openapi.json
  - scripts/generate-openapi.mjs
  - scripts/verify-openapi-drift.mjs
  - tooling-tests/openapi-pipeline.test.mjs
autonomous: true
requirements: [FOUND-03, DATA-02]
must_haves:
  truths:
    - "Rust contract schemas and one semantic contract-version constant are the canonical source of committed OpenAPI metadata."
    - "Schema-only export produces deterministic contracts/openapi.json without a runtime operation."
    - "OpenAPI verification regenerates into isolation, byte-compares, fails on drift, and tells the developer how to regenerate."
  artifacts:
    - "contracts/openapi.json"
    - "crates/platform/src/bin/export_openapi.rs"
    - "scripts/generate-openapi.mjs"
    - "scripts/verify-openapi-drift.mjs"
  key_links:
    - "contracts Rust schemas and CONTRACT_VERSION -> platform OpenAPI composition -> export_openapi Cargo binary -> contracts/openapi.json"
  prohibitions:
    - statement: "OpenAPI must remain Rust-derived and generated artifacts must not be hand-edited."
      status: planned
      verification: "Task 2 repeats deterministic generation and uses isolated byte comparison against the committed document."
    - statement: "Schema-only export must not expose a production runtime endpoint; any tool-required fixture must be neutral, test-only, and absent from production registration."
      status: planned
      verification: "Task 1 keeps platform as exporter composition only; Plan 03-06 pipeline/scope proof asserts fixture registration absence if a fixture is needed."
    - statement: "No UI, Tauri, persistence, Docker, CI, authentication, multiplayer, or product behavior is added with contract export."
      status: planned
      verification: "Task acceptance excludes listeners/adapters/product operations and Plan 03-06 source inventory enforces the phase fence."
---

<objective>
Create the canonical Rust-schema-to-versioned-OpenAPI export and its deterministic verifier.

Purpose: make contracts backend-owned without adding a runtime API surface.
Output: Utoipa-backed schema-only export with an executable platform Cargo target, committed canonical JSON, and non-mutating drift proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-02-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Make Rust contracts canonical and compose schema-only OpenAPI</name>
<files>Cargo.toml, crates/contracts/Cargo.toml, crates/contracts/src/lib.rs, crates/platform/Cargo.toml, crates/platform/src/lib.rs, crates/platform/src/bin/export_openapi.rs</files>
<read_first>
- `03-SPEC.md` requirements 3, 4, 6, and 8, constraints, and prohibitions: canonical Rust source, deterministic committed JSON, schema-only boundary, and forbidden runtime scope.
- `03-CONTEXT.md` D-05 through D-08 and D-13 through D-16: ownership, one semantic version, artifact path, and fixture fallback rules.
- `03-RESEARCH.md` Standard Stack, Pattern 1, and Architectural Responsibility Map: `utoipa` 5.5.0 schema-only composition and platform/contracts separation.
- `03-VALIDATION.md` row 03-04-01: expected Rust test proof for schemas/version and no runtime operation.
- `03-01-SUMMARY.md` and `03-02-SUMMARY.md`: completed crate interfaces and required human package decision before dependent work.
</read_first>
<behavior>
- CONTRACT_VERSION appears in the composed OpenAPI info.version.
- The document exposes neutral schema components and no paths in this phase.
- Platform composes/serializes contracts but contracts remains the schema and metadata owner.
 - `cargo run --quiet --package rivallo-platform --bin export-openapi -- --output <path>` is the stable exporter interface and writes the complete deterministic JSON document to the supplied path.
</behavior>
<action>After the Plan 03-03 package approval, add `utoipa` 5.5.0 with macros only where required by the canonical contracts/export path. Per D-05 through D-08, define neutral transport schemas and the single explicit semantic `CONTRACT_VERSION` in contracts, derive schema metadata there, and have platform compose a schema-only OpenAPI document with stable metadata and serialization. Add the executable `crates/platform/src/bin/export_openapi.rs` Cargo binary target named `export-openapi`; its stable interface is `cargo run --quiet --package rivallo-platform --bin export-openapi -- --output <path>`, and it writes the full deterministic JSON document to that supplied output path. Keep platform as library/generator composition only; do not add a listener, a persistence adapter, or a product operation. Platform translates the domain-owned PreparedContractInput from application into the contracts-owned schema/metadata composition; application never imports contracts.</action>
<verify><automated>cargo test --workspace &amp;&amp; cargo run --quiet --package rivallo-platform --bin export-openapi -- --output target/openapi-test.json</automated></verify>
<done>Contracts owns all schema/semantic-version definitions, and the executable platform exporter writes a schema-only OpenAPI document containing only neutral metadata/components to its requested output path.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add explicit deterministic OpenAPI generation and drift verification</name>
<files>contracts/openapi.json, scripts/generate-openapi.mjs, scripts/verify-openapi-drift.mjs, tooling-tests/openapi-pipeline.test.mjs</files>
<read_first>
- `03-SPEC.md` requirements 4 and 6 plus constraints: explicit writers, non-mutating verification, serialized writers, and test-only fixture boundary.
- `03-CONTEXT.md` D-06, D-08, D-12 through D-16: version source, canonical artifact location, root command behavior, and fixture restrictions.
- `03-RESEARCH.md` Pattern 2, Common Pitfalls, and Verification Checklist: stable serialization and isolated temporary-output comparison.
- `03-VALIDATION.md` row 03-04-02 and Wave 0 Requirements: required focused test file and deterministic/drift cases.
- Task 1’s contracts/platform exporter API and existing `scripts/check-rust-quality.mjs`: generator invocation and `RUSTUP_AUTO_INSTALL=0` child-process convention.
</read_first>
<behavior>
- Two explicit exports from unchanged Rust source are byte-identical.
- Verification writes only isolated temporary output and passes against the committed document.
- A controlled difference fails non-zero and prints the exact writer command.</behavior>
<action>Create a cross-platform explicit writer that invokes exactly `cargo run --quiet --package rivallo-platform --bin export-openapi -- --output <destination>` through Node-mediated Rust/Cargo execution with `RUSTUP_AUTO_INSTALL=0`. Supply `contracts/openapi.json` as the explicit writer destination so the Cargo binary, not Node, materializes the complete deterministic JSON file there. Create a separate verifier that supplies a unique temporary output path to that same exporter, compares its bytes without repairing the tracked document, and prints `pnpm contracts:openapi:generate` as remediation on drift. Add integration coverage for repeated deterministic export, normal verifier success, and controlled drift failure. Keep writers serialized by their root command; do not make verifier paths concurrent writers.</action>
<verify><automated>pnpm contracts:openapi:generate &amp;&amp; pnpm contracts:openapi:check &amp;&amp; pnpm test -- tooling-tests/openapi-pipeline.test.mjs</automated></verify>
<done>The committed OpenAPI file is reproducible from Rust contracts; check mode is byte-based, actionable, and non-mutating.</done>
</task>

</tasks>

<verification>
Run the explicit writer twice in isolated copies, then `pnpm contracts:openapi:check` and the focused pipeline tests; confirm no tracked file changes after check mode.
</verification>

<success_criteria>
Versioned schema-only OpenAPI is deterministically produced from Rust contracts without a runtime endpoint.
</success_criteria>

## Artifacts this phase produces

- Canonical `contracts/openapi.json` generated from Rust schema metadata.
- Explicit OpenAPI writer, non-mutating drift verifier, and determinism coverage.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-04-SUMMARY.md` when done.
</output>
