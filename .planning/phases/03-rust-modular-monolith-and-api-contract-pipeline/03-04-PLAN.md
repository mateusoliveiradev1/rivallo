---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "04"
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/contracts-client/package.json
  - packages/contracts-client/openapi-ts.config.ts
  - packages/contracts-client/src/index.ts
  - packages/contracts-client/src/generated/index.ts
  - scripts/generate-contract-client.mjs
  - scripts/verify-contract-client-drift.mjs
  - tooling-tests/contracts-client-generation.test.mjs
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "The dedicated TypeScript workspace package is generated only from contracts/openapi.json and contains types, metadata, and a minimum client."
    - "Client generation is deterministic and client drift verification is non-mutating and actionable."
    - "The generated package has no application dependency or hand-maintained Rust request/response duplicate."
  artifacts:
    - "packages/contracts-client/package.json"
    - "packages/contracts-client/src/generated/index.ts"
    - "scripts/generate-contract-client.mjs"
    - "scripts/verify-contract-client-drift.mjs"
  key_links:
    - "contracts/openapi.json -> pinned generator config -> packages/contracts-client generated output"
  prohibitions:
    - statement: "Generated OpenAPI/client artifacts and Rust request/response types must not be hand-edited or manually duplicated in TypeScript."
      status: planned
      verification: "Task 2 proves input provenance, generated-tree drift, and inventory ownership of request/response schema definitions."
    - statement: "The contracts client must not add application coupling, authentication, retries, runtime product endpoints, or UI/Tauri/persistence/infrastructure behavior."
      status: planned
      verification: "Task 1 package-independence/typecheck acceptance and Plan 05 scope inventory reject those additions."
---

<objective>
Generate and version the application-independent TypeScript contract package from committed OpenAPI.

Purpose: complete the downstream half of the contract pipeline without creating a product transport layer.
Output: pinned generator configuration, generated client/types/metadata, and isolated drift verification.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-02-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-03-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Configure the generated contracts-client workspace package</name>
<files>package.json, pnpm-workspace.yaml, packages/contracts-client/package.json, packages/contracts-client/openapi-ts.config.ts, packages/contracts-client/src/index.ts, packages/contracts-client/src/generated/index.ts</files>
<read_first>
- `03-SPEC.md` requirements 3, 5, and 8 plus generated-artifact constraints/prohibitions: OpenAPI-only provenance, no handwritten duplicate models, and no product leakage.
- `03-CONTEXT.md` D-09 through D-12: package location/independence, minimum client boundary, selected generator gate, and writer/check command split.
- `03-RESEARCH.md` Standard Stack, Package Legitimacy Audit, and Hey API recommendation: exact approved pin and required generator capabilities.
- `03-VALIDATION.md` row 03-04-01: generation/typecheck evidence and expected generated-only package behavior.
- `03-02-SUMMARY.md` and `03-03-SUMMARY.md`: recorded package approval and canonical committed `contracts/openapi.json` producer.
- Root `package.json` and `pnpm-workspace.yaml`: existing workspace/script conventions to extend.
</read_first>
<behavior>
- The generator input is the committed contracts/openapi.json path only.
- Generated output includes schema types, contract metadata containing the OpenAPI semantic version, and a Fetch-based minimum client surface.
- The package can be type-checked independently without an application package.</behavior>
<action>After the exact Plan 02 approval, add the approved exact `@hey-api/openapi-ts` 0.97.3 development dependency and create `packages/contracts-client` as an independent pnpm workspace package. Configure generation exclusively from `contracts/openapi.json` into a clearly generated source directory, with the generator's TypeScript, metadata, SDK, and Fetch-client capabilities needed for the minimum contract surface. Derive exported version metadata from the OpenAPI info version, and make the package entrypoint a generated-output barrel rather than a handwritten request/response model. Do not add authentication, retry policy, application coupling, or convenience transport behavior.</action>
<verify><automated>pnpm contracts:client:generate &amp;&amp; pnpm typecheck</automated></verify>
<done>A versioned generated-only contract package exposes types, metadata, and a minimum client from the canonical OpenAPI document.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add deterministic client generation and non-mutating drift proof</name>
<files>scripts/generate-contract-client.mjs, scripts/verify-contract-client-drift.mjs, tooling-tests/contracts-client-generation.test.mjs</files>
<read_first>
- `03-SPEC.md` requirements 5 and 7 plus constraints: byte-stable generated client, explicit serialized writer, and non-mutating check mode.
- `03-CONTEXT.md` D-10 through D-12: minimum generated surface, no handwritten request/response types, and explicit root commands.
- `03-RESEARCH.md` Pattern 2, Don't Hand-Roll, and Common Pitfalls: temporary-tree comparison and semantic-version provenance.
- `03-VALIDATION.md` row 03-04-02 and Wave 0 Requirements: required generated-client test coverage and focused command.
- Task 1’s generator config/package entrypoint and `contracts/openapi.json`: exact input/output boundary being verified.
</read_first>
<behavior>
- Repeated generation from unchanged OpenAPI is byte-identical.
- Verification generates into a unique temporary package tree and does not modify tracked client output.
- Controlled drift fails non-zero with the exact client-writer remediation command.</behavior>
<action>Implement portable Node writer and verifier adapters around the pinned generator. The writer may intentionally replace only the generated client directory and must serialize access to that destination. The verifier must supply a unique temporary output directory, compare the complete generated tree deterministically against the tracked package, and report `pnpm contracts:client:generate` on a difference. Add focused tests covering input provenance, repeated byte-stable generation, clean check mode, and a simulated mismatch. Include an inventory assertion that request/response schema definitions live in generated output rather than manually authored TypeScript files.</action>
<verify><automated>pnpm contracts:client:generate &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm test -- tooling-tests/contracts-client-generation.test.mjs</automated></verify>
<done>Client drift is detected without mutation, generation is reproducible, and TypeScript contract models remain generated from OpenAPI.</done>
</task>

</tasks>

<verification>
Run client generation twice, run its drift verifier, type-check the workspace, and execute the focused generator tests while checking that verifier mode leaves Git-tracked output untouched.
</verification>

<success_criteria>
The canonical OpenAPI artifact deterministically produces a dedicated generated TypeScript package with no manually duplicated schema layer.
</success_criteria>

## Artifacts this phase produces

- `packages/contracts-client`, containing generated contract types, metadata, and a minimum Fetch client.
- Explicit client writer/verifier commands and generation-proof tests.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-04-SUMMARY.md` when done.
</output>
