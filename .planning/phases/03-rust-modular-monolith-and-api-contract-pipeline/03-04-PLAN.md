---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "04"
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - Cargo.toml
  - crates/contracts/Cargo.toml
  - crates/contracts/src/lib.rs
  - crates/platform/Cargo.toml
  - crates/platform/src/lib.rs
  - crates/platform/src/bin/export_openapi.rs
autonomous: true
requirements: [FOUND-03, DATA-02]
must_haves:
  truths:
    - "Per D-06, Rust contract schemas and one semantic contract-version constant are the canonical source of OpenAPI metadata."
    - "The platform exporter composes a schema-only OpenAPI document without a runtime operation."
  artifacts:
    - "crates/contracts/src/lib.rs"
    - "crates/platform/src/bin/export_openapi.rs"
  key_links:
    - "contracts Rust schemas and CONTRACT_VERSION -> platform OpenAPI composition -> export_openapi Cargo binary"
  prohibitions:
    - statement: "OpenAPI remains Rust-derived and generated artifacts are not hand-edited."
      status: planned
      verification: "Dependent Plan 03-05 materializes and byte-compares the committed document through this exporter."
    - statement: "Schema-only export must not expose a production runtime endpoint; any tool-required fixture is neutral, test-only, and absent from production registration."
      status: planned
      verification: "This plan adds no listener or registration; Plan 03-05 proves schema-first behavior and conditional fixture isolation."
---

<objective>
Create canonical Rust schemas and the schema-only OpenAPI exporter executable.

Purpose: establish backend-owned contract composition before a separate focused plan materializes and verifies the committed artifact.
Output: Utoipa-backed contracts metadata and an executable platform Cargo exporter.
</objective>

<execution_context>
@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-02-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Make Rust contracts canonical and compose schema-only OpenAPI</name>
<files>Cargo.toml, crates/contracts/Cargo.toml, crates/contracts/src/lib.rs</files>
<read_first>
- `03-SPEC.md` requirements 3, 4, 6, and 8 plus constraints/prohibitions: canonical Rust source, schema-only boundary, and forbidden runtime scope.
- `03-CONTEXT.md` D-05 through D-08 and D-13 through D-16: ownership, one semantic version, artifact path, and fixture fallback rules.
- `03-RESEARCH.md` Standard Stack, Pattern 1, and Architectural Responsibility Map: `utoipa` 5.5.0 schema-only composition and platform/contracts separation.
- `03-VALIDATION.md` row 03-04-01: expected Rust test proof for schemas/version and no runtime operation.
- `03-01-SUMMARY.md` and `03-02-SUMMARY.md`: completed crate interfaces and metadata policy.
</read_first>
<behavior>
- Contracts exports neutral schemas and one CONTRACT_VERSION semantic version constant.
- Schema metadata is available to platform without importing application types.
</behavior>
<action>After Plan 03-03 package approval, add `utoipa` 5.5.0 with macros only where required by canonical contract schemas. Per D-05, D-06, D-07, and D-08, define neutral transport schemas and the single explicit semantic `CONTRACT_VERSION` in contracts, and derive schema metadata there. Keep contracts as the source of truth and do not import application types, add product models, or manually assemble the generated JSON.</action>
<verify><automated>cargo test --workspace</automated></verify>
<done>Contracts owns neutral schema metadata and the sole semantic version required by downstream OpenAPI composition.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Compose the schema-only platform exporter executable</name>
<files>crates/platform/Cargo.toml, crates/platform/src/lib.rs, crates/platform/src/bin/export_openapi.rs</files>
<read_first>
- `03-CONTEXT.md` D-01, D-02, D-05, and D-13 through D-16: platform is composition only, contracts own schemas, and schema-first export avoids a runtime operation.
- `03-RESEARCH.md` Pattern 1 and Architectural Responsibility Map: platform composes and serializes Utoipa components without an HTTP framework.
- Task 1 contract exports and `03-02-SUMMARY.md`: available metadata and strict inward dependency policy.
</read_first>
<behavior>
- The exporter uses contracts-owned schemas and CONTRACT_VERSION to compose a document with neutral components and no paths.
- `cargo run --quiet --package rivallo-platform --bin export-openapi -- --output <path>` writes deterministic JSON to the supplied path.
</behavior>
<action>Per D-01, D-02, D-05, and D-13, make platform compose the Task 1 schema metadata into a stable schema-only OpenAPI document and add executable `crates/platform/src/bin/export_openapi.rs` named `export-openapi` with the stable supplied-output interface. Platform may translate the domain-owned PreparedContractInput from application into contracts-owned composition, but application never imports contracts. Keep platform as generator composition only: do not add a listener, persistence adapter, product operation, fixture, or runtime registration.</action>
<verify><automated>cargo test --workspace &amp;&amp; cargo run --quiet --package rivallo-platform --bin export-openapi -- --output target/openapi-test.json</automated></verify>
<done>The platform executable writes only deterministic neutral schema metadata/components to its requested output path without adding runtime behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canonical contracts to exporter | The platform serializes schema metadata owned by contracts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Schema composition | mitigate | Keep schema/version ownership in contracts and exercise the deterministic exporter in Rust tests. |
| T-03-05E | Elevation | Platform exporter | mitigate | Keep the executable as composition only with no listener, adapter, or runtime operation. |
| T-03-SC | Tampering | Cargo dependency | mitigate | The exact Utoipa package is research-approved; package additions remain constrained to the canonical export path. |
</threat_model>

<verification>
Run Rust tests and the stable exporter interface to an isolated target path. Dependent Plan 03-05 proves committed-artifact determinism and drift behavior.
</verification>

<success_criteria>
Canonical Rust schemas and an executable schema-only exporter are ready for deterministic artifact generation without a runtime endpoint.
</success_criteria>

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-04-SUMMARY.md` when done.
</output>
