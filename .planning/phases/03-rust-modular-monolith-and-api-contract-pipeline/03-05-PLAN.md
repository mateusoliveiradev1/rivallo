---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "05"
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - contracts/openapi.json
  - scripts/generate-openapi.mjs
  - scripts/verify-openapi-drift.mjs
  - tooling-tests/openapi-pipeline.test.mjs
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "The committed OpenAPI document is deterministically materialized only by the Rust exporter from canonical contracts."
    - "OpenAPI verification regenerates into isolation, byte-compares, fails on drift, and tells the developer how to regenerate."
    - "The complete Rust-schema-to-OpenAPI path is proven without a runtime operation; a test-only fixture exists only if tool mechanics force it."
  artifacts:
    - "contracts/openapi.json"
    - "scripts/generate-openapi.mjs"
    - "scripts/verify-openapi-drift.mjs"
    - "tooling-tests/openapi-pipeline.test.mjs"
  key_links:
    - "platform export_openapi binary -> explicit Node writer -> contracts/openapi.json"
    - "isolated exporter output -> byte comparison -> actionable non-mutating drift check"
  prohibitions:
    - statement: "OpenAPI remains Rust-derived and generated artifacts are not hand-edited."
      status: planned
      verification: "The writer delegates materialization to the platform exporter, while the verifier byte-compares unique temporary output against the committed document."
    - statement: "Schema-only export must not expose a production runtime endpoint; any tool-required fixture is neutral, test-only, and absent from production registration."
      status: planned
      verification: "Focused pipeline coverage proves schema-first behavior; only demonstrated generator necessity permits a neutral test-only fixture with registration-absence coverage."
---

<objective>
Materialize and verify the canonical OpenAPI artifact through the schema-only Rust exporter.

Purpose: complete deterministic, committed, and non-mutating OpenAPI delivery without adding runtime behavior.
Output: canonical JSON, explicit writer, isolated verifier, and focused integration proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-04-SUMMARY.md
@scripts/check-rust-quality.mjs
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Materialize the committed OpenAPI artifact through the platform exporter</name>
<files>contracts/openapi.json, scripts/generate-openapi.mjs</files>
<read_first>
- `03-SPEC.md` requirements 3, 4, 6, and 8 plus constraints: canonical Rust provenance, deterministic committed JSON, and no runtime scope.
- `03-CONTEXT.md` D-05 through D-08 and D-12 through D-16: contracts ownership, semantic version, canonical path, writer/check split, and fixture restrictions.
- `03-VALIDATION.md` row 03-05-01: writer/determinism command and committed-artifact expectation for this task.
- `03-04-SUMMARY.md`: completed exporter interface and schema-only composition that this writer must call.
- `scripts/check-rust-quality.mjs`: established Node-mediated Cargo child-process and `RUSTUP_AUTO_INSTALL=0` convention.
</read_first>
<behavior>
- The writer invokes the stable platform exporter with `contracts/openapi.json` as output.
- Repeated exports from unchanged Rust source are byte-identical.
- The committed document contains the contracts-owned semantic version, neutral schemas, and no paths unless demonstrated tool necessity activates D-13.
</behavior>
<action>Per D-05 through D-08 and D-12, create the cross-platform explicit writer that invokes exactly `cargo run --quiet --package rivallo-platform --bin export-openapi -- --output <destination>` through Node-mediated Rust/Cargo execution with `RUSTUP_AUTO_INSTALL=0`. Supply `contracts/openapi.json` as the explicit writer destination so the Cargo binary, not Node, materializes the complete deterministic JSON file there. Commit the generated document. Preserve the schema-first document selected under D-13: do not add an operation, listener, adapter, or runtime registration merely to exercise the pipeline.</action>
<verify><automated>pnpm contracts:openapi:generate &amp;&amp; pnpm contracts:openapi:generate &amp;&amp; git diff --exit-code -- contracts/openapi.json</automated></verify>
<done>The committed OpenAPI artifact is generated only by the stable Rust exporter and remains byte-identical across unchanged explicit writes.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Verify isolated OpenAPI drift and schema-first pipeline proof</name>
<files>scripts/verify-openapi-drift.mjs, tooling-tests/openapi-pipeline.test.mjs</files>
<read_first>
- `03-SPEC.md` requirements 4, 6, and 8 plus prohibitions: isolated byte comparison, complete pipeline proof, and no product/runtime scope.
- `03-CONTEXT.md` D-08 and D-12 through D-16: committed artifact, non-mutating checks, schema-first preference, and conditional fixture safeguards.
- `03-RESEARCH.md` Pattern 2, Common Pitfalls, and Verification Checklist: unique temporary-output comparison and deterministic serialization expectations.
- `03-VALIDATION.md` row 03-05-02 and Wave 0 Requirements: focused deterministic/drift cases and test file.
- Task 1 writer interface and `03-04-SUMMARY.md`: exact exporter command and source ownership.
</read_first>
<behavior>
- Verification writes only a unique temporary output and passes without changing tracked files.
- A controlled difference fails non-zero and prints the explicit OpenAPI writer command.
- Pipeline coverage proves schema/version provenance and no production runtime operation registration.
</behavior>
<action>Per D-12 through D-16, create a verifier that supplies a unique temporary output path to the platform exporter, byte-compares it without repairing the tracked document, and prints `pnpm contracts:openapi:generate` on drift. Add integration coverage for deterministic export, normal verifier success, controlled drift failure, schema/version provenance, and absence of runtime registration. Keep writers serialized by root command; do not make verifier paths concurrent writers. Only if generator mechanics demonstrably require an operation, introduce the D-14/D-15 neutral introspection fixture in test-only source and assert its absence from every production registration under D-16.</action>
<verify><automated>pnpm contracts:openapi:check &amp;&amp; pnpm test -- tooling-tests/openapi-pipeline.test.mjs &amp;&amp; git diff --exit-code -- contracts/openapi.json</automated></verify>
<done>OpenAPI check mode is byte-based, actionable, and non-mutating, while focused proof protects the schema-only/no-runtime boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Rust schemas to committed JSON | Generated contract data crosses from canonical source to a tracked artifact. |
| Drift verifier to temporary filesystem | Verification materializes change-detection output outside tracked artifact paths. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | OpenAPI writer and verifier | mitigate | Explicit writes, unique temporary output in check mode, and byte comparison against committed JSON. |
| T-03-06R | Repudiation | Drift failure | mitigate | Emit the exact writer command and preserve tracked output during verification. |
| T-03-06E | Elevation | Export path | mitigate | Keep exporter schema-only; test absence of listener, adapter, and production registration. |
| T-03-SC | Tampering | Cargo/npm packages | accept | Package installation was separately gated in 03-03; this plan installs no package. |
</threat_model>

<verification>
Run the explicit writer twice, then the isolated drift checker and focused pipeline tests; confirm the checker leaves `contracts/openapi.json` unchanged.
</verification>

<success_criteria>
Versioned schema-only OpenAPI is deterministically committed from Rust contracts, and verification is actionable without mutating the repository.
</success_criteria>

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-05-SUMMARY.md` when done.
</output>
