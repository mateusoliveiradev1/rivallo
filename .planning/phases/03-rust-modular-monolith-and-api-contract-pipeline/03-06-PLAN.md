---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "06"
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - package.json
  - scripts/check-rust-quality.mjs
  - scripts/run-quality.mjs
  - tooling-tests/workspace-config.test.mjs
  - tooling-tests/phase-3-scope.test.mjs
  - docs/operations/local-development.md
  - docs/testing/strategy.md
autonomous: true
requirements: [FOUND-03, DATA-02]
must_haves:
  truths:
    - "Root commands separately expose Rust architecture, OpenAPI/client writers, and non-mutating contract checks; pnpm check calls verification only."
    - "Rust formatting, warnings-denied Clippy, nextest, architecture audit, and both drift checks validate the real Phase-3 workspace with RUSTUP_AUTO_INSTALL=0."
    - "The full Rust contracts to OpenAPI to generated TypeScript proof has no runtime product registration and no later-phase implementation."
  artifacts:
    - "tooling-tests/phase-3-scope.test.mjs"
    - "docs/operations/local-development.md"
    - "docs/testing/strategy.md"
  key_links:
    - "pnpm check -> toolchains -> rust checks -> architecture -> OpenAPI/client drift checks"
    - "pipeline proof -> Rust contracts -> openapi.json -> generated client package"
  prohibitions:
    - statement: "Verification commands must not invoke writers or mutate tracked OpenAPI/client artifacts."
      status: planned
      verification: "Task 1 configuration test rejects writer calls from `pnpm check`; Task 2 runs both isolated drift checks."
    - statement: "The domain must not acquire direct or transitive framework, platform, network, persistence, frontend, or database contamination."
      status: planned
      verification: "Task 1 retains `pnpm rust:architecture` in the aggregate path; Plan 01 metadata policy tests controlled and live graphs."
    - statement: "Schema-only export must not become a production endpoint; any required fixture is strictly test-only and absent from runtime registration."
      status: planned
      verification: "Task 2 scope proof checks the schema-first path and asserts production-registration absence for any fixture."
    - statement: "No football/product behavior, runtime endpoints, UI/React/Tauri, persistence, Docker, CI, authentication, infrastructure, or multiplayer implementation may enter Phase 3."
      status: planned
      verification: "Task 2 targeted source/script inventory fails on prohibited additions; Task 3 documentation keeps later-phase ownership explicit."
---

<objective>
Wire the Phase 3 quality surface, end-to-end contract proof, scope protections, and developer documentation.

Purpose: make the new modular foundation repeatable and verifiable without claiming Phase 4+ runtime coverage.
Output: atomic root commands, aggregate verification-only orchestration, pipeline/scope tests, and accurate local guidance.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/02-workspace-toolchains-quality-scripts/02-VERIFICATION.md
@scripts/check-rust-quality.mjs
@scripts/run-quality.mjs
@docs/operations/local-development.md
@docs/testing/strategy.md
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Extend root quality commands for real members and contract verification</name>
<files>package.json, scripts/check-rust-quality.mjs, scripts/run-quality.mjs, tooling-tests/workspace-config.test.mjs</files>
<read_first>
- `03-SPEC.md` requirement 7, constraints, and acceptance criteria: real Rust checks, explicit writers versus non-mutating checks, no-auto-install, and clean-checkout repeatability.
- `03-CONTEXT.md` D-12 plus Existing Code Insights/Established Patterns: root command contract and cross-platform Node orchestration.
- `03-VALIDATION.md` row 03-05-01 and Sampling Rate: exact focused command and aggregate-after-wave validation cadence.
- `02-VERIFICATION.md`, `scripts/check-rust-quality.mjs`, `scripts/run-quality.mjs`, and `package.json`: prior quality behavior that must remain toolchain-first and writer-free in check mode.
- `03-02-SUMMARY.md`, `03-04-SUMMARY.md`, and `03-05-SUMMARY.md`: actual crate architecture and drift commands being integrated.
</read_first>
<behavior>
- Rust fmt checks source, Clippy denies warnings, and nextest runs real workspace tests.
- Atomic writer commands are distinct from OpenAPI/client verifier commands.
- pnpm check remains toolchain-first, fail-fast, and invokes only non-mutating verification paths.</behavior>
<action>Update the existing portable Node adapters rather than adding shell-specific orchestration. Preserve `RUSTUP_AUTO_INSTALL=0` for every Rust/Cargo child process, switch the non-empty workspace branch to source-level formatting, warnings-denied Clippy, and nextest, and add atomic root scripts for architecture audit, OpenAPI writer/check, and client writer/check. Update the aggregate order so `pnpm check` invokes toolchain validation first and thereafter only check commands; generation writers remain explicit and separate. Extend configuration tests to require the real Phase-3 scripts and to reject a check aggregate that calls either writer.</action>
<verify><automated>pnpm rust:fmt &amp;&amp; pnpm rust:clippy &amp;&amp; pnpm rust:test &amp;&amp; pnpm rust:architecture &amp;&amp; pnpm check</automated></verify>
<done>Root quality commands validate actual Rust source and contracts while aggregate verification never repairs generated artifacts.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Prove the complete pipeline and enforce Phase 3 scope fences</name>
<files>tooling-tests/phase-3-scope.test.mjs</files>
<read_first>
- `03-SPEC.md` requirements 6 and 8, all Prohibitions, and Acceptance Criteria: full provenance proof and exact forbidden-scope inventory.
- `03-CONTEXT.md` D-13 through D-16: schema-first preference and strictly test-only fixture contract.
- `03-RESEARCH.md` Verification Checklist, Anti-Patterns to Avoid, and Security Considerations: required pipeline checks and prohibited runtime/framework patterns.
- `03-VALIDATION.md` row 03-05-02 and Wave 0 Requirements: focused proof command, threat behavior, and required test file.
- `03-04-SUMMARY.md` and `03-05-SUMMARY.md`: exporter/client interfaces and generated artifacts whose provenance must be asserted.
- `scripts/verify-cargo-architecture.mjs`, `scripts/verify-openapi-drift.mjs`, and `scripts/verify-contract-client-drift.mjs`: existing checks to compose without turning verification into a writer.
</read_first>
<behavior>
- A test proves contracts schemas/version produce tracked OpenAPI and generated TypeScript types/client/metadata.
- Repository inventory fails if runtime registration, product behavior, desktop integration, persistence, identity, infrastructure, or multiplayer source is introduced.
- The schema-first path proves no fixture is required; if tool behavior forces one, tests prove it is test-only and absent from production registration.</behavior>
<action>Build an automated integration proof that runs the non-mutating OpenAPI and client drift checks and asserts provenance across Rust contracts, `contracts/openapi.json`, and the generated package. Add a targeted source/script inventory protecting every Phase 3 prohibition from the specification, while allowing generator and test files. Prefer the schema-only document selected in research. Only if generator mechanics demonstrably require an operation, introduce a neutral introspection fixture in test-only source and add an automated absence-from-production-registration assertion; do not create a runtime artifact merely to satisfy the proof.</action>
<verify><automated>pnpm test -- tooling-tests/phase-3-scope.test.mjs &amp;&amp; pnpm contracts:openapi:check &amp;&amp; pnpm contracts:client:check</automated></verify>
<done>The complete contract path is mechanically proven, and protected scope tests make runtime/product leakage fail fast.</done>
</task>

<task type="auto">
<name>Task 3: Document generation versus repeatable verification accurately</name>
<files>docs/operations/local-development.md, docs/testing/strategy.md</files>
<read_first>
- `03-SPEC.md` requirement 7 and requirement 8 boundaries: exact supported Phase 3 commands and later-phase exclusions to document.
- `03-CONTEXT.md` Canonical References and D-12: command split, no-auto-install policy, and phase-boundary language.
- `03-VALIDATION.md` Test Infrastructure, Sampling Rate, and row 03-05-03: documented command expectations and clean-tree repeatability proof.
- `02-CONTEXT.md`, `02-VERIFICATION.md`, `docs/operations/local-development.md`, and `docs/testing/strategy.md`: carried-forward toolchain/documentation baseline and exact sections to revise.
- Completed Plan 02/04/05 summaries and root `package.json`: final crate responsibilities plus actual writer/check command names.
</read_first>
<action>Revise local-development and test-strategy guidance for the real Phase 3 members. Document supported-platform prerequisites, exact explicit generation commands, exact non-mutating verification commands, failure remediation for drift, serialized writer expectations, and the carried-forward no-auto-install policy. State the crate responsibilities and boundary audit coverage, and clearly reserve executable API routes, desktop integration, persistence, infrastructure, identity, and product behavior for later phases. Do not claim CI, runtime health checks, or API delivery exists yet.</action>
<verify><automated>pnpm format:check &amp;&amp; pnpm check</automated></verify>
<done>Documentation lets a developer reproduce writers and checks from a clean checkout and distinguishes the Phase 3 foundation from later runtime work.</done>
</task>

</tasks>

<verification>
Run `pnpm check` twice from a clean tracked tree, run `git diff --check`, and confirm the second verification does not alter `contracts/openapi.json` or `packages/contracts-client`.
</verification>

<success_criteria>
All declared Phase 3 checks are real, repeatable, and non-mutating in verification mode; the pipeline proof and scope guard prevent premature product/runtime delivery.
</success_criteria>

## Artifacts this phase produces

- Root generation/check command surface integrated with Phase 2 quality behavior.
- End-to-end pipeline and Phase-boundary regression tests.
- Updated local-development and testing guidance.

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-06-SUMMARY.md` when done.
</output>
