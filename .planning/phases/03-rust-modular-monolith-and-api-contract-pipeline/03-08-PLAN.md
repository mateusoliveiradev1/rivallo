---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "08"
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - packages/contracts-client/openapi-ts.config.ts
  - packages/contracts-client/src/index.ts
  - scripts/verify-contract-client-drift.mjs
  - tooling-tests/contracts-client-generation.test.mjs
  - tooling-tests/phase-3-scope.test.mjs
  - .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DATA-02]
must_haves:
  truths:
    - "Consumers can import the generated Fetch client and its generated configuration/type surface from @rivallo/contracts-client."
    - "The approved bundled generator's dormant auth, SSE, and internal support modules remain private and inert: they are not exported, configured, invoked, or reachable through generated public types or metadata."
    - "The public generated client enables no authentication, retry, or backoff behavior and adds no external runtime package."
    - "Client drift verification regenerates in an isolated temporary directory and proves the tracked package remains unchanged."
  artifacts:
    - path: "packages/contracts-client/openapi-ts.config.ts"
      provides: "Pinned bundled-generator configuration that preserves the approved D-10 exception boundary."
    - path: "packages/contracts-client/src/index.ts"
      provides: "Public package export boundary for generated types, metadata, and generated Fetch client."
    - path: "scripts/verify-contract-client-drift.mjs"
      provides: "Isolated deterministic regeneration and complete-tree byte comparison without mutating tracked output."
    - path: "tooling-tests/contracts-client-generation.test.mjs"
      provides: "Public-import, dormant-module reachability, behavior, inventory, and non-mutating drift regressions."
  key_links:
    - from: "packages/contracts-client/src/index.ts"
      to: "packages/contracts-client/src/generated/client.gen.ts"
      via: "transparent ESM re-exports of generator-owned client symbols only"
      pattern: "generated/(client\\.gen|client/index)"
    - from: "packages/contracts-client/openapi-ts.config.ts"
      to: "scripts/generate-contract-client.mjs"
      via: "the pinned @hey-api/openapi-ts 0.97.3 CLI receives only the local OpenAPI configuration"
      pattern: "openapi-ts.*--file"
    - from: "scripts/verify-contract-client-drift.mjs"
      to: "packages/contracts-client/src/generated"
      via: "temporary regeneration and complete-tree byte comparison"
      pattern: "CONTRACT_CLIENT_OUTPUT"
  prohibitions:
    - statement: "D-10's approved exception permits only dormant generator-owned core modules; it does not permit public reachability or enabled authentication, retry, or backoff behavior."
      status: planned
      verification: "Focused package-entrypoint, source/inventory, generated-public-type/metadata, and behavior tests fail if the dormant modules become public or active."
    - statement: "Generated files must not be manually edited."
      status: planned
      verification: "The explicit writer regenerates the complete tree through the pinned generator; isolated byte-for-byte drift verification passes afterward."
    - statement: "Verification must not repair or mutate tracked generated artifacts."
      status: planned
      verification: "The focused verifier test snapshots every committed generated byte before and after the isolated check."
---

<objective>
Close the two verified D-10 generated-client gaps without broadening Phase 3: make the approved generated Fetch client reachable from the package API and enforce the approved exception boundary around the bundled generator core.

Purpose: DATA-02's contract foundation is blocked until the client advertised by the dedicated package is usable while generator-owned dormant core remains private and behaviorally inactive.
Output: bundled-generator configuration, transparent public exports, and regression/drift/scope proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VERIFICATION.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-06-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Preserve bundled generation and expose only its Fetch-client public surface</name>
<files>packages/contracts-client/openapi-ts.config.ts, packages/contracts-client/src/index.ts</files>
<read_first>
- `03-VERIFICATION.md` gaps 1 and 2 and `03-08-FEASIBILITY.md`: the unavailable public client and the exact bundled-core limitation.
- `03-CONTEXT.md` D-09 through D-12, including the 2026-07-14 approved D-10 exception: dedicated package, narrow exception, generated-only ownership, and non-mutating verification.
- `03-RESEARCH.md` Package Legitimacy Audit: retain only the human-approved exact `@hey-api/openapi-ts@0.97.3` package and the committed local OpenAPI input.
- `packages/contracts-client/openapi-ts.config.ts`, `src/index.ts`, `src/generated/client.gen.ts`, and `src/generated/client/index.ts`: verify the exact generated Fetch exports before naming them in the transparent root re-export.
</read_first>
<behavior>
- The exact approved generator consumes only `contracts/openapi.json`, remains bundled, and emits deterministic generated types, metadata, and Fetch-client functionality.
- The package root transparently exposes generated Fetch-client/configuration symbols without exposing dormant core modules, handwritten transport behavior, or duplicate contract models.
- No configuration adds auth, retry, backoff, an external runtime package, or a generator-version change.
</behavior>
<action>Per D-09, D-10's approved exception, D-11, and D-12, retain the exact `@hey-api/openapi-ts@0.97.3` bundled generation path, local relative OpenAPI input, and current explicit writer/check split. Do not add `@hey-api/client-fetch`, alter the generator version, or add any external runtime dependency. Keep configuration free of security schemes and generated SDK auth configuration; remove an SDK plugin only if regeneration proves it is unnecessary for the committed schema-only contract and does not alter required generated types, metadata, or Fetch-client functionality. In the handwritten package root, transparently re-export only the generated `client`, factory/configuration symbols, and their generated public types after verifying their exact names in regenerated output. Do not re-export a generated core directory, auth/SSE/internal module, or a type/metadata path that exposes one; do not create a wrapper client, transport, auth hook, retry policy, or duplicate request/response model. Regenerate only through the explicit writer whenever configuration changes; never edit files under `src/generated`.</action>
<verify><automated>pnpm contracts:client:generate &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm test -- tooling-tests/contracts-client-generation.test.mjs</automated></verify>
<done>The root package exposes the generated Fetch public surface while retaining the bundled generator and all dormant generator-core modules as non-public implementation detail, with no new runtime package or enabled auth/retry/backoff configuration.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Prove dormant core is private and behaviorally inactive without mutating drift checks</name>
<files>tooling-tests/contracts-client-generation.test.mjs, tooling-tests/phase-3-scope.test.mjs, scripts/verify-contract-client-drift.mjs, .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md</files>
<read_first>
- `03-VERIFICATION.md` Key Link Verification and Disconfirmation Findings: package-level import is the missing public-boundary proof and package source is absent from the existing scope inventory.
- `03-CONTEXT.md` D-09 through D-12 and the approved D-10 exception, plus `03-SPEC.md` requirements 3-5/8: generated-only package ownership, bounded minimum client, deterministic client drift, and Phase 3 exclusions.
- `packages/contracts-client/package.json`, `src/index.ts`, `src/generated/client.gen.ts`, `src/generated/client/index.ts`, generated `types.gen.ts`, and generated metadata/barrel files: the public ESM boundary and every symbol/type path that could expose dormant core.
- `tooling-tests/contracts-client-generation.test.mjs`, `tooling-tests/phase-3-scope.test.mjs`, and `scripts/verify-contract-client-drift.mjs`: existing focused provenance, non-mutation, and scope-fence patterns to extend.
- `03-VALIDATION.md`: append only the two Wave 7 gap-closure validation rows using the established command/status format.
</read_first>
<behavior>
- A consumer import from `@rivallo/contracts-client` reaches the generated Fetch client and its generated configuration/type surface without importing a private generated path.
- Source and inventory tests allow only the approved dormant generator-core files while proving no root export, generated public type, or generated metadata makes auth, SSE, or internal support usable.
- Tests prove the committed OpenAPI/configuration has no security schemes and the publicly usable generated client has no configured or invoked authentication, retry, or backoff behavior.
- The client drift checker regenerates outside the repository and compares the complete post-regeneration inventory and bytes without changing the tracked package.
</behavior>
<action>Write source/inventory tests before accepting the public export change. Import only `@rivallo/contracts-client` through its declared export map and prove the generated Fetch client plus its documented generated configuration/type surface are usable. Enumerate the generated tree so the approved dormant auth, SSE, and internal support paths are explicit, generator-owned allowances rather than a broad exemption. Assert the package root, generated public barrel, generated public types, and generated metadata contain no export/re-export/reference that makes those dormant paths or symbols usable from the public entrypoint. Assert `contracts/openapi.json` has no security scheme and configuration/public client setup has no auth, retry, or backoff option, callback, hook, or invocation; do not require deleting dormant generator-owned implementation files. Extend the Phase 3 scope test to inspect the contracts-client source tree with these exact allowances, while retaining Phase 3's prohibition on handwritten product/auth behavior. Preserve the temporary-directory byte snapshot and complete-tree comparison so `pnpm contracts:client:check` remains writer-free. Append two Wave 7 validation rows that name the public-entrypoint/dormant-core proof and the non-mutating scope/drift proof.</action>
<verify><automated>pnpm test -- tooling-tests/contracts-client-generation.test.mjs tooling-tests/phase-3-scope.test.mjs &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm check &amp;&amp; git diff --check</automated></verify>
<done>Consumers can import the generated Fetch client from the public package; approved dormant generator-core files are expressly inventory-bounded and cannot be exported, reached through public generated types/metadata, configured, or invoked; and drift verification leaves tracked output unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed OpenAPI → generator | Contract input controls the shipped generated API surface. |
| generator output → package consumers | Only the declared public entrypoint may expose generated contract functionality. |
| temporary generation → tracked tree | Verification must compare output without repairing repository state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-12 | Elevation of privilege | generated client public boundary | mitigate | Restrict public re-exports and generated public type/metadata reachability; inventory-test the approved dormant core allowance and inactive behavior. |
| T-03-13 | Tampering | client drift verifier | mitigate | Regenerate only in a unique temporary directory, byte-compare complete inventory, and snapshot tracked bytes in tests. |
| T-03-14 | Information disclosure | public package entrypoint | accept | The package exposes only committed neutral contract metadata and generated client/type APIs; no credentials, sessions, or product data exist in Phase 3. |
| T-03-SC | Tampering | npm package provenance | mitigate | Retain only the recorded human-approved exact `@hey-api/openapi-ts@0.97.3` pin; no package installation or version change occurs. |
</threat_model>

<verification>
Run focused public-entrypoint, dormant-core inventory/reachability, behavior, and scope tests before and after regeneration, then run `pnpm contracts:client:check` and `pnpm check` from a clean tracked tree. Run `git diff --check`; after each non-mutating check, confirm `git status --short` has no generated-client changes.
</verification>

<success_criteria>
The Phase 3 generated contract package publicly exposes its generator-owned Fetch client; its complete generated tree is deterministically recreated by the approved pinned generator; the approved dormant core remains private and inactive with no enabled auth/retry/backoff behavior; and check mode remains isolated, actionable, and non-mutating.
</success_criteria>

## Gap-closure source audit

| Source | ID | Requirement or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Rust-owned deterministic contract pipeline remains usable without runtime product scope | 03-08 | COVERED | Repairs only the generated-client link and its D-10 inventory. |
| REQ | DATA-02 | Contract foundation provides stable generated client/types provenance | 03-08 | COVERED | Verification identified this requirement as blocked solely by these two gaps. |
| RESEARCH | — | Pinned approved Hey API generator, local input, deterministic temporary-tree verification | 03-08 | COVERED | No new package or generator is introduced. |
| CONTEXT | D-09 | Dedicated application-independent package | 03-08 | COVERED | Public package boundary is exercised directly. |
| CONTEXT | D-10 | Types, metadata, minimum client only; approved 2026-07-14 exception for dormant bundled core | 03-08 | COVERED | Public-boundary, generated-type/metadata reachability, inactive-behavior, and exact-inventory fence. |
| CONTEXT | D-11 | Mature deterministic generator without output edits | 03-08 | COVERED | Retains exact approved generator and regenerates complete output. |
| CONTEXT | D-12 | Explicit writers plus non-mutating verification | 03-08 | COVERED | Existing commands and temporary-tree drift behavior are retained and tested. |

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-08-SUMMARY.md` when done.
</output>
