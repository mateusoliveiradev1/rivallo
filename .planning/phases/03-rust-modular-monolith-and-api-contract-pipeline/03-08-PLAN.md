---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "08"
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - packages/contracts-client/openapi-ts.config.ts
  - packages/contracts-client/src/index.ts
  - packages/contracts-client/src/generated/index.ts
  - packages/contracts-client/src/generated/client.gen.ts
  - packages/contracts-client/src/generated/core/auth.gen.ts
  - packages/contracts-client/src/generated/core/serverSentEvents.gen.ts
  - scripts/generate-contract-client.mjs
  - scripts/verify-contract-client-drift.mjs
  - tooling-tests/contracts-client-generation.test.mjs
  - tooling-tests/phase-3-scope.test.mjs
  - .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DATA-02]
must_haves:
  truths:
    - "Consumers can import the generated Fetch client and its generated configuration/type surface from @rivallo/contracts-client."
    - "The committed generated-client tree contains only the D-10 minimum generated surface: types, contract metadata, and Fetch client support."
    - "Client drift verification regenerates in an isolated temporary directory and proves the tracked package remains unchanged."
  artifacts:
    - path: "packages/contracts-client/openapi-ts.config.ts"
      provides: "Pinned approved-generator configuration that emits only the D-10 minimum client surface."
    - path: "packages/contracts-client/src/index.ts"
      provides: "Public package export boundary for generated types, metadata, and generated Fetch client."
    - path: "scripts/generate-contract-client.mjs"
      provides: "Approved-generator wrapper that rejects output outside the configured minimum generated inventory."
    - path: "tooling-tests/contracts-client-generation.test.mjs"
      provides: "Public-import, generated-inventory, and non-mutating drift regressions."
  key_links:
    - from: "packages/contracts-client/src/index.ts"
      to: "packages/contracts-client/src/generated/client.gen.ts"
      via: "explicit ESM re-export of generator-owned client symbols"
      pattern: "generated/client\\.gen"
    - from: "packages/contracts-client/openapi-ts.config.ts"
      to: "scripts/generate-contract-client.mjs"
      via: "the pinned @hey-api/openapi-ts 0.97.3 CLI receives only the local OpenAPI configuration"
      pattern: "openapi-ts.*--file"
    - from: "scripts/verify-contract-client-drift.mjs"
      to: "packages/contracts-client/src/generated"
      via: "temporary regeneration and complete-tree byte comparison"
      pattern: "CONTRACT_CLIENT_OUTPUT"
  prohibitions:
    - statement: "D-10 prohibits generated authentication, retry/backoff, and product transport convenience mechanisms."
      status: planned
      verification: "Focused generated-inventory and package-import tests, plus a package-tree scope fence, fail if the approved configuration or generated tree exceeds the minimum surface."
    - statement: "Generated files must not be manually edited."
      status: planned
      verification: "The explicit writer regenerates the complete tree through the pinned generator; isolated byte-for-byte drift verification passes afterward."
    - statement: "Verification must not repair or mutate tracked generated artifacts."
      status: planned
      verification: "The focused verifier test snapshots every committed generated byte before and after the isolated check."
---

<objective>
Close the two verified D-10 generated-client gaps without broadening Phase 3: make the approved generated Fetch client reachable from the package API and ensure the generated tree contains no prohibited mechanism.

Purpose: DATA-02's contract foundation is blocked until the client advertised by the dedicated package is usable and remains the deliberately narrow contract-only client.
Output: narrowed pinned-generator configuration, generator-owned regenerated output, public package exports, and regression/drift/scope proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VERIFICATION.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-RESEARCH.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-06-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Configure and regenerate only the approved minimum client surface</name>
<files>packages/contracts-client/openapi-ts.config.ts, scripts/generate-contract-client.mjs, packages/contracts-client/src/generated/index.ts, packages/contracts-client/src/generated/client.gen.ts, packages/contracts-client/src/generated/core/auth.gen.ts, packages/contracts-client/src/generated/core/serverSentEvents.gen.ts</files>
<read_first>
- `03-VERIFICATION.md` gaps 1 and 2: the exact unreachable-client and generated-tree findings to close.
- `03-CONTEXT.md` D-09 through D-12: dedicated package, minimum surface, pinned deterministic generator, explicit writer/check split.
- `03-RESEARCH.md` Standard Stack and Package Legitimacy Audit: use only the human-approved exact `@hey-api/openapi-ts@0.97.3` package and the committed local OpenAPI input.
- `03-06-PLAN.md` Tasks 1-2 and `03-06-SUMMARY.md`: existing writer/check interfaces, generated-only ownership, and temporary-directory drift behavior to retain.
- `packages/contracts-client/openapi-ts.config.ts`, `scripts/generate-contract-client.mjs`, and the complete current generated tree: installed-generator configuration shape, wrapper arguments, and the emitted files that presently exceed D-10.
</read_first>
<behavior>
- The exact approved generator consumes only `contracts/openapi.json` and emits the same deterministic types, metadata, and Fetch-client functionality required by D-10.
- Generator configuration, rather than manual edits, prevents the excess generated modules from being emitted.
- The writer fails with an actionable error if the configured output inventory includes a prohibited D-10 mechanism, while leaving all generator-produced bytes otherwise untouched.
</behavior>
<action>First add the focused failing inventory assertions from Task 2 before changing production configuration. Retain the exact human-approved `@hey-api/openapi-ts@0.97.3` dependency, local relative OpenAPI input, and existing writer command. Use only configuration capabilities supported by that installed version to select its TypeScript and Fetch-client generation needed for D-10 while disabling generator features that produce the excess mechanisms identified in verification. Update the existing Node writer wrapper to validate the generated inventory after the approved CLI exits and to report the explicit writer command if configuration emits an out-of-scope module; it must never rewrite, prune, patch, or otherwise post-process generated file contents. Run the explicit writer to recreate the whole generated directory, including removals, and commit its output as generator-owned source. Do not hand-edit any file beneath `src/generated`.</action>
<verify><automated>pnpm test -- tooling-tests/contracts-client-generation.test.mjs &amp;&amp; pnpm contracts:client:generate &amp;&amp; pnpm contracts:client:check</automated></verify>
<done>The generated inventory is reproducible from the approved pin and contains the generated types, metadata, and Fetch client needed by D-10, with the verified excess modules absent because of configuration rather than manual modification.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Export the generated client and lock both gap regressions into non-mutating checks</name>
<files>packages/contracts-client/src/index.ts, tooling-tests/contracts-client-generation.test.mjs, tooling-tests/phase-3-scope.test.mjs, scripts/verify-contract-client-drift.mjs, .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md</files>
<read_first>
- `03-VERIFICATION.md` Key Link Verification and Disconfirmation Findings: package-level import is the missing public-boundary proof and package source is absent from the existing scope inventory.
- `03-CONTEXT.md` D-09 through D-12 and `03-SPEC.md` requirements 3-5/8: generated-only package ownership, minimum client, deterministic client drift, and Phase 3 exclusions.
- `packages/contracts-client/package.json`, `src/index.ts`, `src/generated/index.ts`, and `src/generated/client.gen.ts`: the current public ESM export map and generator-owned client symbols to expose without duplicating implementation.
- `tooling-tests/contracts-client-generation.test.mjs`, `tooling-tests/phase-3-scope.test.mjs`, and `scripts/verify-contract-client-drift.mjs`: existing focused provenance, non-mutation, and scope-fence patterns to extend.
- `03-VALIDATION.md`: append only the two Wave 7 gap-closure validation rows using the established command/status format.
</read_first>
<behavior>
- A consumer import from `@rivallo/contracts-client` reaches the generated Fetch client and generated configuration/type surface without importing private generated paths.
- Regression tests verify the configured generated inventory excludes all D-10-prohibited mechanism categories and that no handwritten contract model is introduced.
- The client drift checker regenerates outside the repository and compares the complete post-regeneration inventory and bytes without changing the tracked package.
</behavior>
<action>Extend the handwritten package boundary only as a transparent ESM re-export layer: retain generated type/metadata exports and explicitly re-export the generated Fetch client, factory/configuration symbols, and their generated types from their generator-owned modules. Do not create a handwritten client, transport abstraction, request/response model, or wrapper behavior. Update the generated-client test to import only the package name through its declared export map and prove those generated symbols are reachable; make its inventory assertion guard the D-10 minimum rather than merely scanning configuration text. Extend the Phase 3 scope fence to inspect the contracts-client source tree so prohibited generated mechanisms cannot re-enter unnoticed, with narrowly named generated-file allowances only where the approved minimum client requires them. Preserve and strengthen the temporary-output drift test so both the public export layer and every committed generated byte are unchanged by check mode. Append precise Wave 7 validation-map rows for this plan's generated-inventory/public-entrypoint proof and isolated drift/scope proof.</action>
<verify><automated>pnpm test -- tooling-tests/contracts-client-generation.test.mjs tooling-tests/phase-3-scope.test.mjs &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm check &amp;&amp; git diff --check</automated></verify>
<done>Consumers can import the generated Fetch client from the public package; targeted tests and non-mutating drift verification prevent both verified gaps from recurring without adding Phase 4+ transport or product scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed OpenAPI → generator | Contract input controls the shipped generated API surface. |
| generator output → package consumers | Only the declared public entrypoint may expose generated contract functionality. |
| temporary generation → tracked tree | Verification must compare output without repairing repository state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-12 | Elevation of privilege | generated client configuration/output | mitigate | Restrict the approved generator configuration to D-10's minimum inventory and enforce it in focused generated-tree tests. |
| T-03-13 | Tampering | client drift verifier | mitigate | Regenerate only in a unique temporary directory, byte-compare complete inventory, and snapshot tracked bytes in tests. |
| T-03-14 | Information disclosure | public package entrypoint | accept | The package exposes only committed neutral contract metadata and generated client/type APIs; no credentials, sessions, or product data exist in Phase 3. |
| T-03-SC | Tampering | npm package provenance | mitigate | Retain only the recorded human-approved exact `@hey-api/openapi-ts@0.97.3` pin; no package installation or version change occurs. |
</threat_model>

<verification>
Run focused generated-client and scope tests before and after regeneration, then run `pnpm contracts:client:check` and `pnpm check` from a clean tracked tree. Run `git diff --check`; after each non-mutating check, confirm `git status --short` has no generated-client changes.
</verification>

<success_criteria>
The Phase 3 generated contract package publicly exposes its generator-owned Fetch client; its complete generated tree is deterministically recreated by the approved pinned generator without D-10-prohibited mechanisms; and check mode remains isolated, actionable, and non-mutating.
</success_criteria>

## Gap-closure source audit

| Source | ID | Requirement or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Rust-owned deterministic contract pipeline remains usable without runtime product scope | 03-08 | COVERED | Repairs only the generated-client link and its D-10 inventory. |
| REQ | DATA-02 | Contract foundation provides stable generated client/types provenance | 03-08 | COVERED | Verification identified this requirement as blocked solely by these two gaps. |
| RESEARCH | — | Pinned approved Hey API generator, local input, deterministic temporary-tree verification | 03-08 | COVERED | No new package or generator is introduced. |
| CONTEXT | D-09 | Dedicated application-independent package | 03-08 | COVERED | Public package boundary is exercised directly. |
| CONTEXT | D-10 | Types, metadata, minimum client only | 03-08 | COVERED | Configuration-driven minimum inventory and regression fence. |
| CONTEXT | D-11 | Mature deterministic generator without output edits | 03-08 | COVERED | Retains exact approved generator and regenerates complete output. |
| CONTEXT | D-12 | Explicit writers plus non-mutating verification | 03-08 | COVERED | Existing commands and temporary-tree drift behavior are retained and tested. |

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-08-SUMMARY.md` when done.
</output>
