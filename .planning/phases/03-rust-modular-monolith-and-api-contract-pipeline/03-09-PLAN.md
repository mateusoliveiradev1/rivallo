---
phase: 03-rust-modular-monolith-and-api-contract-pipeline
plan: "09"
type: execute
wave: 8
depends_on: [03-08]
files_modified:
  - .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-09-APPROVAL.md
  - package.json
  - pnpm-lock.yaml
  - packages/contracts-client/openapi-ts.config.ts
  - packages/contracts-client/orval.config.ts
  - crates/platform/src/lib.rs
  - contracts/openapi.json
  - packages/contracts-client/src/generated/contracts.ts
  - packages/contracts-client/src/index.ts
  - scripts/generate-contract-client.mjs
  - tooling-tests/contracts-client-generation.test.mjs
  - tooling-tests/phase-3-scope.test.mjs
  - docs/operations/local-development.md
  - .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DATA-02]
must_haves:
  truths:
    - "The Rust-owned committed OpenAPI document deterministically generates versioned TypeScript types, metadata, and at least one direct Fetch operation through exact Orval 8.21.0."
    - "A consumer imports only generated contract types, metadata, and direct Fetch operations from @rivallo/contracts-client; no public auth, SSE, retry, or backoff surface exists."
    - "Client generation and whole-tree drift checking remain explicit, deterministic, isolated, actionable, and non-mutating in check mode."
    - "The neutral operation needed to prove the Fetch output is test-only, has no runtime registration, and adds no product or runtime behavior."
  artifacts:
    - path: ".planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-09-APPROVAL.md"
      provides: "Recorded 2026-07-14 approval for exact orval@8.21.0 and supersession of the temporary D-10 generated-core exception."
    - path: "packages/contracts-client/orval.config.ts"
      provides: "Pinned local OpenAPI-to-TypeScript configuration using official client: 'fetch' output."
    - path: "packages/contracts-client/src/generated/contracts.ts"
      provides: "Versioned generator-owned TypeScript contract types, metadata, and direct Fetch operation output."
    - path: "scripts/verify-contract-client-drift.mjs"
      provides: "Existing isolated complete-tree byte comparison retained for Orval output."
  key_links:
    - from: "contracts/openapi.json"
      to: "packages/contracts-client/orval.config.ts"
      via: "repository-local input.target only"
      pattern: "contracts/openapi\\.json"
    - from: "packages/contracts-client/orval.config.ts"
      to: "packages/contracts-client/src/generated/contracts.ts"
      via: "exact orval@8.21.0 with client: 'fetch'"
      pattern: "client:\\s*['\"]fetch['\"]"
    - from: "scripts/verify-contract-client-drift.mjs"
      to: "packages/contracts-client/src/generated"
      via: "temporary output override, complete inventory, and byte comparison"
      pattern: "CONTRACT_CLIENT_OUTPUT"
---

<objective>
Close the remaining D-10 public-reachability gap by replacing only the OpenAPI-to-TypeScript generator with the approved exact Orval Fetch generator.

Purpose: the bundled Hey API output cannot provide a usable generated Fetch client without exposing authentication and SSE/retry/backoff machinery. Orval's official direct-Fetch output removes that capability while retaining Rust-owned OpenAPI provenance and deterministic generated-only ownership.
Output: approval/supersession record, exact Orval configuration and lockfile, neutral test-only OpenAPI operation, regenerated public types/metadata/direct Fetch function, and strengthened deterministic drift/scope proof.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-CONTEXT.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-SPEC.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VERIFICATION.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-08-SUMMARY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-09-FEASIBILITY.md
@.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-10-GENERATOR-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Record the approved exact replacement and atomically replace generator ownership</name>
<files>.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-09-APPROVAL.md, package.json, pnpm-lock.yaml, packages/contracts-client/openapi-ts.config.ts, packages/contracts-client/orval.config.ts</files>
<read_first>
- `03-10-GENERATOR-RESEARCH.md` Summary, Standard Stack, Package Legitimacy Audit, Required Migration Shape, and Assumptions Log: exact approved package/version, official Fetch config, local input, no paired runtime package, and the `mode` compatibility observation.
- `03-09-FEASIBILITY.md` conclusion and public export graph: Hey API cannot meet D-10 through a barrel-only change.
- `03-VERIFICATION.md` remaining gap and disconfirmation findings: public reachability is executable, not merely an inventory problem.
- `03-CONTEXT.md` D-09 through D-12 and D-13 through D-16: generated-only package, narrow direct client, deterministic commands, and permitted neutral fixture conditions.
- `package.json`, `pnpm-lock.yaml`, `packages/contracts-client/openapi-ts.config.ts`, and `03-03-SUMMARY.md`: current exact package/configuration and the prior approval-record format.
</read_first>
<action>Record the user's 2026-07-14 approval for exact `orval@8.21.0` in `03-09-APPROVAL.md`, including the official provenance, SUS-for-recency-only result, verified absence of a postinstall lifecycle hook, and the fact that no further human checkpoint is required. State in that decision record that the dated D-10 bundled-core exception for `@hey-api/openapi-ts@0.97.3` is superseded and must no longer be retained as an inventory allowance. Per D-09 through D-12, remove the exact Hey API dependency, its lockfile resolution, and `openapi-ts.config.ts`; add only exact `orval@8.21.0`, lock it, and create `orval.config.ts` using `defineConfig`, the repository-local committed OpenAPI JSON input, `mode: 'single'`, `clean: true`, `formatter: 'prettier'`, and official `client: 'fetch'`. Do not configure a mutator, mock, query framework, runtime validation, base URL, security scheme, streaming response, remote input, auth behavior, retry/backoff behavior, or a separate Orval/runtime package. Before and after the dependency transaction, run the project package-legitimacy protections for the exact tarball/lockfile provenance and lifecycle-script inspection; do not waive the recorded no-postinstall finding and do not add package-install hooks.</action>
<verify><automated>pnpm install --frozen-lockfile &amp;&amp; pnpm exec orval --config packages/contracts-client/orval.config.ts --output /tmp/rivallo-orval-legitimacy-proof</automated></verify>
<done>The exact approved Orval package is the sole OpenAPI-to-TypeScript generator, the Hey API configuration/dependency/output ownership is removed, and a durable decision record supersedes the temporary D-10 generated-core exception without adding an install hook or runtime package.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add the neutral contract-introspection operation and regenerate the narrow public Fetch surface</name>
<files>crates/platform/src/lib.rs, contracts/openapi.json, packages/contracts-client/src/generated/contracts.ts, packages/contracts-client/src/index.ts</files>
<read_first>
- `03-10-GENERATOR-RESEARCH.md` Architectural Responsibility Map, Required Migration Shape items 4-5, Verification Contract, and Common Pitfalls: Orval Fetch output is operation-scoped and the only permitted fixture shape.
- `03-CONTEXT.md` D-01 through D-16, especially D-05/D-06 and D-13 through D-16: contracts own schemas/version, platform composes only, and the fixture is neutral/test-only/unregistered.
- `03-SPEC.md` requirements 3 through 8 and prohibitions: preserve schema ownership and no runtime route, listener, persistence, authentication, or product scope.
- `crates/platform/src/lib.rs`, `crates/contracts/src/lib.rs`, `contracts/openapi.json`, and the regenerated Orval output from Task 1: use the existing Utoipa/exporter ownership and actual generated symbol names rather than guessing an API.
- `packages/contracts-client/package.json` and `src/index.ts`: retain the declared package export map and a transparent generated-only root boundary.
</read_first>
<behavior>
- The committed OpenAPI document has exactly one neutral contract-introspection operation returning `ContractManifest`, with no parameters, security requirement, streaming content, state, or runtime registration.
- Regeneration creates versioned types/metadata and at least one generated direct Fetch operation that accepts ordinary `RequestInit` options only.
- The package root exposes generated types, metadata, and operation functions without a handwritten request wrapper or duplicate request/response model.
</behavior>
<action>After a temporary dry run establishes that the schema-only document has no callable Fetch operation, add exactly one D-13 through D-16-compliant Utoipa operation at the existing Rust contract-composition/export boundary. It must be named and documented as test-only contract introspection, return only the already-owned `ContractManifest`, have no request parameters, security scheme, state, football data, persistence, authentication, SSE/NDJSON streaming, listener, endpoint registration, or runtime artifact. Keep contracts as the canonical schema/version owner and platform as the composition/exporter-only boundary per D-01 through D-06. Regenerate `contracts/openapi.json` only through the established Rust writer, then regenerate the entire TypeScript output only through the new explicit Orval writer. Replace the former Hey API generated tree with the deterministic Orval output and make `src/index.ts` a transparent export of generated types, metadata, and direct operation functions only. Do not hand-maintain a client, request/response type, auth-header injection, retry wrapper, backoff policy, SSE adapter, or any product/runtime implementation.</action>
<verify><automated>pnpm contracts:openapi:generate &amp;&amp; pnpm contracts:client:generate &amp;&amp; pnpm contracts:openapi:check &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm typecheck</automated></verify>
<done>Rust-owned OpenAPI deterministically yields a versioned, generated-only contracts package with a callable direct Fetch operation and no public authentication, SSE, retry, or backoff capability.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Preserve whole-tree drift proof and fence the new generated public surface</name>
<files>scripts/generate-contract-client.mjs, tooling-tests/contracts-client-generation.test.mjs, tooling-tests/phase-3-scope.test.mjs, docs/operations/local-development.md, .planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-VALIDATION.md</files>
<read_first>
- `scripts/generate-contract-client.mjs` and `scripts/verify-contract-client-drift.mjs`: retain the portable local CLI invocation, `CONTRACT_CLIENT_OUTPUT` redirect, fresh temporary directory, complete inventory comparison, byte comparison, cleanup, and actionable writer command.
- `tooling-tests/contracts-client-generation.test.mjs` and `tooling-tests/phase-3-scope.test.mjs`: replace the obsolete Hey API inventory assertions with consumer-import, complete-tree, non-mutation, and test-only-fixture fences.
- `03-VERIFICATION.md` D-10 failed truth, Key Link Verification, and Disconfirmation Findings: tests must prove transitive public reachability is impossible, not only inspect a direct barrel or defaults.
- `03-10-GENERATOR-RESEARCH.md` Verification Contract and Security Domain: source/runtime checks for the direct Fetch surface and isolated deterministic drift checks.
- `docs/operations/local-development.md` and `03-VALIDATION.md`: existing writer/check guidance and validation-row format.
</read_first>
<behavior>
- The only client writer invokes the locally locked Orval CLI and honors `CONTRACT_CLIENT_OUTPUT`; the existing check regenerates a complete fresh tree and never modifies tracked output.
- A root-package consumer can import the generated direct Fetch operation, types, and metadata, while no generated/public source or callable surface includes auth, security, SSE, retry, or backoff behavior.
- Tests prove the neutral operation exists only in the contract-generation path and has no production runtime registration; Phase 3 scope remains otherwise schema-only.
</behavior>
<action>Adapt the portable Node writer from Hey API's CLI to the locally locked Orval CLI while preserving the exact temporary-output override consumed by the existing whole-tree drift verifier; do not turn check mode into a writer or weaken byte/inventory comparison. Replace every Hey API-specific generated inventory assertion with assertions against the regenerated Orval tree and actual generated exports. Import only the package root to prove generated types, version metadata, and the direct Fetch operation are usable; recursively inspect generated public types and runtime source so a transitive public auth/security/SSE/retry/backoff path fails. Assert the operation accepts only standard `RequestInit` transport options and make no network call in the test. Extend the scope proof to establish that the neutral operation is unregistered and that no new endpoint, listener, product model, authentication, persistence, retry, or streaming implementation was introduced. Update developer guidance to name exact Orval generation, the explicit writer versus writer-free checks, and the neutral test-only operation boundary. Append Wave 8 validation rows for package-legitimacy/no-install-hook evidence, generated direct-Fetch public-surface proof, isolated complete-tree drift proof, and fixture non-registration. Run only repository test/build commands; do not add a runtime package, a custom transport, or hand-maintained contract model.</action>
<verify><automated>pnpm exec vitest run tooling-tests/contracts-client-generation.test.mjs tooling-tests/phase-3-scope.test.mjs &amp;&amp; pnpm contracts:client:check &amp;&amp; pnpm check &amp;&amp; git diff --check</automated></verify>
<done>The package exposes only Orval-generated direct Fetch types/metadata/operations, tests detect transitive forbidden transport capability, the whole generated tree is checked without tracked-file mutation, and documentation/validation accurately describe the migrated bounded pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry/lockfile → local generator | Exact approved build-time package must retain verified provenance and no lifecycle hook. |
| committed Rust/OpenAPI → generated package | Contract input defines every publicly generated operation and type. |
| generated package → consumer | Only generated direct Fetch symbols may cross the package-root boundary. |
| temporary generation → tracked tree | Drift verification must detect changes without repairing them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-15 | Tampering | generator dependency | mitigate | Record approval, exact pin/lock integrity, official provenance, and lifecycle-hook inspection before migration. |
| T-03-16 | Elevation of privilege | generated public transport surface | mitigate | Use official Orval direct Fetch without mutator and recursively test public types/runtime for forbidden auth, SSE, retry, or backoff reachability. |
| T-03-17 | Tampering | generated drift verification | mitigate | Preserve temporary output override, complete-tree inventory, byte comparison, and tracked-byte non-mutation tests. |
| T-03-18 | Spoofing | neutral OpenAPI operation | mitigate | Restrict it to test-only contract introspection, prove no runtime registration, parameters, security, or state. |
| T-03-SC | Tampering | package installation | mitigate | The approved exact package has no postinstall hook; do not add lifecycle hooks or secondary runtime packages. |
</threat_model>

<verification>
Run the package-legitimacy/provenance and lifecycle-hook inspection before dependency mutation; then run the OpenAPI writer, Orval writer twice, focused generated-client/scope tests, both isolated drift checks, `pnpm typecheck`, and writer-free `pnpm check`. Confirm `git status --short` is unchanged after each check-mode command and run `git diff --check` before committing.
</verification>

<success_criteria>
The Phase 3 pipeline preserves its Rust-to-committed-OpenAPI authority and deterministic generated-only TypeScript ownership while exact Orval 8.21.0 creates a usable direct Fetch operation with no public auth, SSE, retry, or backoff reachability. The superseded Hey API exception and all of its config/output/dependency traces are removed, no runtime package or product/runtime scope is introduced, and checks remain whole-tree, isolated, and non-mutating.
</success_criteria>

## Gap-closure source audit

| Source | ID | Requirement or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Rust-owned deterministic API contract pipeline without runtime product scope | 03-09 | COVERED | Replaces only the failing TS generator layer and preserves Rust/OpenAPI. |
| REQ | FOUND-03 | Framework-independent domain/application boundaries | 03-01 through 03-08 | COVERED | This migration does not alter Rust boundary ownership. |
| REQ | DATA-02 | Stable generated client/types contract provenance | 03-09 | COVERED | Direct Fetch output, local input, exact pin, and drift proof close the blocked requirement. |
| RESEARCH | — | Exact Orval Fetch recommendation, Node compatibility, local input, no mutator/runtime package | 03-09 | COVERED | Task 1 locks/configures; Tasks 2-3 generate and prove the surface. |
| RESEARCH | — | Operation-scoped Fetch generation needs neutral fixture; no streaming output | 03-09 | COVERED | Task 2 adds only the permitted test-only introspection operation after dry-run evidence. |
| RESEARCH | — | Isolated full-tree byte comparison and writer-free checks | 03-09 | COVERED | Task 3 preserves and exercises the existing verifier. |
| CONTEXT | D-01 to D-08 | Rust dependency direction, contracts ownership, schema-only exporter | 03-01 through 03-08; 03-09 | COVERED | Task 2 preserves these boundaries while adding only the allowed generator fixture. |
| CONTEXT | D-09 | Dedicated application-independent generated package | 03-09 | COVERED | Orval output remains solely in `packages/contracts-client`. |
| CONTEXT | D-10 | Types/metadata/minimum client only, no auth/retry/product conveniences | 03-09 | COVERED | Direct Fetch operation only; former temporary generated-core exception is superseded. |
| CONTEXT | D-11 | Mature deterministic generator without output edits | 03-09 | COVERED | Exact approved Orval pin and regenerated-only output. |
| CONTEXT | D-12 | Explicit writers and non-mutating verification | 03-09 | COVERED | Writer adapter plus existing isolated checker are retained and tested. |
| CONTEXT | D-13 to D-16 | Neutral test-only fixture only when required, unregistered and tested | 03-09 | COVERED | Task 2 dry-run gate and Task 3 scope regression prove the constrained fixture. |

<output>
Create `.planning/phases/03-rust-modular-monolith-and-api-contract-pipeline/03-09-SUMMARY.md` when done.
</output>
