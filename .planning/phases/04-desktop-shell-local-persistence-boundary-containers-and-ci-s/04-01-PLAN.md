---
phase: 04-desktop-shell-local-persistence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-PACKAGE-REVIEW.md
autonomous: false
requirements: [FOUND-01]
must_haves:
  truths:
    - "Every SUS npm package required for the desktop shell is explicitly approved by the user before its installation."
  artifacts:
    - path: ".planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-PACKAGE-REVIEW.md"
      provides: "Audited registry provenance, lifecycle-script evidence, exact versions, and the human approval record for SUS desktop packages."
  key_links:
    - from: "04-01-PACKAGE-REVIEW.md"
      to: "package.json and pnpm-lock.yaml"
      via: "The later installation task may use only the exact versions approved here."
---

<objective>
Record the mandatory human review of the suspicious-for-recency npm packages before any desktop dependency transaction.

Purpose: research marked the Tauri CLI/API and Vite packages SUS solely because their registry publications are recent; the shell cannot install them until the user has reviewed their provenance and lifecycle evidence.
Output: a durable package-review record and a blocking user approval.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-CONTEXT.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Capture exact registry and lifecycle evidence for the SUS desktop packages</name>
<files>.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-PACKAGE-REVIEW.md</files>
<action>Before changing any manifest or lockfile, query the npm registry for exact `@tauri-apps/cli@2.11.4`, `@tauri-apps/api@2.11.1`, `vite@8.1.4`, and `@vitejs/plugin-react@6.0.3`. Record publisher/repository provenance, version, integrity/tarball metadata, publication date, and lifecycle-script inspection in the review record. State that each package is SUS for recency only, that research found no postinstall hook, and that no installation has occurred. Per D-01 through D-04, do not substitute another process-management stack while collecting evidence.</action>
<verify><automated>pnpm view @tauri-apps/cli@2.11.4 repository dist.integrity scripts --json &amp;&amp; pnpm view @tauri-apps/api@2.11.1 repository dist.integrity scripts --json &amp;&amp; pnpm view vite@8.1.4 repository dist.integrity scripts --json &amp;&amp; pnpm view @vitejs/plugin-react@6.0.3 repository dist.integrity scripts --json</automated></verify>
<done>The review record contains reproducible provenance and lifecycle evidence for all four exact SUS packages without modifying dependencies.</done>
</task>

<task type="checkpoint:human-verify">
<name>Task 2: Approve or reject the exact SUS package set before installation</name>
<what-built>Registry provenance and lifecycle evidence for the exact Tauri CLI/API, Vite, and React-plugin packages.</what-built>
<how-to-verify>Review `04-01-PACKAGE-REVIEW.md`. If acceptable, add your name, date, and explicit approval for all four exact packages; otherwise state the rejected package and reason.</how-to-verify>
<action>Pause for the user's package-legitimacy decision. Do not run `pnpm install`, edit `package.json`, edit the lockfile, scaffold an app, or add any of the reviewed packages until the approval record contains an explicit approval covering every exact package. A rejection blocks all downstream plans that install the SUS package set.</action>
<verify><automated>node -e "const fs=require('node:fs'); const s=fs.readFileSync('.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-PACKAGE-REVIEW.md','utf8'); if (!/APPROVED/.test(s)) process.exit(1)"</automated></verify>
<done>An explicit user decision is recorded before any SUS npm package is installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → repository dependency set | A recent package can execute install-time code or change the desktop supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Tampering | SUS npm packages | mitigate | Record exact provenance and lifecycle evidence, then require an explicit human approval before installation. |
| T-04-SC | Tampering | dependency transaction | mitigate | No manifest or lockfile mutation may precede the approval checkpoint. |
</threat_model>

<verification>
The record must contain all four exact packages, registry evidence, lifecycle-script evidence, and an explicit APPROVED decision before Plan 04-03 may run.
</verification>

<success_criteria>
The required review is auditable and blocks dependency installation until the user explicitly approves the exact SUS packages.
</success_criteria>

<output>
Create `.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-SUMMARY.md` when done.
</output>
