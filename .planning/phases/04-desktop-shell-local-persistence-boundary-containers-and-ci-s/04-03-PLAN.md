---
phase: 04-desktop-shell-local-persistence
plan: "03"
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [package.json, pnpm-lock.yaml, tsconfig.json, apps/desktop/package.json, apps/desktop/vite.config.ts, apps/desktop/index.html, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/build.rs, apps/desktop/src-tauri/tauri.conf.json]
autonomous: true
requirements: [FOUND-01]
must_haves:
  truths: ["The approved desktop workspace has a reproducible Vite/React webview and named Tauri sidecar configuration without lifecycle or product UI behavior."]
  artifacts:
    - path: apps/desktop/package.json
      provides: Desktop scripts and the generated-contract dependency boundary.
  key_links:
    - from: apps/desktop/package.json
      to: packages/contracts-client/src/index.ts
      via: The webview declares the generated client as its sole TypeScript transport-contract dependency.
---

<objective>
Create the approved reproducible desktop workspace and packaging configuration.
Purpose: D-01 through D-04 need a buildable host foundation before lifecycle code; the dependency approval from 04-01 remains mandatory.
Output: Vite/React package, root wiring, and Tauri sidecar packaging configuration.
</objective>

<execution_context>
@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-CONTEXT.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-RESEARCH.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-01-PACKAGE-REVIEW.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Install approved webview dependencies and declare the workspace</name>
<files>package.json, pnpm-lock.yaml, tsconfig.json, apps/desktop/package.json, apps/desktop/vite.config.ts</files>
<action>Proceed only after 04-01 explicitly approves every SUS package. Add the approved exact Tauri CLI/API, Vite, React plugin, React, and React DOM versions; update the frozen lockfile; declare `@rivallo/desktop` scripts; and extend root TypeScript/quality configuration. Keep `@rivallo/contracts-client` as the sole TypeScript transport-contract dependency. Per D-06, do not add SQLite, direct shell access, a browser backend, custom HTTP client, lifecycle logic, or product UI.</action>
<verify><automated>pnpm install --frozen-lockfile &amp;&amp; pnpm --filter @rivallo/desktop typecheck &amp;&amp; pnpm format:check &amp;&amp; pnpm lint</automated></verify>
<done>The approved dependency set and typed desktop workspace install reproducibly.</done>
</task>
<task type="auto">
<name>Task 2: Configure the named Tauri sidecar package boundary</name>
<files>apps/desktop/index.html, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/build.rs, apps/desktop/src-tauri/tauri.conf.json</files>
<action>Configure the React webview entrypoint and Tauri build metadata to package the platform `local_api` binary by its fixed name. Declare only the approved host/build dependencies. Preserve the platform binary as the sole source of readiness behavior; defer shell permissions, host commands, process management, and UI lifecycle state to 04-04.</action>
<verify><automated>pnpm exec tauri build --config apps/desktop/src-tauri/tauri.conf.json --bundles none</automated></verify>
<done>The desktop package resolves its named local API sidecar without a duplicate runtime or lifecycle implementation.</done>
</task>
</tasks>

<verification>Run the two mapped checks in 04-VALIDATION.md and preserve a non-mutating worktree.</verification>
<success_criteria>The workspace is ready for the dependency-ordered lifecycle host plan.</success_criteria>
<output>Create `.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-03-SUMMARY.md` when done.</output>
