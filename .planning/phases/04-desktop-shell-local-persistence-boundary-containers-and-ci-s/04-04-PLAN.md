---
phase: 04-desktop-shell-local-persistence
plan: "04"
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: [apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/capabilities/default.json, apps/desktop/src/main.tsx, apps/desktop/src/App.tsx, apps/desktop/src/styles.css, tooling-tests/phase-4-desktop.test.mjs]
autonomous: true
requirements: [FOUND-01]
must_haves:
  truths: ["The Tauri host manages only children it created and reuses only an exactly compatible service.", "D-03: The desktop shows an initializing state while it waits for `/ready` only for a short bounded interval; timeout or failure is a recoverable state with retry.", "After Ready, the host continues monitoring its owned child and loopback readiness; unexpected loss becomes a typed recoverable lifecycle status.", "The desktop visibly presents initializing, ready, and recoverable failure states without ordinary-UI raw logs."]
  artifacts:
    - path: apps/desktop/src-tauri/src/main.rs
      provides: Owned-child lifecycle state machine and typed command bridge.
    - path: apps/desktop/src/App.tsx
      provides: Accessible operational lifecycle shell.
  key_links:
    - from: apps/desktop/src-tauri/src/main.rs
      to: crates/platform/src/runtime.rs
      via: The host consumes the fixed readiness constants and strict validator.
    - from: apps/desktop/src-tauri/src/main.rs
      to: apps/desktop/src-tauri/src/main.rs
      via: The retained owned-child handle and continued readiness monitor map an unexpected post-Ready exit or probe loss to typed recoverable lifecycle status; reused responders have no child handle and receive no monitoring contact or termination.
    - from: apps/desktop/src/App.tsx
      to: apps/desktop/src-tauri/src/main.rs
      via: Typed status and retry commands without shell arguments.
---

<objective>
Deliver the Tauri-owned lifecycle state machine and its operational React shell.
Purpose: D-01 through D-04 make startup, compatible reuse, recovery, and owned-child shutdown host responsibilities.
Output: narrow host bridge, exact shell capability, accessible lifecycle UI, and source/behavior regression tests.
</objective>

<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-CONTEXT.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-RESEARCH.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-02-SUMMARY.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement the narrow owned-sidecar lifecycle host</name>
<files>apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/capabilities/default.json, tooling-tests/phase-4-desktop.test.mjs</files>
<behavior>- Probe before spawn; reuse only the exact resolved `/ready` shape.
- Spawn only when nothing listens; map occupied, unhealthy, incompatible, malformed, and timeout results to typed recoverable diagnostics.
- On exit send the resolved private stdin shutdown message and terminate only the stored owned child after a bounded wait.</behavior>
- After Ready, an owned child exit or failed/malformed/incompatible readiness observation maps to a typed recoverable lifecycle status; a reused responder is neither monitored through this owned-child path nor contacted during shutdown.</behavior>
<action>Implement `probe → reuse | spawn → bounded wait → ready | recoverable failure` using Tauri 2 and the fixed platform runtime contract. Restrict the shell capability to the named packaged sidecar and expose only typed lifecycle-status/retry commands. Per the resolved research decision, send private stdin `shutdown` to an owned child for graceful cancellation then apply bounded termination only to that stored handle. After Ready, retain and await the owned child handle and periodically re-probe the fixed readiness contract for that owned lifecycle: map an unexpected child exit, failed probe, malformed payload, or compatibility loss to a typed recoverable lifecycle status that the UI can render and retry. Cancel this monitoring when orderly owned shutdown begins; retry must probe again. Per D-02, do not create an owned-child monitor for a reused responder and never send it a shutdown request, probe it after Ready through this monitor, kill by PID/port, or change ports; never accept UI-controlled executable, path, port, or command arguments. Add focused behavior/source tests that simulate post-Ready owned-child exit and readiness loss and assert each becomes typed recoverable status, while compatible reuse proves no child handle, ongoing monitor contact, shutdown message, or termination is created for the reused responder; retain tests for ownership, strict compatibility, bounded retry, capability scope, and safe diagnostics.</action>
<verify><automated>pnpm test -- tooling-tests/phase-4-desktop.test.mjs &amp;&amp; pnpm rust:architecture</automated></verify>
<done>The host has test-proven lifecycle ownership and post-Ready owned-child/readiness loss recovery, with no arbitrary shell capability, reused-responder contact, or non-owned termination.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Render the accessible operational lifecycle shell</name>
<files>apps/desktop/src/main.tsx, apps/desktop/src/App.tsx, apps/desktop/src/styles.css, tooling-tests/phase-4-desktop.test.mjs</files>
<behavior>- Initializing, ready, and recoverable-failure states are visibly distinct.
- Failure provides retry and confines copyable diagnostic detail to a development-oriented disclosure.
- The UI uses only typed lifecycle commands and generated contracts exports.</behavior>
<action>Create the keyboard-operable operational shell driven by the typed host bridge. Give retry a visible control, use clear recovery copy, and keep technical code/detail copyable only in an explicitly development-oriented disclosure; never render raw process logs. Respect reduced-motion preferences and keep styling to this operational shell rather than Phase 5 tokens/primitives. Extend focused tests for state rendering, retry wiring, safe diagnostic placement, and absence of React SQLite or direct shell usage.</action>
<verify><automated>pnpm test -- tooling-tests/phase-4-desktop.test.mjs &amp;&amp; pnpm typecheck &amp;&amp; pnpm check &amp;&amp; git diff --check</automated></verify>
<done>The desktop presents recoverable lifecycle status without widening product, persistence, or shell authority scope.</done>
</task>
</tasks>
<verification>Run the lifecycle and aggregate checks mapped in 04-VALIDATION.md; the Linux build is verified by 04-06 CI.</verification>
<success_criteria>The Tauri desktop owns only its sidecar lifecycle and gives users bounded, safe recovery feedback.</success_criteria>
<output>Create `.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-04-SUMMARY.md` when done.</output>
