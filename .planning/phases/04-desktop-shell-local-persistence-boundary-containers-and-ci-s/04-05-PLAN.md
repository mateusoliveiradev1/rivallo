---
phase: 04-desktop-shell-local-persistence
plan: "05"
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - crates/application/src/persistence.rs
  - crates/application/src/lib.rs
  - crates/platform/src/persistence/mod.rs
  - crates/platform/src/persistence/sqlite.rs
  - crates/platform/src/lib.rs
  - tooling-tests/phase-4-persistence.test.mjs
autonomous: true
requirements: [FOUND-01]
must_haves:
  truths:
    - "Persistence capability and recoverable public failure taxonomy are owned by application, without SQLite knowledge in domain or UI-facing code."
    - "D-07: The outer platform SQLite boundary resolves a per-user local-data location through an abstraction, fixes no repository-relative database path, and creates no file in this phase."
    - "The outer platform SQLite boundary creates no schema, migration, seed, or database connection."
  artifacts:
    - path: "crates/application/src/persistence.rs"
      provides: "Application-owned local-persistence port and stable unavailable-versus-invalid-data errors."
    - path: "crates/platform/src/persistence/sqlite.rs"
      provides: "Inert SQLite adapter boundary and per-user local-data location resolver abstraction."
  key_links:
    - from: "crates/platform/src/persistence/sqlite.rs"
      to: "crates/application/src/persistence.rs"
      via: "The platform adapter implements the application-owned port and maps future storage failures to public application errors."
---

<objective>
Prepare the application-to-platform local-persistence boundary without opening a SQLite database or implementing product storage.

Purpose: D-05 through D-08 reserve a clean adapter seam for later local careers and cache work while keeping the desktop, React, and domain independent of SQLite.
Output: application port/error taxonomy, inert platform adapter/location abstraction, and scope regression tests.
</objective>

<execution_context>
@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-CONTEXT.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-RESEARCH.md
@crates/application/src/lib.rs
@crates/platform/src/lib.rs
@scripts/verify-cargo-architecture.mjs
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Define the application-owned persistence port and recovery-safe errors</name>
<files>crates/application/src/persistence.rs, crates/application/src/lib.rs</files>
<behavior>
- Consumers can distinguish local-store unavailability from invalid persisted data using typed application errors.
- The port expresses capability and recovery semantics without a SQLite path, driver, SQL statement, schema, migration, seed, or product record.
</behavior>
<action>Write Rust tests first for an application-owned local-persistence port and public failure taxonomy. Define only the generic capability/command boundary needed for a later adapter and an error enum whose stable cases distinguish `Unavailable` from `InvalidData`; keep error messages/storage causes out of the public UI contract. Re-export the port and errors from application. Do not add SQLite/SQLx/rusqlite/Tauri/filesystem dependencies, database paths, serialization schema, a repository implementation, migration, seed, or local-career/cache/preferences/queue behavior. Preserve application → domain-only dependency direction.</action>
<verify><automated>pnpm rust:test &amp;&amp; pnpm rust:clippy &amp;&amp; pnpm rust:architecture</automated></verify>
<done>Application owns a compiled, tested persistence abstraction and recovery-safe error distinction without storage implementation or platform dependency.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add the inert platform SQLite adapter and per-user location seam</name>
<files>crates/platform/src/persistence/mod.rs, crates/platform/src/persistence/sqlite.rs, crates/platform/src/lib.rs, tooling-tests/phase-4-persistence.test.mjs</files>
<behavior>
- The adapter accepts a location supplied by a platform resolver abstraction rather than a repository-relative path.
- It maps future local-store failures to the application taxonomy without surfacing database details.
- Constructing or testing the boundary performs no file, directory, database, schema, migration, seed, or driver I/O.
</behavior>
<action>Create the platform-only `persistence` module with a `LocalDataDirectoryResolver` abstraction and an unconnected `SqlitePersistenceAdapter` shape that depends on the application port/errors. Keep the resolver's eventual OS-specific lookup behind platform and inject its result into the adapter; do not choose a repository-relative database location, create directories, create a file, open a driver, or import a SQLite crate. Map planned adapter failures only to `Unavailable` or `InvalidData`, retaining implementation detail privately. Add focused Vitest source/manifest checks that prove ownership, forbid SQLite knowledge/dependencies in domain/application/React/Tauri-facing source, reject filesystem/connection/migration/seed behavior, and require a resolver rather than a fixed checkout path.</action>
<verify><automated>pnpm test -- tooling-tests/phase-4-persistence.test.mjs &amp;&amp; pnpm rust:test &amp;&amp; pnpm rust:architecture &amp;&amp; pnpm check</automated></verify>
<done>The platform exposes an inert, test-fenced SQLite adapter boundary with a per-user location abstraction and typed application errors, but no persistence side effect exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| application → platform adapter | Storage failures and locations cross from outer adapter into application semantics. |
| platform → OS local-data location | Platform may eventually resolve filesystem locations unavailable to domain/UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-08 | Information disclosure | persistence failure | mitigate | Expose only typed recovery classes, never database/OS error text to UI-facing consumers. |
| T-04-09 | Tampering | local storage boundary | mitigate | Keep adapter inert and prohibit unplanned schema/file/migration operations until later phase ownership. |
| T-04-10 | Elevation of privilege | location resolution | mitigate | Keep OS-path resolution platform-owned and reject repository-relative/fixed storage paths. |
</threat_model>

<verification>
Run focused persistence source tests, Rust tests/Clippy/architecture, and the aggregate. Inspect Cargo metadata to confirm application remains independent of platform and database drivers.
</verification>

<success_criteria>
The persistence seam is application-owned and platform-adapted with recoverable errors and per-user location abstraction, while every real SQLite storage side effect remains outside Phase 4.
</success_criteria>

<output>
Create `.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-05-SUMMARY.md` when done.
</output>
