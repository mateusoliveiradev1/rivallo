---
phase: 04-desktop-shell-local-persistence
plan: "06"
type: execute
wave: 4
depends_on: [04-04, 04-05]
files_modified: [docker-compose.yml, docs/operations/local-development.md, package.json, scripts/run-quality.mjs, .github/workflows/ci.yml, tooling-tests/phase-4-infrastructure.test.mjs]
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths: ["D-10: Docker Compose persists local PostgreSQL data in a named volume by default, while destructive volume removal is a separate explicit documented command.", "D-11: Development connection values are non-secret local defaults that can be overridden with environment variables; no committed `.env` file is used.", "D-14: GitHub Actions runs separate JavaScript/TypeScript quality, Rust/contracts quality, and desktop-build jobs rather than one aggregate job.", "D-15: The desktop integration build runs on Linux only, with no Windows/macOS matrix or packaging in this phase.", "Docker Compose starts only local PostgreSQL with a healthcheck and no initialization; CI publishes no artifacts."]
  artifacts:
    - path: docker-compose.yml
      provides: Local PostgreSQL service, named volume, and healthcheck only.
    - path: .github/workflows/ci.yml
      provides: Three scoped non-mutating CI jobs.
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: Each job calls an existing atomic check or the explicit no-bundle desktop build.
    - from: docs/operations/local-development.md
      to: docker-compose.yml
      via: Normal start/health/stop instructions are separated from destructive volume removal.
---

<objective>
Add bounded local PostgreSQL developer infrastructure and the first scoped GitHub Actions skeleton.
Purpose: D-09 through D-16 require repeatable local operations and transparent verification boundaries without external provisioning or publication.
Output: Compose configuration, operations documentation, CI/root command wiring, and static infrastructure tests.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-CONTEXT.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-RESEARCH.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-04-SUMMARY.md
@.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-05-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Declare and document bounded local PostgreSQL Compose operations</name>
<files>docker-compose.yml, docs/operations/local-development.md, tooling-tests/phase-4-infrastructure.test.mjs</files>
<behavior>- Compose starts one local PostgreSQL service with environment-overridable non-secret defaults, named volume, loopback port, and `pg_isready` healthcheck.
- Ordinary stop preserves storage; the sole destructive command is explicit and separately documented.</behavior>
<action>Choose a reviewed official PostgreSQL major image; declare one `postgres` service, explicit named volume, loopback mapping, non-secret environment-overridable local values, and bounded `pg_isready` healthcheck. Document exact start, health inspection, normal stop, and separately conspicuous destructive volume-removal commands. Per D-09 through D-12, do not add a committed environment file, hosted service, init SQL, migration, fixture, schema, product table, application mount, or cache. Add static Vitest checks for the allowed shape and scope fences; retain runtime health validation for an environment where Docker Compose exists.</action>
<verify><automated>pnpm test -- tooling-tests/phase-4-infrastructure.test.mjs &amp;&amp; docker compose -f docker-compose.yml config</automated></verify>
<done>Per D-10 and D-11, local PostgreSQL is declarative and persistent by default, destructive removal is separately explicit, connection defaults are non-secret and environment-overridable, no committed `.env` exists, and the service is health-checked and free of data initialization.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Wire the three scoped CI jobs and no-publish fence</name>
<files>package.json, scripts/run-quality.mjs, .github/workflows/ci.yml, tooling-tests/phase-4-infrastructure.test.mjs</files>
<behavior>- PR and primary-branch pushes run separate JS/TS, Rust/contracts, and Linux desktop-build jobs.
- Jobs use frozen installation and lockfile-keyed dependency-store caching only; none publishes output.</behavior>
<action>Add a root `desktop:build` command that delegates to the no-bundle desktop build and update the quality-script validator only for real non-mutating commands. Create CI with reviewed checkout/setup-node/pnpm action majors, PR plus primary-branch push triggers, frozen install, dependency-store cache keyed by lockfile, and exactly three jobs: JS/TS quality, Rust/contracts quality with both drift checks, and Linux desktop build with only documented prerequisites. Per D-13 through D-16, do not add writer commands, Windows/macOS builds, release/package publishing, artifact upload, credentials, target/volume caching, installers, screenshots, or generated application output. Extend static checks for job separation, trigger/command/cache boundaries, Linux target, and absent publication surface.</action>
<verify><automated>pnpm test -- tooling-tests/phase-4-infrastructure.test.mjs &amp;&amp; pnpm check &amp;&amp; git diff --check</automated></verify>
<done>Per D-14 and D-15, CI transparently runs separate JS/TS, Rust/contracts, and Linux-only desktop integration-build checks with dependency-only caches, no Windows/macOS matrix or packaging, and no publication surface.</done>
</task>
</tasks>
<verification>Run every 04-VALIDATION.md automated command; perform Docker health and observe a green CI run where the required environment is available.</verification>
<success_criteria>Developers have bounded local PostgreSQL operations and CI exposes required quality responsibilities without publishing anything.</success_criteria>

## Source audit

| Source | ID | Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Desktop/API shell, persistence boundary, Docker, CI | 04-02 through 04-06 | COVERED |
| REQ | FOUND-01 | Tauri/React/Rust boundary and repeatable tooling | 04-01 through 04-06 | COVERED |
| REQ | FOUND-02 | Scoped CI and desktop build | 04-06 | COVERED |
| RESEARCH | — | Loopback runtime, private stdin shutdown-to-cancellation wiring, and owned lifecycle monitoring | 04-02, 04-04 | COVERED |
| RESEARCH | — | Inert application/platform SQLite boundary | 04-05 | COVERED |
| RESEARCH | — | Compose and three-job CI | 04-06 | COVERED |
| CONTEXT | D-01 to D-04 | Lifecycle, private graceful shutdown, reuse, post-Ready owned-child/readiness recovery, retry, safe diagnostics | 04-02, 04-04 | COVERED |
| CONTEXT | D-05, D-06, D-08 | Persistence seam | 04-05 | COVERED |
| CONTEXT | D-07 | Per-user platform location abstraction; no repository path or file | 04-05 | COVERED |
| CONTEXT | D-09, D-12 | Local Compose service and scope fences | 04-06 | COVERED |
| CONTEXT | D-10 | Named volume by default; separately explicit destructive removal | 04-06 | COVERED |
| CONTEXT | D-11 | Non-secret overrideable local defaults; no committed environment file | 04-06 | COVERED |
| CONTEXT | D-13, D-16 | CI trigger and no-publication boundaries | 04-06 | COVERED |
| CONTEXT | D-14 | Separate JS/TS, Rust/contracts, and desktop-build jobs | 04-06 | COVERED |
| CONTEXT | D-15 | Linux-only integration build; no Windows/macOS matrix or packaging | 04-06 | COVERED |

<output>Create `.planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-06-SUMMARY.md` when done.</output>
