---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md
  - scripts/verify-phase-5-package-approval.mjs
  - tooling-tests/phase-5-package-approval.test.mjs
autonomous: false
requirements: [UI-01]
must_haves:
  truths:
    - "D-06 and D-11: every direct npm package needed for Lucide, accessible composite behavior, DOM interaction tests, OKLCH resolution, and browser evidence is identified by exact version and approved before any dependency transaction."
    - "No shadcn package, registry, theme, table framework, chart package, second icon family, or speculative future dependency enters the approved set."
  artifacts:
    - path: ".planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md"
      provides: "Exact package inventory, provenance, lifecycle/install-hook review, license, transitive-risk assessment, and Mateus approval record."
    - path: "scripts/verify-phase-5-package-approval.mjs"
      provides: "Strict canonical approval parser and installed-manifest/lockfile divergence verifier."
  key_links:
    - from: "05-01-PACKAGE-REVIEW.md"
      to: "05-02-PLAN.md"
      via: "The dependency transaction may install only exact package/version rows marked APPROVED."
---

<objective>
Establish the mandatory human supply-chain gate before Phase 5 adds any npm dependency.

Purpose: UI-01 requires new icon, composite-behavior, DOM-test, colour-resolution, and Playwright capabilities, but D-06/D-11 and the project trust policy prohibit an unreviewed dependency transaction.
Output: an auditable exact-version package review and explicit Mateus approval or rejection.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@PRODUCT.md
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-RESEARCH.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@package.json
@apps/desktop/package.json
@pnpm-lock.yaml
</context>

<tasks>

<task type="auto">
<name>Task 1: Inventory and audit the smallest exact Phase 5 package set</name>
<files>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md, scripts/verify-phase-5-package-approval.mjs, tooling-tests/phase-5-package-approval.test.mjs</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-RESEARCH.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md, package.json, apps/desktop/package.json, pnpm-lock.yaml</read_first>
<action>Before editing any manifest or lockfile, derive the smallest direct package set required by the approved implementation: Lucide as the sole generic icon family (D-06); only the Radix behavior primitives that directly serve D-11 composite controls; one reviewed OKLCH-to-browser-target conversion capability if native project code cannot safely prove gamut-mapped contrast; React DOM interaction-test support; official React type packages if replacing the bootstrap shim requires them; and Playwright/browser accessibility evidence. For every proposed direct package, choose and record one exact version, exact owning scope (`root-dev`, `desktop-runtime`, `icons-runtime`, or `design-tokens-runtime`), official registry/repository URL, maintainer or trusted-publisher evidence (or explicitly document its absence), publication date, `dist.integrity`, license, lifecycle/install scripts from both metadata and packed tarball inspection, direct/transitive inventory and risk, peer requirements, and why an existing/native alternative is insufficient. Define a canonical sorted inventory row as `name@version|scope|integrity` and record its SHA-256 digest. The record has exactly one field each for `Decision: PENDING`, `Approved by: —`, `Approved at: —`, and `Inventory digest: sha256:<64 lowercase hex>`. Implement a strict parser whose approval mode accepts only exactly one `Decision: APPROVED`, `Approved by: Mateus`, valid ISO-8601 `Approved at`, and a recomputed matching canonical digest; it rejects PENDING/REJECTED, negative approval phrases, missing/duplicate fields/rows, malformed dates/digests, and later installed name/version/scope/integrity divergence. Add fixture-driven tests for every rejection and valid approval/installed comparison. Explicitly reject shadcn/registries, visual themes, table/grid frameworks, charting, a second icon family, Lab-only router, mascot/branding packages, and placeholders. Do not run add/install/update or edit dependency files.</action>
<verify><automated>pnpm test -- tooling-tests/phase-5-package-approval.test.mjs &amp;&amp; git diff -- package.json apps/desktop/package.json pnpm-lock.yaml pnpm-workspace.yaml --exit-code</automated></verify>
<acceptance_criteria>
- `05-01-PACKAGE-REVIEW.md` contains one exact canonical row per proposed direct package plus a recomputable `Inventory digest: sha256:<64 lowercase hex>`.
- `phase-5-package-approval.test.mjs` proves valid APPROVED input and rejects PENDING, REJECTED, negative phrasing, duplicate/missing decision fields, non-Mateus reviewer, invalid ISO date, digest mismatch, and name/version/scope/integrity divergence.
- Dependency manifests and `pnpm-lock.yaml` have no diff after Task 1.
</acceptance_criteria>
<done>The review contains a minimal exact direct-package inventory with reproducible provenance, install-hook, license, peer, integrity, and transitive-risk evidence, while all dependency files remain unchanged.</done>
</task>

<task type="checkpoint:human-verify">
<name>Task 2: Mateus approves or rejects the exact dependency inventory</name>
<files>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md, scripts/verify-phase-5-package-approval.mjs</read_first>
<what-built>A supply-chain review of every exact package/version proposed for Phase 5, including its reason, scope, provenance, install behavior, license, and transitive risk.</what-built>
<how-to-verify>Read `05-01-PACKAGE-REVIEW.md`. Confirm each direct package is necessary, trustworthy enough, exactly pinned, and owned by its stated scope. To approve, replace the three terminal fields with exactly `Decision: APPROVED`, `Approved by: Mateus`, and a valid ISO-8601 `Approved at`; to reject, use `Decision: REJECTED` and explain why. Do not edit canonical inventory rows/digest while deciding.</how-to-verify>
<action>Pause for Mateus. Do not infer approval from prior package approvals, prose, or approval of this plan. After the edit, run the strict record parser; only its structured APPROVED result unblocks 05-02. Do not run pnpm dependency transactions. If any inventory row changes, regenerate the canonical digest and repeat human review.</action>
<verify><automated>node scripts/verify-phase-5-package-approval.mjs --record .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md</automated></verify>
<acceptance_criteria>
- The record has exactly one `Decision: APPROVED`, `Approved by: Mateus`, valid ISO-8601 `Approved at`, and matching canonical inventory digest.
- The strict parser exits zero for the approved record; PENDING, REJECTED, negative approval wording, duplicates, missing fields, or digest drift exit non-zero.
- No dependency manifest or lockfile changes during the checkpoint.
</acceptance_criteria>
<done>The exact package set has an explicit dated Mateus decision before any dependency transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry/publisher → Rivallo workspace | New code may execute during install/build or compromise development and CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01-SC | Tampering | npm dependency set | mitigate | Exact versions, integrity, packed scripts, provenance, license, peers, and transitive graph are reviewed before approval. |
| T-05-01-SP | Spoofing | package identity | mitigate | Match registry ownership, official repository, maintainer/trusted-publisher evidence, and tarball metadata. |
| T-05-01-EX | Elevation of privilege | install hooks | mitigate | Inspect lifecycle scripts and block the transaction until Mateus explicitly approves the exact set. |
</threat_model>

<verification>No manifest/lockfile diff exists, every canonical dependency row is exact/evidenced, and the strict parser validates exactly `Decision: APPROVED`, `Approved by: Mateus`, ISO date, and matching inventory digest before Plan 05-02.</verification>
<success_criteria>UI-01 can proceed through an explicit, minimal, auditable dependency trust decision without authorizing any visual implementation or package transaction.</success_criteria>
<output>Create `05-01-SUMMARY.md` when complete.</output>
