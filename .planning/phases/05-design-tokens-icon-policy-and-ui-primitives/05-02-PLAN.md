---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - package.json
  - apps/desktop/package.json
  - pnpm-lock.yaml
  - packages/icons/package.json
  - packages/design-tokens/package.json
  - vitest.config.mjs
  - apps/desktop/src/react-runtime.d.ts
  - apps/desktop/src/test/dom-environment.test.tsx
  - scripts/verify-phase-5-package-approval.mjs
  - tooling-tests/phase-5-package-approval.test.mjs
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Only the exact package/version set explicitly approved by Mateus in 05-01 is installed, with pnpm and narrow workspace placement."
    - "D-06/D-11: Lucide is the sole generic icon family and Radix contributes behavior only; no package supplies Rivallo appearance."
    - "The resulting dependency and test foundation is reproducible, non-watch, and contains no placeholder future script."
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Frozen exact resolution of the approved Phase 5 dependency transaction."
    - path: "packages/icons/package.json"
      provides: "Owning manifest and export boundary for approved icon dependencies."
    - path: "packages/design-tokens/package.json"
      provides: "Owning manifest and export boundary for approved colour-resolution dependencies."
    - path: "vitest.config.mjs"
      provides: "Separate deterministic Node tooling and browser-like React component test projects/configuration."
    - path: "apps/desktop/src/test/dom-environment.test.tsx"
      provides: "Real React render/interaction, automatic cleanup, and error-surfacing infrastructure proof."
  key_links:
    - from: "05-01-PACKAGE-REVIEW.md"
      to: "package.json and apps/desktop/package.json"
      via: "Every added direct name/version/scope must match an approved row exactly."
    - from: "git show HEAD owner-manifest baselines"
      to: "post-transaction owner manifests and pnpm-lock.yaml"
      via: "All baseline direct dependencies remain unchanged; registry delta equals approved inventory; only two named workspace links are separately allowed."
---

<objective>
Apply the approved Phase 5 dependency transaction and establish real non-watch component-test infrastructure.

Purpose: later token, icon, primitive, DenseTable, and UI Lab plans need reproducible approved capabilities without bypassing supply-chain review.
Output: exact manifest/lockfile changes, valid React typings, and a functioning browser-like Vitest boundary.
</objective>

<execution_context>@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-SUMMARY.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@package.json
@apps/desktop/package.json
@vitest.config.mjs
</context>

<tasks>
<task type="auto">
<name>Task 1: Install only the exact approved packages in their approved scopes</name>
<files>package.json, apps/desktop/package.json, pnpm-lock.yaml, packages/icons/package.json, packages/design-tokens/package.json, apps/desktop/src/react-runtime.d.ts, scripts/verify-phase-5-package-approval.mjs, tooling-tests/phase-5-package-approval.test.mjs</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md, scripts/verify-phase-5-package-approval.mjs, tooling-tests/phase-5-package-approval.test.mjs, package.json, apps/desktop/package.json, pnpm-workspace.yaml, pnpm-lock.yaml, packages/contracts-client/package.json</read_first>
<action>Run the strict record parser first. Capture the deterministic direct-dependency baseline for root and desktop from `git show HEAD:package.json` and `git show HEAD:apps/desktop/package.json`, including exact section/name/specifier across dependencies/devDependencies/optionalDependencies/peerDependencies; new icons/design-token manifests have explicitly empty baselines. Create minimal private ESM owner manifests with narrow exports. Add separately allowed internal desktop links only as `@rivallo/icons: workspace:*` and `@rivallo/design-tokens: workspace:*`. Install every canonical registry row with `--save-exact` in its approved owner/scope. Extend parser/tests so `--installed` proves every baseline entry unchanged; complete registry direct-dependency delta across all four owners equals approved rows exactly by name/version/scope/integrity; the only non-registry delta is those two links; and each approved registry delta has matching lockfile integrity. Reject extra/missing approved dependencies, baseline modification/removal, wrong section/scope/specifier, any other workspace link, and lockfile omission/integrity drift. Re-query registry evidence before transaction; remove/narrow React shim only for approved typings; frozen reinstall after strict comparison. Add no unapproved package/override.</action>
<verify><automated>node scripts/verify-phase-5-package-approval.mjs --record .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md --installed &amp;&amp; pnpm test -- tooling-tests/phase-5-package-approval.test.mjs &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- `packages/icons/package.json` and `packages/design-tokens/package.json` are private ESM manifests with narrow exports; desktop declares both workspace links.
- Baseline direct dependency sections/names/specifiers from HEAD are unchanged; registry delta equals canonical approved rows exactly; only the two exact desktop `workspace:*` links are additionally allowed.
- Fixtures prove preserved pre-existing dependencies and valid links/exact delta pass, while extra dependency, missing approved delta, modified/removed baseline, wrong scope/link and lockfile integrity mismatch fail.
- Strict `--installed`, approval tests, frozen install, and typecheck all exit zero.
</acceptance_criteria>
<done>Manifest and lockfile contain only reviewed exact additions in approved scopes, React typing is coherent, and frozen install/typecheck pass.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Establish real non-watch DOM/component test configuration</name>
<files>vitest.config.mjs, package.json, apps/desktop/src/test/dom-environment.test.tsx</files>
<read_first>vitest.config.mjs, package.json, apps/desktop/package.json, tsconfig.json, tooling-tests/workspace-config.test.mjs, apps/desktop/src/App.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
<behavior>- Existing Node tooling tests retain their Node environment and serial protection for tracked generated artifacts.
- React component tests execute in the approved browser-like DOM environment with deterministic cleanup and no watch mode.
- A real infrastructure test renders/interacts with a test-local React control, proves cleanup between tests, and surfaces thrown/unhandled render errors.</behavior>
<action>Extend Vitest using explicit projects or equivalent scoped matching so Node tooling remains Node-based/non-parallel while desktop `*.test.ts(x)` uses the approved DOM environment. Create `apps/desktop/src/test/dom-environment.test.tsx`: render a test-local React counter/button, query by accessible role/name, interact through user-event and assert update; in a following test assert prior DOM is absent before rendering to prove cleanup; render a deliberate throwing fixture and assert the harness surfaces the error, while unhandled errors remain fatal. This is infrastructure proof, not a shared placeholder component. Add only real commands for existing scopes and preserve non-watch aggregate.</action>
<verify><automated>pnpm test -- apps/desktop/src/test/dom-environment.test.tsx &amp;&amp; pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; git diff --check</automated></verify>
<acceptance_criteria>
- Tooling tests remain Node-based and serial; desktop `*.test.ts(x)` resolves the approved DOM environment with deterministic cleanup.
- Exact DOM infrastructure test renders/interacts with a named React control, proves cleanup, and observes a deliberate thrown render failure; unhandled errors remain fatal.
- Focused DOM test, full test, typecheck, frozen install, and whitespace validation exit zero.
</acceptance_criteria>
<done>Node and React DOM tests have explicit real environments, run non-watch, and preserve existing test behavior.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-SC | Tampering | manifest/lockfile transaction | mitigate | Preserve HEAD baseline direct entries; require exact approved registry delta, only two named workspace links, matching lockfile integrities, and frozen reinstall. |
| T-05-02-EX | Elevation of privilege | package install | mitigate | Abort on registry/tarball evidence mismatch and install no unapproved package. |
| T-05-02-RP | Repudiation | dependency approval | mitigate | Preserve the dated review and exact manifest/lockfile diff as the audit trail. |
</threat_model>

<verification>Compare the direct dependency diff to every approved row, run frozen installation, test, typecheck, and confirm no package outside the approved set was added.</verification>
<success_criteria>The reviewed Phase 5 capabilities install reproducibly and provide a real DOM-test foundation without visual templates or speculative scripts.</success_criteria>
<output>Create `05-02-SUMMARY.md` when complete.</output>
