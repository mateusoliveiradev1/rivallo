---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "03"
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - packages/design-tokens/src/tokens.ts
  - packages/design-tokens/src/contrast.ts
  - packages/design-tokens/src/generated.css
  - packages/design-tokens/src/index.ts
  - scripts/generate-design-tokens.mjs
  - scripts/verify-design-token-drift.mjs
  - tooling-tests/design-tokens.test.mjs
  - package.json
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-01 through D-05: one TypeScript-authored semantic OKLCH source implements Noite de Comando without pure black, decorative colour, or colour-only state."
    - "D-02: the visual route remains Arquitetura tática with minimal brand heraldry—precise, geometric, calm, and institutional rather than neon, saturated, mascot-led, or decorative."
    - "D-03: operational text and data use a cold high-contrast near-white, with pure white reserved for exceptional emphasis and no warm cream, beige, or low-contrast grey substitute."
    - "Every approved text/non-text pair is measured against the actual browser-target sRGB value and meets WCAG 2.2 AA thresholds."
    - "Generated CSS is byte-deterministic; its check detects drift without mutating tracked files and prints the repair command."
  artifacts:
    - path: "packages/design-tokens/src/tokens.ts"
      provides: "Canonical semantic colour, typography, spacing, radius, elevation, layer, and motion definitions plus declared contrast pairs."
    - path: "packages/design-tokens/src/generated.css"
      provides: "Tracked deterministic CSS custom properties consumed by the desktop."
    - path: "scripts/verify-design-token-drift.mjs"
      provides: "Non-mutating generated-token drift enforcement."
  key_links:
    - from: "packages/design-tokens/src/tokens.ts"
      to: "packages/design-tokens/src/generated.css"
      via: "The writer derives CSS; manual generated CSS edits are rejected."
    - from: "packages/design-tokens/src/contrast.ts"
      to: "05-UI-SPEC.md contrast contract"
      via: "Declared rendered pairs are measured at 4.5:1 or 3:1 as applicable."
---

<objective>
Implement Rivallo's single-source semantic token system with deterministic generation and rendered-value contrast proof.

Purpose: D-01–D-05 and UI-01 require a trustworthy visual vocabulary before any primitive or UI Lab composition.
Output: authored TypeScript tokens, tracked generated CSS, actionable drift checking, and integrity/contrast tests.
</objective>

<execution_context>@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-RESEARCH.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-PATTERNS.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@scripts/generate-openapi.mjs
@scripts/verify-openapi-drift.mjs
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Author the semantic OKLCH contract and rendered contrast engine</name>
<files>packages/design-tokens/src/tokens.ts, packages/design-tokens/src/contrast.ts, packages/design-tokens/src/index.ts, tooling-tests/design-tokens.test.mjs</files>
<read_first>packages/design-tokens/package.json, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#color, DESIGN.md#semantic-colour-strategy, scripts/verify-phase-5-package-approval.mjs</read_first>
<behavior>- The token inventory includes every canonical colour role and fixed typography/spacing/radius/elevation/layer/motion scale from the UI-SPEC.
- Canvas rejects pure black; semantic roles retain D-04 restrictions; all normal text pairs meet 4.5:1 and meaningful UI/focus pairs meet 3:1 after browser-target sRGB gamut mapping.
- Failures identify token pair, resolved values, measured ratio, and required threshold.</behavior>
<action>Write tests first for required keys, exact fixed scales, semantic layers, motion limits/reduced-motion metadata, no pure-black canvas, and declared foreground/background contrast pairs. Implement one TypeScript source using the provisional UI-SPEC OKLCH candidates only where their rendered values pass; adjust candidates when evidence fails while preserving D-01–D-05 roles. Use only the package approved in 05-01 for colour conversion, or a standards-referenced local implementation with known-value tests if no package was approved. Export authored and resolved/gamut-mapped values plus measured pair metadata for UI Lab display. Do not infer arbitrary foregrounds, duplicate values in CSS, add a light theme, club theme, final brand palette, gradient, raw product-state colour, or dashboard token.</action>
<verify><automated>pnpm test -- tooling-tests/design-tokens.test.mjs &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- Canonical source exports every required colour/type/spacing/radius/elevation/layer/motion token and every declared contrast pair.
- Tests reject pure-black canvas and report pair, resolved sRGB values, measured ratio, and threshold for any contrast failure.
- All normal-text pairs are at least 4.5:1 and meaningful UI/focus pairs at least 3:1 after target gamut mapping.
</acceptance_criteria>
<done>The canonical TypeScript source owns every required scale and semantic colour pair, rendered contrast passes with actionable failure evidence, and no pure-black or decorative-role violation exists.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Generate tracked semantic CSS deterministically</name>
<files>scripts/generate-design-tokens.mjs, packages/design-tokens/src/generated.css, tooling-tests/design-tokens.test.mjs, package.json</files>
<read_first>packages/design-tokens/src/tokens.ts, scripts/generate-openapi.mjs, scripts/generate-contract-client.mjs, package.json</read_first>
<behavior>- Repeated writer runs emit byte-identical, stable-order CSS from the TypeScript source.
- The generated artifact contains semantic `--rv-*` properties and the required static reduced-motion contract, not copied authored literals.
- Writer failure is non-zero and names the actionable cause.</behavior>
<action>Mirror the established contracts writer pattern: resolve repository paths, support an explicit isolated output override for tests/checks, serialize in stable semantic order, include an ownership header, and expose the real writer command `tokens:generate`. Generate only the declared CSS artifact. Tests run the writer twice and compare bytes, validate every authored public token has one generated property, and reject arbitrary z-index/motion/radius values. Do not place writer execution inside `quality` or any non-mutating aggregate.</action>
<verify><automated>pnpm tokens:generate &amp;&amp; pnpm test -- tooling-tests/design-tokens.test.mjs &amp;&amp; pnpm prettier --check packages/design-tokens/src/generated.css</automated></verify>
<acceptance_criteria>
- Two isolated writer runs produce byte-identical stable-order CSS with an ownership header and one semantic `--rv-*` property per public token.
- `tokens:generate` writes only the declared generated CSS target and exits non-zero with actionable detail on failure.
- Generated CSS passes its focused tests and Prettier check.
</acceptance_criteria>
<done>The tracked CSS is a complete stable derivation of the TypeScript source and repeated generation is byte-identical.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Enforce non-mutating drift and clean-check behavior</name>
<files>scripts/verify-design-token-drift.mjs, tooling-tests/design-tokens.test.mjs, package.json</files>
<read_first>scripts/verify-openapi-drift.mjs, scripts/verify-contract-client-drift.mjs, scripts/generate-design-tokens.mjs, tooling-tests/contracts-client-generation.test.mjs</read_first>
<behavior>- The check generates into an OS temporary target, byte-compares against tracked CSS, cleans up, and never repairs the repository.
- Controlled drift returns non-zero, leaves drifted bytes untouched, and prints `pnpm tokens:generate`.
- A clean check leaves `git status --porcelain` unchanged.</behavior>
<action>Adapt the established OpenAPI/client drift-check shape. Add `tokens:check`, compare bytes from isolated output, report missing/drifted artifact plus exact repair command, and clean temporary files in `finally`. Tests prove success, controlled drift, non-mutation, and actionable output. Do not use `git diff` as the comparison engine and do not hide mismatch by running the writer against the tracked target.</action>
<verify><automated>pnpm tokens:check &amp;&amp; pnpm test -- tooling-tests/design-tokens.test.mjs &amp;&amp; git diff --check</automated></verify>
<acceptance_criteria>
- `tokens:check` generates only in OS temporary storage, byte-compares the tracked artifact, and cleans temporary output in `finally`.
- Controlled drift exits non-zero, leaves altered tracked bytes unchanged, and prints exactly `pnpm tokens:generate` as repair command.
- Success and failure-path focused tests pass; whitespace validation is clean.
</acceptance_criteria>
<done>Token drift is deterministic, actionable, non-mutating, and suitable for later quality/CI integration.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-03-TM | Tampering | generated CSS | mitigate | Generate only from canonical TS, byte-check isolated output, and reject manual drift. |
| T-05-03-SP | Spoofing | contrast evidence | mitigate | Measure declared pairs against resolved browser-target sRGB values, not authored ideal values. |
| T-05-03-ID | Information disclosure | CSS/source | accept | Tokens are public visual constants and contain no secrets or user data. |
</threat_model>

<verification>Run token unit/integrity/contrast tests, repeated generation, controlled drift test, typecheck, and a clean-status check.</verification>
<success_criteria>D-01–D-05 have one deterministic, WCAG-evidenced semantic implementation that later components can consume without raw colour duplication.</success_criteria>
<output>Create `05-03-SUMMARY.md` when complete.</output>
