---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "04"
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - packages/icons/src/Icon.tsx
  - packages/icons/src/football-icons.tsx
  - packages/icons/AUTHORSHIP.md
  - packages/icons/src/index.ts
  - packages/icons/src/Icon.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-06: every generic icon is a curated Lucide name consumed through one Rivallo boundary at 16/20/24px and approximately 1.75px stroke."
    - "D-07–D-09: football SVGs are original, versioned, geometrically coherent, static, and accessible; icon-plus-label remains the default."
    - "D-10: no mascot, final mark, copied crest/path, or irreversible identity is created."
  artifacts:
    - path: "packages/icons/src/Icon.tsx"
      provides: "Typed curated generic icon wrapper with fixed sizes/stroke and decorative/semantic accessibility behavior."
    - path: "packages/icons/src/football-icons.tsx"
      provides: "Small original versioned football SVG registry for UI primitive evidence."
  key_links:
    - from: "packages/icons/src/Icon.tsx"
      to: "lucide-react"
      via: "Only the approved curated map imports Lucide components; consumers cannot supply arbitrary icon paths or families."
---

<objective>
Implement Rivallo's constrained generic-icon boundary and an original football-SVG proof registry.

Purpose: UI-01 and D-06–D-10 need consistent, accessible iconography before components expose icon controls or the Lab audits optical behavior.
Output: one private workspace icon package, component tests, and a small non-product football vocabulary.
</objective>
<execution_context>@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@DESIGN.md
@docs/design/iconography.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-03-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Create the curated Lucide boundary and accessibility contract</name>
<files>packages/icons/src/Icon.tsx, packages/icons/src/index.ts, packages/icons/src/Icon.test.tsx</files>
<read_first>packages/icons/package.json, docs/design/iconography.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#icon-contract, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-01-PACKAGE-REVIEW.md</read_first>
<behavior>- Only curated semantic names map to Lucide; approved sizes are 16, 20, 24 and stroke is fixed near 1.75.
- Decorative icons are hidden; semantic icons expose deliberate metadata while icon-only button naming remains the controlling component's responsibility.
- Arbitrary SVG paths, colour, animation, stroke, family, and unsupported size cannot enter through the public API.</behavior>
<action>Create a small private `@rivallo/icons` package with one public entrypoint. Write DOM tests first for `currentColor`, exact size/stroke mapping, decorative `aria-hidden`, semantic metadata, and rejection/prevention of unsupported configuration. Curate only generic icons actually consumed by Phase 5 primitives/Lab, with names based on meaning rather than Lucide implementation names where useful. Add no dynamic icon-name import, remote SVG, raw HTML, second library, emoji, or animated icon. D-08 means the wrapper does not pretend to make an unnamed IconButton accessible; that enforcement belongs in the primitive.</action>
<verify><automated>pnpm test -- packages/icons/src/Icon.test.tsx &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- Public Icon API accepts only curated semantic names and sizes 16/20/24, fixes optical stroke near 1.75, and emits `currentColor`.
- Decorative mode is hidden from assistive technology; semantic mode has deliberate metadata and cannot accept arbitrary path/family/stroke/animation.
- Icon DOM tests and workspace typecheck exit zero.
</acceptance_criteria>
<done>All generic icons cross one tested Rivallo boundary with fixed optical rules and explicit accessibility behavior.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add a bounded original football SVG registry</name>
<files>packages/icons/src/football-icons.tsx, packages/icons/src/index.ts, packages/icons/src/Icon.test.tsx, packages/icons/AUTHORSHIP.md</files>
<read_first>packages/icons/src/Icon.tsx, docs/design/iconography.md, PRODUCT.md#anti-references, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md#icon-policy-and-brand-direction</read_first>
<behavior>- Each football icon has an original path, stable semantic name, version metadata, 16/20/24 rendering, currentColor, and the same approximate optical weight.
- Meaningful and decorative modes obey the same accessible boundary.
- No external path, crest, club identity, mascot, or tactical-pitch illustration is present.</behavior>
<action>Design only the smallest unambiguous football concepts required to prove D-07 in the Lab. Author React SVG geometry locally with safe fixed attributes and no raw HTML, URL, external reference, event handler, style injection, or consumer-supplied SVG markup. Attach stable version/source metadata and create `AUTHORSHIP.md` declaring authoring process, Rivallo project provenance, date/version, and that no external SVG path/crest library was imported; tests validate presence/consistency of this declaration, not the truth of originality. Test viewBox, allowed sizes, labelling, currentColor, non-animation, and unsafe-attribute absence. If unclear at 16px, omit/use text. Reserve originality, non-imitation and optical harmony judgment exclusively for the 05-10 human checklist.</action>
<verify><automated>pnpm test -- packages/icons/src/Icon.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated></verify>
<acceptance_criteria>
- Every football entry has stable semantic name/version/viewBox, original fixed geometry, currentColor, and 16/20/24 rendering.
- Tests reject external URLs/references, raw HTML, handlers, style injection, consumer paths and animation; every entry has matching source/version provenance metadata and an explicit authorship declaration.
- Automated checks make no claim that geometry is original, non-imitative, mascot-free or optically harmonious; those are 05-10 human judgments.
- Icon tests, typecheck, and lint exit zero.
</acceptance_criteria>
<done>A small safe, versioned football registry has fixed local geometry and explicit authorship/provenance metadata; originality, non-imitation, and optical harmony remain pending 05-10 human review.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04-XSS | Tampering | SVG surface | mitigate | Fixed React-authored geometry; forbid raw HTML, URLs, event attributes, external references, and arbitrary paths. |
| T-05-04-SP | Spoofing | copied/external iconography | mitigate | Original bounded registry with source/version metadata and human optical review. |
| T-05-04-ID | Information disclosure | accessible names | accept | Static public labels contain no user data. |
</threat_model>
<verification>Run icon DOM/type/lint tests and later inspect every used icon at 16/20/24px in the UI Lab.</verification>
<success_criteria>D-06–D-10 are enforced by a narrow, accessible icon API and safe original football SVG proof set.</success_criteria>
<output>Create `05-04-SUMMARY.md` when complete.</output>
