---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "05"
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - apps/desktop/src/ui/primitives/actions.tsx
  - apps/desktop/src/ui/primitives/forms.tsx
  - apps/desktop/src/ui/primitives/feedback.tsx
  - apps/desktop/src/ui/primitives/layout.tsx
  - apps/desktop/src/ui/primitives/primitives.css
  - apps/desktop/src/ui/primitives/primitives.test.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/styles.css
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-11–D-13: native-first actions, fields, status/feedback, pagination, and overflow primitives implement real applicable states and the fixed 32px/6px dense geometry."
    - "D-12: every applicable primitive proves default, hover, focus-visible, active, selected, disabled, loading, and error states; compact inputs/selectors are 32px with a visible label above and helper or error text below."
    - "Every control has visible naming/description/error relationships, focus-visible distinct from selection, and state cues beyond colour."
    - "The existing lifecycle shell retains initializing/ready/recoverable-failure semantics while consuming the token/primitive foundation."
  artifacts:
    - path: "apps/desktop/src/ui/primitives/primitives.test.tsx"
      provides: "Real DOM interaction evidence for native primitive roles, names, descriptions, states, focus, loading, and recovery."
    - path: "apps/desktop/src/ui/primitives/primitives.css"
      provides: "Rivallo-owned token-consuming primitive appearance and complete applicable state selectors."
  key_links:
    - from: "apps/desktop/src/ui/primitives/*.tsx"
      to: "packages/design-tokens/src/generated.css and @rivallo/icons"
      via: "Components consume semantic variables and approved icon APIs, never raw palette/icon paths."
---

<objective>
Build the native-first half of Rivallo's accessible primitive inventory and migrate the lifecycle shell onto the visual foundation.

Purpose: D-11–D-13 require compact, reusable, state-complete controls without creating product screens.
Output: tested action/form/feedback/layout primitives, token-owned styling, and preserved host lifecycle behavior.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-03-SUMMARY.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-04-SUMMARY.md
@apps/desktop/src/App.tsx
@apps/desktop/src/styles.css
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement compact actions and labelled native form controls</name>
<files>apps/desktop/src/ui/primitives/actions.tsx, apps/desktop/src/ui/primitives/forms.tsx, apps/desktop/src/ui/primitives/primitives.css, apps/desktop/src/ui/primitives/primitives.test.tsx</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#primitive-inventory-and-state-matrix, packages/design-tokens/src/generated.css, packages/icons/src/index.ts, apps/desktop/src/App.tsx</read_first>
<behavior>- Button/IconButton, TextField, Select, Checkbox, and native RadioGroup expose roles/names, 32px geometry, visible label, helper/error associations, native disabled behavior, and applicable default/hover/focus-visible/active/selected/loading/error states.
- IconButton requires an accessible label and delegates a keyboard tooltip composition without using it as the name.
- Checkbox exposes unchecked, checked, and indeterminate states; indeterminate sets the actual DOM property and a non-colour visible cue.
- Emerald appears only on the primary action; error is text/icon plus styling.</behavior>
<action>Write interaction/DOM tests first, then implement native HTML primitives with stable generated IDs, explicit prop unions, and no polymorphic clickable divs. Button supports primary/secondary/quiet/destructive-proof and width-preserving loading with accessible context; IconButton accepts only the Rivallo icon boundary and mandatory accessible label (Tooltip composition is finalized in 05-06). Fields put visible label above and helper/error below with `aria-describedby`/`aria-invalid`. Checkbox has an explicit tri-state API; use a ref/effect to set the native input's actual `indeterminate` DOM property, expose checked/unchecked/indeterminate visually with a text/shape cue beyond colour, retain label hit target, focus, disabled behavior, and group error association. Radio selection is also non-colour. Style only through generated semantic variables, 12/14px Inter, 32px controls, 6px radius, 2px/2px cyan focus, 100/150ms tokens, and reduced motion. Do not add product forms or domain types.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/primitives/primitives.test.tsx &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- Button/IconButton/TextField/Select/Checkbox/Radio tests prove roles, visible labels, accessible names/descriptions/errors, 32px geometry contract, disabled/loading/focus and non-colour state cues.
- Checkbox tests prove unchecked, checked and indeterminate APIs; indeterminate sets `HTMLInputElement.indeterminate === true`, remains labelled/focusable when enabled, and exposes disabled/error-group behavior.
- Primitive DOM tests and typecheck exit zero without product/domain imports.
</acceptance_criteria>
<done>Native action/form primitives pass real semantic, state, focus, loading, disabled, and error interaction tests with approved compact geometry.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement persistent feedback, status, pagination, and native overflow primitives</name>
<files>apps/desktop/src/ui/primitives/feedback.tsx, apps/desktop/src/ui/primitives/layout.tsx, apps/desktop/src/ui/primitives/primitives.css, apps/desktop/src/ui/primitives/primitives.test.tsx</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#primitive-inventory-and-state-matrix, apps/desktop/src/ui/primitives/primitives.css, apps/desktop/src/App.tsx, DESIGN.md#components-and-states</read_first>
<behavior>- Status selects appropriate polite/assertive/live semantics and always combines icon/label/text with colour.
- Skeleton is inert/layout-preserving and static under reduced motion; Empty/Error use approved Portuguese copy and recovery semantics.
- Pagination/nav and ScrollArea remain native, labelled, keyboard-reachable, and do not imply fetching or hidden custom-scrollbar behavior.</behavior>
<action>Test then implement Status, Skeleton, Empty State, Error State, Pagination, and ScrollArea. Use explicit status variants including neutral/info/positive/warning/danger/offline/loading; do not announce decorative icons or repeatedly spam live regions. Use exact UI-SPEC empty/error copy and allow only meaningful action callbacks. Pagination proves current/boundary semantics with local numbers only; ScrollArea wraps native overflow and a discoverable label/affordance. Implement the applicable state matrix, non-colour cues, long text, and reduced-motion CSS without real offline, API, product pagination, or persistence behavior.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/primitives/primitives.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm tokens:check</automated></verify>
<acceptance_criteria>
- Status variants expose appropriate status/alert/live/busy semantics and visible icon+label/text cues independent of colour.
- Skeleton, Empty, Error, Pagination and ScrollArea tests prove inert/reduced-motion, exact recovery copy, labelled native navigation/current boundaries, and reachable overflow.
- Focused tests, typecheck, and token drift check exit zero.
</acceptance_criteria>
<done>Feedback/layout primitives communicate state, recovery, progress, and overflow accessibly without fake product behavior.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Migrate the lifecycle shell without changing host authority semantics</name>
<files>apps/desktop/src/App.tsx, apps/desktop/src/styles.css, apps/desktop/src/ui/primitives/primitives.test.tsx</files>
<read_first>apps/desktop/src/App.tsx, apps/desktop/src/styles.css, .planning/phases/04-desktop-shell-local-persistence-boundary-containers-and-ci-s/04-04-SUMMARY.md, packages/design-tokens/src/generated.css</read_first>
<behavior>- Initializing, ready, and recoverable failure remain explicit and retain their existing Tauri lifecycle/retry authority.
- Host status uses the new semantic tokens/primitives, visible non-colour labels, and reduced motion.
- Development diagnostics remain development-only; no UI Lab or product route depends on service readiness.</behavior>
<action>Import generated token CSS at the desktop style boundary and replace raw bootstrap colour/radius/spacing/motion values with semantic variables. Adopt Button/Status/Skeleton/Error-state composition where it preserves existing `invoke`, polling, retry, copy, live-region, and ownership behavior exactly. Keep the normal lifecycle shell available and do not translate it into dashboard/navigation/product UI. Add regression assertions for the three host states and development-only diagnostics; retain English lifecycle copy unless a separate product copy decision owns translation.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/primitives/primitives.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm --filter @rivallo/desktop build &amp;&amp; pnpm tokens:check</automated></verify>
<acceptance_criteria>
- Initializing/ready/recoverableFailure, polling, retry, ownership and development diagnostic behavior remain covered and unchanged in authority.
- `styles.css` consumes semantic `--rv-*` variables for migrated raw palette/radius/spacing/motion and preserves reduced motion/focus visibility.
- Primitive regression tests, typecheck, desktop web build and token drift check all exit zero.
</acceptance_criteria>
<done>The lifecycle shell uses the visual foundation while preserving all established host-state behavior, authority, and production diagnostics boundary.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-05-SP | Spoofing | status/error UI | mitigate | Explicit text/icon/live semantics prevent colour-only or visually ambiguous host state. |
| T-05-05-DoS | Denial of service | loading/retry controls | mitigate | Native disabled/busy behavior prevents duplicate actions and preserves layout. |
| T-05-05-EP | Elevation of privilege | lifecycle UI | mitigate | Preserve Tauri host authority; primitives never gain process/API authority. |
</threat_model>
<verification>Run DOM tests, typecheck, token drift, and desktop web build; compare lifecycle behavior to the Phase 4 contract.</verification>
<success_criteria>The native primitive foundation is accessible, visually coherent, state-complete, and safely adopted by the existing host shell.</success_criteria>
<output>Create `05-05-SUMMARY.md` when complete.</output>
