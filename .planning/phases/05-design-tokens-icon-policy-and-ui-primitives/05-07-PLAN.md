---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "07"
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - apps/desktop/src/ui/DenseTable/DenseTable.tsx
  - apps/desktop/src/ui/DenseTable/DenseTable.css
  - apps/desktop/src/ui/DenseTable/fixtures.ts
  - apps/desktop/src/ui/DenseTable/DenseTable.test.tsx
  - apps/desktop/src/ui/DenseTable/index.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-14: DenseTable begins with native table semantics and proves sticky header, sorting, selection, deterministic column priority/visibility, row actions, and loading/empty/error states."
    - "D-08/D-15: each row has at most one visible primary action; secondary actions are keyboard-accessible, and nationality remains readable without flag or colour."
    - "Density/visibility/sorting/selection are deterministic local fixture state only—no product model, persistence, API, virtualization, server behavior, or tactical surface."
  artifacts:
    - path: "apps/desktop/src/ui/DenseTable/DenseTable.tsx"
      provides: "Reusable semantic dense-table primitive with explicit finite states and local control hooks."
    - path: "apps/desktop/src/ui/DenseTable/DenseTable.test.tsx"
      provides: "DOM and keyboard evidence for semantics, sort, selection, visibility, row actions, nationality, and all content states."
  key_links:
    - from: "DenseTable.tsx"
      to: "primitives and @rivallo/icons"
      via: "Header/action/menu/status controls reuse approved primitives rather than one-off Lab widgets."
---

<objective>
Implement and prove DenseTable as Rivallo's first-class semantic information primitive.

Purpose: UI-01 and D-14/D-15 make table craft the principal evidence that high density can remain legible, configurable, and accessible.
Output: a reusable native table, deterministic non-domain fixture, local controls, complete states, and interaction tests.
</objective>
<execution_context>@<local-user>/.codex/gsd-core/workflows/execute-plan.md
@<local-user>/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-06-SUMMARY.md
@DESIGN.md
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Establish native table structure, density, sticky header, and content states</name>
<files>apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/DenseTable.css, apps/desktop/src/ui/DenseTable/DenseTable.test.tsx, apps/desktop/src/ui/DenseTable/index.ts</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#densetable-contract, apps/desktop/src/ui/primitives/layout.tsx, apps/desktop/src/ui/primitives/feedback.tsx, packages/design-tokens/src/generated.css</read_first>
<behavior>- Output includes labelled overflow region, table/caption/thead/tbody/scoped headers/cells; compact is 32px and comfortable is 40px with 12/14px floor.
- Header can be sticky with opaque semantic surface/layer and aligned columns.
- Loading preserves column geometry; empty/error stay inside the labelled table structure with exact approved copy/recovery semantics.</behavior>
<action>Write semantic DOM tests first. Define typed generic column/row contracts that format fixture values without importing domain/contracts/Tauri/API types. Use native `<table>` and a native labelled ScrollArea wrapper, deterministic column widths/priority metadata, right-aligned tabular numerics, and token CSS. Model content state as an explicit union (`ready`, `loading`, `empty`, `error`) and keep skeleton/empty/error rows inside the table region with valid column spans. Do not use an ARIA grid, div rows, table framework, virtualization, data fetching, or product squad schema.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/DenseTable/DenseTable.test.tsx &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- Rendered structure includes labelled overflow region, table/caption/thead/tbody, scoped headers/cells, correct colspans and no div-grid substitution.
- Compact/comfortable dimensions are 32/40px with 12/14px type floor; sticky header is opaque/aligned and loading/empty/error remain inside table structure.
- DenseTable focused tests and typecheck exit zero.
</acceptance_criteria>
<done>DenseTable exposes correct native relationships, compact/comfortable geometry, sticky-header support, overflow, and structurally stable content states.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Add semantic sorting, labelled selection, visibility, and row actions</name>
<files>apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/DenseTable.test.tsx, apps/desktop/src/ui/DenseTable/DenseTable.css</files>
<read_first>apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/DenseTable.test.tsx, apps/desktop/src/ui/primitives/disclosure.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#densetable-contract</read_first>
<behavior>- Sort buttons expose visible direction and `aria-sort` only on the sorted header.
- Labelled row/select-all checkboxes toggle by keyboard and selected rows have text/marker/border cues beyond colour.
- Deterministic priority/visibility and one primary plus overflow secondary row action work without persistence.</behavior>
<action>Add controlled local interaction contracts and tests for header sort cycles, row/select-all checkbox announcements, focused Space toggling, visible-column menu checkboxes, priority hiding/overflow at constrained width, and row action focus/menu behavior. Use a visible text primary action where space permits and a labelled Menu for secondary actions. Ensure hidden columns are removed consistently from headers, body, state-row spans, and accessible names. Keep changes reset-on-reload and do not implement column reordering, saved views, preference storage, server sorting/pagination, or real mutations.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/DenseTable/DenseTable.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm tokens:check</automated></verify>
<acceptance_criteria>
- Sort tests prove button operation, visible direction and `aria-sort` only on the active header.
- Row/select-all checkboxes, non-colour selected cue, visibility/priority consistency, one primary and labelled overflow actions work by keyboard and reset locally.
- DenseTable tests, typecheck and token drift check exit zero.
</acceptance_criteria>
<done>Sort, selection, visibility, and action affordances are semantic, keyboard-operable, non-colour, and strictly local.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Prove deterministic football-shaped fixtures and nationality fallback</name>
<files>apps/desktop/src/ui/DenseTable/fixtures.ts, apps/desktop/src/ui/DenseTable/DenseTable.test.tsx</files>
<read_first>apps/desktop/src/ui/DenseTable/DenseTable.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md#accessible-primitives-and-dense-table-foundation, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md#deferred-ideas, PRODUCT.md#anti-references</read_first>
<behavior>- Stable fixtures exercise long names, tabular data, every semantic status, missing values, overflow priorities, and flag/code/full-country-name behavior.
- Country code remains visible when flag imagery fails; full country name is discoverable through approved tooltip/detail.
- Fixtures have no API/domain contract authority and no real club/player identity.</behavior>
<action>Create small fictional, deterministic UI evidence objects owned by DenseTable, explicitly documented as non-domain fixtures. Include long Portuguese/localisation stress, stable IDs/order, neutral player-like labels, semantic statuses, null/missing presentation, and fictional nationality display data sufficient for D-15. Prefer local safe flag representation or intentionally broken-image test fallback; never fetch external flags, import real datasets, contracts-client, or encode scouting/tactics/product rules. Test the visible country code and accessible full name independent of image/colour.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui/DenseTable/DenseTable.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated></verify>
<acceptance_criteria>
- Fixtures have stable IDs/order and cover long text, numeric/status/null/overflow/nationality cases using fictional non-domain values.
- Country code remains visible when flag fails and full country name is keyboard-discoverable independently of image/colour.
- DenseTable tests, typecheck and lint exit zero; fixture files import no API, Tauri, contracts-client or domain types.
</acceptance_criteria>
<done>The fixture exhaustively proves table layout/state/nationality behavior without becoming real football data or a squad screen.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-07-SP | Spoofing | table state/data | mitigate | Explicit state union and non-domain deterministic fixtures; no network/domain authority. |
| T-05-07-TM | Tampering | sorting/selection | mitigate | Typed local state, stable IDs, native controls, and interaction tests. |
| T-05-07-ID | Information disclosure | fixtures | mitigate | Fictional committed data only; no user/API/real-person data. |
</threat_model>
<verification>Run DenseTable semantic/keyboard/state tests, typecheck, token drift, and lint; manually inspect alignment later at all Lab viewports.</verification>
<success_criteria>D-14/D-15 are realized as an accessible, dense, configurable semantic primitive without crossing into product or persistence scope.</success_criteria>
<output>Create `05-07-SUMMARY.md` when complete.</output>
