---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "08"
type: execute
wave: 8
depends_on: [05-07]
files_modified:
  - apps/desktop/src/main.tsx
  - apps/desktop/src/ui-lab/UiLab.tsx
  - apps/desktop/src/ui-lab/UiLab.css
  - apps/desktop/src/ui-lab/specimens.tsx
  - apps/desktop/src/ui-lab/UiLab.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-16: `/__ui-lab` is a development-only entry independent of lifecycle/Tauri/API/network/persistence and absent from production behavior/navigation."
    - "D-17/D-18: exactly seven proof categories expose authored/resolved token evidence, typography, icons, real primitive states, DenseTable, accessibility, and shell proof at the three prescribed presets."
    - "D-19/D-20: 232px/56px local shell proof preserves focus and content order, while no dashboard, squad, pitch, mascot, or copied manager composition is created."
  artifacts:
    - path: "apps/desktop/src/ui-lab/UiLab.tsx"
      provides: "Development-only deterministic UI Lab information architecture, viewport controls, and shell proof."
    - path: "apps/desktop/src/ui-lab/UiLab.test.tsx"
      provides: "DOM evidence for category inventory, real states, dev isolation, local reset behavior, and service independence."
  key_links:
    - from: "apps/desktop/src/main.tsx"
      to: "UiLab.tsx or App.tsx"
      via: "A compile-time DEV guard plus exact local path chooses the Lab; production always retains the operational shell/not-found behavior."
---

<objective>
Assemble the development-only UI Lab that proves the Phase 5 visual system without becoming a product screen.

Purpose: UI-01 and D-16–D-20 require one deterministic inspection surface for tokens, icons, component states, DenseTable, accessibility, and desktop composition.
Output: isolated Lab entry, exact seven-category IA, real specimen controls, three viewport presets, and expanded/collapsed shell proof.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@PRODUCT.md
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-07-SUMMARY.md
@apps/desktop/src/main.tsx
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Enforce the development-only, service-independent Lab entry boundary</name>
<files>apps/desktop/src/main.tsx, apps/desktop/src/ui-lab/UiLab.tsx, apps/desktop/src/ui-lab/UiLab.test.tsx</files>
<read_first>apps/desktop/src/main.tsx, apps/desktop/src/App.tsx, apps/desktop/vite.config.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#access-and-isolation</read_first>
<behavior>- Exact `/__ui-lab` renders the Lab only when `import.meta.env.DEV` is true.
- Production behavior cannot register/navigate to hidden Lab content and resolves to the normal operational shell or deliberate not-found outcome.
- Lab rendering performs no Tauri invoke, fetch, contracts-client import, storage access, or service-readiness wait.</behavior>
<action>Write source-boundary and rendered tests before wiring the entry. Use the existing single mount and a tiny explicit path predicate; do not add a router dependency. Keep `App` as the normal path. Structure the guard so production bundling can tree-shake or otherwise exclude Lab entry behavior, and add a build/static assertion that the production route does not expose its heading/copy/navigation. Mock no service merely to make the Lab work—its code/fixtures must have no service import in the first place. Do not add a production navigation link.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui-lab/UiLab.test.tsx &amp;&amp; pnpm --filter @rivallo/desktop build &amp;&amp; pnpm typecheck</automated></verify>
<acceptance_criteria>
- Exact `/__ui-lab` chooses Lab only under DEV; normal path still renders App and production output/behavior exposes no Lab heading/navigation.
- Static/import tests find no Tauri invoke, fetch, contracts-client, storage, API or lifecycle-readiness dependency in the Lab subtree.
- Lab boundary tests, production desktop web build and typecheck exit zero.
</acceptance_criteria>
<done>The UI Lab is reachable only in development by exact internal path, requires no host/service/network state, and is absent from production behavior.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Build the exact seven-category proof hierarchy and real specimens</name>
<files>apps/desktop/src/ui-lab/UiLab.tsx, apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#information-architecture, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#typography, packages/design-tokens/src/index.ts, packages/icons/src/index.ts, apps/desktop/src/ui/primitives/forms.tsx</read_first>
<behavior>- Navigation contains exactly Semantic tokens, Typography, Icons, Primitives, DenseTable, Accessibility evidence, and Shell proof with clear headings/purpose.
- Tokens show semantic/authored/resolved values, approved pair, ratio/threshold and usage; typography respects the isolated eight-token catalog and four-size/two-weight composition budget.
- Every consumed icon and primitive exposes actual applicable states; primary fixture action is exactly `Aplicar estado`.</behavior>
<action>Compose existing exports into one focal sequence—context heading, dominant specimen canvas, secondary controls/evidence—and enforce the UI-SPEC colour priority. Build real local state toggles rather than cosmetic classes. Display all eight type tokens only in the isolated inventory; every other composition uses at most 12/14/18/24 and weights 400/600. Render every consumed generic/original icon at 16/20/24 with semantics/tooltip evidence. Inventory every primitive and applicable default/hover/focus/active/selected/disabled/loading/error proof, including Checkbox unchecked/checked/indeterminate with actual DOM property, visible non-colour cue, label/focus/disabled/error-group evidence. Use exact Portuguese empty/error/dialog/collapse copy. Do not duplicate component implementation/styles inside the Lab, add giant cards/metrics, a pitch, dashboard, product navigation, real club/player action, mascot, logo, or final brand.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui-lab/UiLab.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm tokens:check</automated></verify>
<acceptance_criteria>
- Navigation has exactly seven approved category labels/headings/purpose statements; focal order and colour priority match the UI-SPEC.
- Token/type/icon inventories contain required evidence and every primitive exposes real applicable states, including Checkbox unchecked/checked/DOM-indeterminate/disabled/error.
- UI Lab tests, typecheck and token drift check exit zero with exact approved Portuguese fixture copy.
</acceptance_criteria>
<done>The Lab exposes the exact approved inventory with real component state, token/contrast evidence, disciplined hierarchy, and no product-screen leakage.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Prove viewport presets, DenseTable controls, accessibility fixtures, and shell collapse</name>
<files>apps/desktop/src/ui-lab/UiLab.tsx, apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx</files>
<read_first>apps/desktop/src/ui-lab/UiLab.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui/DenseTable/DenseTable.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#viewport-presets-and-responsive-behavior, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#shell-proof</read_first>
<behavior>- Labelled 1366x768, 1920x1080, and 2560x1080 preview frames constrain deterministic evidence without claiming device emulation.
- DenseTable local density/visibility/state controls reset on reload; long text, 200% expansion, focus, non-colour, and reduced-motion evidence remain reachable.
- Shell proof toggles 232px expanded/56px collapsed with exact labels, same icons, tooltip/accessibility names, workspace expansion, focus retention, and no persistence.</behavior>
<action>Add labelled preset controls and CSS constraints exactly from UI-SPEC: 1366 compact/overflow, 1920 stable default, 2560 bounded 1680 prose/2200 table. Implement DenseTable proof controls using local state only. Add accessibility fixtures for keyboard order, visible focus, status without colour, long Portuguese/200% expansion, text spacing, reduced motion, and contrast. Build only a shell composition proof; toggle must preserve DOM order/focus and reset on reload. At effective 200% constrained width allow one-column reflow/controlled overflow without unreachable actions. Do not use localStorage, preference APIs, responsive text shrinking, mobile navigation, or real AppShell ownership.</action>
<verify><automated>pnpm test -- apps/desktop/src/ui-lab/UiLab.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm --filter @rivallo/desktop build</automated></verify>
<acceptance_criteria>
- Preset controls expose exact 1366x768/1920x1080/2560x1080 labels and enforce the approved compact/default/ultrawide bounds without shrinking text.
- DenseTable density/visibility/state and shell 232/56 collapse reset on reload; collapse retains toggle focus/content order and accessibility fixtures remain reachable at effective 200%.
- UI Lab tests, typecheck and desktop web build exit zero.
</acceptance_criteria>
<done>All target viewport, table-local, accessibility, and shell-collapse proofs are deterministic, usable, focus-safe, and strictly Lab-owned.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-08-ID | Information disclosure | development UI Lab | mitigate | Exact DEV/path boundary, no production navigation/registration, and production build test. |
| T-05-08-SSRF | Spoofing | fixtures/service state | mitigate | No fetch/Tauri/contracts/storage imports; committed deterministic fixtures only. |
| T-05-08-DoS | Denial of service | shell/viewport focus | mitigate | Preserve DOM order/focus, controlled overflow, constrained widths, and keyboard tests. |
</threat_model>
<verification>Run Lab DOM/boundary tests, production build, typecheck/token drift; inspect import graph for Tauri/API/contracts/network/storage absence.</verification>
<success_criteria>D-16–D-20 and UI-01 are visible in an isolated, deterministic, dense, accessible Lab without production or product-feature leakage.</success_criteria>
<output>Create `05-08-SUMMARY.md` when complete.</output>
