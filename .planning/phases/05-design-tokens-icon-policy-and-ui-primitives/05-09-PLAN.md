---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "09"
type: execute
wave: 9
depends_on: [05-08]
files_modified:
  - playwright.config.ts
  - browser-tests/ui-lab.spec.ts
  - .gitignore
  - package.json
  - scripts/run-quality.mjs
  - scripts/verify-clean-worktree.mjs
  - eslint.config.mjs
  - tsconfig.json
  - .github/workflows/ci.yml
  - tooling-tests/phase-5-quality.test.mjs
  - .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Real non-watch Playwright evidence covers 1366x768, 1920x1080, 2560x1080, keyboard/focus, long text, reduced motion, non-colour status, overlays, and production Lab exclusion."
    - "Token drift/contrast, DOM interactions, DenseTable semantics, Lab boundary, browser evidence, type/lint/format, and desktop build are real quality surfaces with no placeholder success."
    - "Checks are idempotent and non-mutating; browser reports, traces, caches, sessions, and incidental screenshots remain narrowly ignored and uncommitted."
  artifacts:
    - path: "browser-tests/ui-lab.spec.ts"
      provides: "Deterministic end-to-end UI-01 viewport, keyboard, focus, reduced-motion, non-colour, and boundary evidence."
    - path: ".github/workflows/ci.yml"
      provides: "Hosted browser installation and real UI Lab evidence without artifact publication."
    - path: "05-VALIDATION.md"
      provides: "Final concrete Nyquist mapping from every implementation task to a non-watch check."
  key_links:
    - from: "package.json and scripts/run-quality.mjs"
      to: "tokens:check, Vitest, ui-lab:test, desktop build"
      via: "The public quality commands invoke only non-mutating checks and never generation writers."
---

<objective>
Make UI-01 continuously verifiable through browser evidence, complete quality tooling, CI, and a concrete Nyquist map.

Purpose: the visual foundation is not complete until its semantic, interaction, viewport, accessibility, isolation, and clean-worktree claims fail automatically when regressed.
Output: Playwright suite/configuration, ignored ephemeral evidence, root/CI wiring, static scope tests, and finalized validation strategy.
</objective>
<execution_context>@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-08-SUMMARY.md
@package.json
@scripts/run-quality.mjs
@.github/workflows/ci.yml
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add deterministic non-watch Playwright UI Lab evidence</name>
<files>playwright.config.ts, browser-tests/ui-lab.spec.ts, .gitignore</files>
<read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#test-and-review-evidence, apps/desktop/src/ui-lab/UiLab.tsx, apps/desktop/package.json, .gitignore, .github/workflows/ci.yml</read_first>
<behavior>- One local deterministic Vite server serves the dev-only Lab without Tauri/API/sidecar; tests use exact labelled viewport projects 1366x768, 1920x1080, 2560x1080.
- Keyboard traversal/focus visibility, table sort/select/menu, shell focus retention, overlay containment/return, long text/controlled overflow, reduced motion, and non-colour status are asserted.
- Ephemeral screenshots/traces/videos/reports are bounded to ignored output; failures are reproducible and no production-hidden Lab is accepted.</behavior>
<action>Configure the exact Playwright version approved in 05-01, Chromium only, deterministic locale/timezone/animation/font assumptions, retries suitable for CI, and a Vite dev web server that never starts Tauri or API services. Implement real assertions at all three browser viewports plus targeted keyboard/reduced-motion/200%-effective-width/non-colour tests; use accessibility scans only if the exact scanner package was approved, and retain manual evidence for judgment. Capture deterministic screenshots to ignored per-run output for human review without committing incidental images or publishing CI artifacts. Add narrow ignores for `playwright-report/`, `test-results/`, blob/traces/videos and browser cache if repository-local. Also run a production preview/bundle boundary assertion proving `/__ui-lab` does not reveal Lab behavior. Do not install browsers automatically inside the test command; document actionable setup failure.</action>
<verify><automated>pnpm exec playwright test browser-tests/ui-lab.spec.ts</automated></verify>
<acceptance_criteria>
- Playwright runs Chromium against a Vite-only server with exact 1366x768/1920x1080/2560x1080 projects and no Tauri/API/sidecar startup.
- Assertions cover keyboard/focus, DenseTable actions, shell focus, modal containment/return, long text, reduced motion, non-colour state and production Lab exclusion.
- Browser command exits zero; reports/screenshots/traces/videos are written only below narrowly ignored paths and no artifact is published/committed.
</acceptance_criteria>
<done>The browser suite is real, service-independent, non-watch, deterministic across the three target viewports, and produces only ignored ephemeral evidence.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Extend formatter, lint, typecheck, and full quality commands to every Phase 5 surface</name>
<files>package.json, scripts/run-quality.mjs, scripts/verify-clean-worktree.mjs, eslint.config.mjs, tsconfig.json, tooling-tests/phase-5-quality.test.mjs</files>
<read_first>package.json, scripts/run-quality.mjs, tooling-tests/workspace-config.test.mjs, eslint.config.mjs, tsconfig.json, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
<behavior>- All new TS/TSX/MJS/CSS/config/test/package files are covered by formatting; executable TS/TSX/MJS is linted/typechecked in its intended environment.
- `tokens:check`, component tests, and `ui-lab:test` are real root commands; `pnpm quality` is a non-mutating fail-fast aggregate and excludes `tokens:generate`.
- Missing browser/tooling prerequisites fail non-zero with an actionable command and never auto-install.
- A clean-worktree wrapper compares exact `git status --porcelain=v1 --untracked-files=all` before/after two aggregate runs and fails on any delta while ignored artifacts remain invisible.</behavior>
<action>Write static quality-surface tests first. Add explicit real commands for token writer/check and UI Lab browser test, expand Prettier glob coverage without formatting generated third-party/cache output, extend ESLint/TypeScript environment boundaries for desktop/components/browser tests, and teach the quality validator/aggregate about required non-mutating Phase 5 checks. Make `pnpm quality` the documented full non-mutating aggregate or delegate consistently without recursion; writer commands remain explicit and excluded. Add `verify-clean-worktree.mjs`: capture exact porcelain baseline, run the supplied aggregate command twice sequentially, compare status after each run to baseline, print the added/removed status lines and exit non-zero on either delta. Ignored caches/artifacts remain allowed because porcelain excludes them; a dirty baseline is allowed only if unchanged. Expose a real `quality:clean` command that wraps full quality plus desktop build without recursion. Preserve Rust/contracts/desktop semantics and serial generated-file tests. Missing browser/tool output names a one-line manual installation command; no script installs/updates tools.</action>
<verify><automated>pnpm test -- tooling-tests/phase-5-quality.test.mjs &amp;&amp; pnpm format:check &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm tokens:check</automated></verify>
<acceptance_criteria>
- Formatter/lint/typecheck include all Phase 5 source/config/test files; real token/component/browser commands exist and no writer appears in quality.
- Clean-worktree tests prove unchanged clean and unchanged dirty baselines pass, tracked/untracked deltas fail with actionable diff, ignored artifacts do not fail, and the aggregate runs exactly twice.
- Focused quality test, format, lint, typecheck and token drift exit zero.
</acceptance_criteria>
<done>Every Phase 5 file and check is covered by real root tooling, and the full quality aggregate remains fail-fast, idempotent, and writer-free.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Wire hosted browser evidence and finalize the validation map</name>
<files>.github/workflows/ci.yml, tooling-tests/phase-5-quality.test.mjs, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
<read_first>.github/workflows/ci.yml, package.json, scripts/verify-clean-worktree.mjs, tooling-tests/phase-5-quality.test.mjs, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
<behavior>- Hosted CI installs the approved Chromium binary explicitly, runs UI Lab evidence in the JS/TS responsibility, and publishes no screenshots/reports/artifacts.
- Every 05-01-01 through 05-10-02 task has a concrete non-watch automated command or explicit human-only rationale; no three consecutive implementation tasks lack automated feedback.
- `nyquist_compliant` and Wave 0 status reflect actual implemented test infrastructure, not future intent.</behavior>
<action>Extend the existing JavaScript/TypeScript CI job with the official approved Playwright browser setup command after frozen dependency installation, then run the real token, component, and UI Lab quality surface without changing the existing three-job architecture or publishing evidence. Static tests reject artifact upload, writer commands, shadcn/registry/table/icon alternatives, hidden Lab production entry, and missing browser execution. Rewrite 05-VALIDATION's placeholders to exact plan/task IDs, files, commands, threat refs, sampling boundaries, and actual Wave 0 completion; set `nyquist_compliant: true` only after every listed command/file exists and passes. Preserve manual optical/hierarchy checks as manual rather than claiming automation.</action>
<verify><automated>pnpm test -- tooling-tests/phase-5-quality.test.mjs &amp;&amp; pnpm quality:clean &amp;&amp; git diff --check</automated></verify>
<acceptance_criteria>
- JS/TS CI installs approved Chromium explicitly, runs real token/component/browser evidence, keeps exactly three jobs and contains no artifact upload/writer execution.
- 05-VALIDATION maps all 26 task IDs to exact commands/threats/files and keeps manual optical review manual; Nyquist becomes true only after files/commands pass.
- Static CI tests and `pnpm quality:clean` exit zero, two aggregate runs leave porcelain status identical to baseline, and whitespace validation is clean.
</acceptance_criteria>
<done>CI and local quality prove UI-01 continuously, validation is concrete/Nyquist-compliant, and no evidence or dependency writer leaks into non-mutating checks.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-09-ID | Information disclosure | Playwright evidence | mitigate | Deterministic fictional fixtures; ignore local reports/screenshots/traces and upload no CI artifacts. |
| T-05-09-TM | Tampering | quality/generated artifacts | mitigate | Writer-free aggregate, isolated drift outputs, static command tests, and clean-status proof. |
| T-05-09-SC | Tampering | CI browser/tooling | mitigate | Frozen approved dependencies, explicit official browser install, no runtime package install. |
</threat_model>
<verification>Run the exact 05-VALIDATION command map and `pnpm quality:clean`; the wrapper repeats full quality/browser/build twice and compares exact porcelain state after each run.</verification>
<success_criteria>UI-01 has non-watch, idempotent local and hosted evidence for tokens, interactions, DenseTable, Lab isolation, accessibility, motion, and all three desktop viewports.</success_criteria>
<output>Create `05-09-SUMMARY.md` when complete.</output>
