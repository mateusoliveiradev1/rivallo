---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: "11"
type: execute
wave: 10
depends_on: [05-09]
files_modified:
  - packages/icons/src/Icon.tsx
  - packages/icons/src/football-icons.tsx
  - packages/icons/src/index.ts
  - packages/icons/src/Icon.test.tsx
  - packages/icons/AUTHORSHIP.md
  - apps/desktop/src/ui-lab/specimens.tsx
  - apps/desktop/src/ui-lab/UiLab.css
  - apps/desktop/src/ui-lab/UiLab.test.tsx
  - browser-tests/ui-lab.spec.ts
  - .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-06–D-09: the generic/navigation and football icon boundaries read as one optically coherent family at 16px, 20px, and 24px without mixing libraries."
    - "The three Rivallo football proofs have refined, versioned geometry and one enforceable construction grammar while originality and optical approval remain human judgments."
    - "The UI Lab proves how training, tactics, scouting, and medicine concepts enter the family through representative evaluation cases, without shipping an exhaustive icon inventory or product screens."
    - "D-10: no menu implementation, training screen, pitch, final logo, mascot, final name, crest, or irreversible identity is introduced."
  artifacts:
    - path: "packages/icons/src/football-icons.tsx"
      provides: "Shared Rivallo football-icon grammar and the refined three-entry versioned proof set."
    - path: "packages/icons/src/Icon.test.tsx"
      provides: "Executable geometry, safety, metadata, accessibility, and family-boundary checks."
    - path: "apps/desktop/src/ui-lab/specimens.tsx"
      provides: "Optical comparison and bounded extension evidence for icon review."
    - path: "browser-tests/ui-lab.spec.ts"
      provides: "Three-viewport non-clipping and deterministic icon-review evidence."
    - path: ".planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md"
      provides: "Nyquist map for all 28 Phase 5 task IDs, including the two planned gap-closure checks."
  key_links:
    - from: "packages/icons/src/football-icons.tsx"
      to: "apps/desktop/src/ui-lab/specimens.tsx"
      via: "The Lab renders the exported grammar, metadata, and exact registries rather than duplicating icon rules."
    - from: "apps/desktop/src/ui-lab/specimens.tsx"
      to: "browser-tests/ui-lab.spec.ts"
      via: "Stable evidence hooks expose the same 16/20/24 optical comparisons at every target viewport."
---

<objective>
Close the failed UAT item “Premium, extensible icon foundation” before the terminal Phase 5 review.

Purpose: preserve the approved visual direction while raising the three football proof symbols to a credible optical bar, making the Rivallo construction grammar executable, and demonstrating disciplined extension without speculative bulk icon production.
Output: refined and versioned proof icons, enforceable family rules, bounded navigation/domain extension evidence, and deterministic UI Lab/browser review coverage.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@PRODUCT.md
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UAT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
@.planning/todos/pending/2026-07-15-design-initial-menu-and-football-icon-system.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-04-SUMMARY.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-08-SUMMARY.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-09-SUMMARY.md
@packages/icons/src/Icon.tsx
@packages/icons/src/football-icons.tsx
@apps/desktop/src/ui-lab/specimens.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify the family grammar and refine the three football proofs</name>
  <files>packages/icons/src/Icon.tsx, packages/icons/src/football-icons.tsx, packages/icons/src/index.ts, packages/icons/src/Icon.test.tsx, packages/icons/AUTHORSHIP.md</files>
  <read_first>packages/icons/src/Icon.tsx, packages/icons/src/football-icons.tsx, packages/icons/src/Icon.test.tsx, packages/icons/AUTHORSHIP.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UAT.md</read_first>
  <behavior>
    - The public generic boundary remains Lucide-only, semantic, currentColor, static, fixed to 16/20/24px and 1.75px stroke; a bounded representative navigation set demonstrates workspace, people, schedule, and settings meanings without exposing Lucide implementation names.
    - One exported football grammar owns the 24-unit master grid, approved render sizes, 1.75 stroke, round caps/joins, currentColor, no fill, minimum optical padding, and a small detail ceiling used by every football entry.
    - `football-ball`, `goal-frame`, and `training-cone` remain the complete SVG proof inventory; each receives visibly distinct, legible geometry and a semantic version increment recorded in metadata and authorship history.
    - Decorative/meaningful accessibility, fixed local geometry, safe attributes, and rejection of consumer paths, markup, styles, handlers, animation, URLs, unsupported names, and unsupported sizes remain enforced.
  </behavior>
  <action>Write the contract tests first. Keep Lucide as the only generic source and add no dependency. Add no more than four representative universal navigation meanings, mapping them privately to Lucide while retaining semantic public names. Define one typed `footballIconGrammar` contract and make the `FootballIcon` root consume its grid, sizes, stroke, cap, join, colour, and fill values instead of repeating literals. Refine only the existing three React-authored geometries on the 24-unit grid, keeping each inside the declared optical safe zone and detail ceiling; bump their metadata to the next semantic version and document the refinement plus normative construction/extension checklist in `AUTHORSHIP.md`. Tests must compare registry metadata to rendered DOM, verify every geometry obeys the machine-checkable grammar, and retain the SVG attack-surface fences. Do not automate claims of beauty, originality, non-imitation, or recognisability: those remain explicit 05-10 human judgments. Do not add domain-icon geometry for training, tactics, scouting, medicine, club identity, a pitch, mark, or mascot.</action>
  <verify>
    <automated>pnpm test -- packages/icons/src/Icon.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <done>The package exposes one tested Lucide-only generic boundary, one consumed football construction grammar, and exactly three safely refined/versioned football proofs with no new dependency or identity claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make optical quality and extension discipline reviewable in the UI Lab</name>
  <files>apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx, browser-tests/ui-lab.spec.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
  <read_first>apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx, browser-tests/ui-lab.spec.ts, packages/icons/src/index.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#icon-contract, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
  <behavior>
    - The Icons specimen separates generic/navigation and Rivallo football evidence, derives its inventory from package exports, and renders every real icon at 16/20/24px without clipping.
    - Each football proof appears beside an appropriate Lucide optical reference at the same size on canvas and raised graphite surfaces, with visible grid, stroke, version, meaning, decorative/semantic, and family metadata.
    - A visible extension rubric covers representative training, tactics, scouting, and medicine concepts as evaluation cases only: owner context, intended meaning, ambiguity/label fallback, and approval status are text evidence, not additional SVG assets or product destinations.
    - Chromium records deterministic ignored icon evidence and verifies the review cells remain bounded and reachable at 1366x768, 1920x1080, and 2560x1080.
    - The Phase 5 Nyquist map contains 28 task IDs and records 05-11-01 against T-05-11-SVG with `packages/icons/src/Icon.test.tsx`, and 05-11-02 against T-05-11-RP with `browser-tests/ui-lab.spec.ts`; both rows remain planned until execution proves their exact commands green.
  </behavior>
  <action>Replace the flat icon listing with one review hierarchy: generic/navigation reference set, football optical comparator, then extension rubric. Render the registries and `footballIconGrammar` directly so the Lab cannot drift into duplicate rules. Use fixed 32px review cells with centred 16/20/24 SVGs, explicit surface labels, and stable `data-*` evidence hooks; CSS must preserve focus visibility, operational contrast, non-colour labels, and controlled wrapping at the three approved desktop frames. The representative domain cases must explicitly say that geometry is created only when the owning screen contract approves an unambiguous meaning, and that visible text remains required when an icon cannot stand alone. Update DOM tests to derive the expected proof count from metadata, assert the exact three football SVG names, grammar/version evidence, navigation representatives, extension cases, and absence of extra football geometry. Extend the existing Playwright suite to open Icons, assert every SVG stays inside its review cell at all three projects, and write the local ignored screenshot through `testInfo.outputPath`. Update `05-VALIDATION.md` in the same change: add 05-11-01 with threat T-05-11-SVG, exact command `pnpm test -- packages/icons/src/Icon.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint`, and evidence `packages/icons/src/Icon.test.tsx`; add 05-11-02 with threat T-05-11-RP, exact command `pnpm test -- apps/desktop/src/ui-lab/UiLab.test.tsx packages/icons/src/Icon.test.tsx &amp;&amp; pnpm ui-lab:test &amp;&amp; pnpm --filter @rivallo/desktop build`, and evidence `browser-tests/ui-lab.spec.ts`. Mark both rows planned until execution proves them green, update the legend accordingly, and change the validation sign-off total from 26 to 28 task IDs. Preserve the seven-category IA and `/__ui-lab` production exclusion. Do not create an initial menu, in-game navigation destinations, training/tactics/scouting/medical screens, pitch, final logo, mascot, or exhaustive library.</action>
  <verify>
    <automated>pnpm test -- apps/desktop/src/ui-lab/UiLab.test.tsx packages/icons/src/Icon.test.tsx &amp;&amp; pnpm ui-lab:test &amp;&amp; pnpm --filter @rivallo/desktop build</automated>
  </verify>
  <done>The UI Lab provides deterministic, three-viewport optical evidence and a concrete extension rubric while the shipped football SVG inventory remains exactly the three refined proofs; the Nyquist map owns all 28 task IDs with both 05-11 rows explicitly planned.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-11-SVG | Tampering | football SVG boundary | mitigate | Fixed React geometry, shared grammar, safe-attribute tests, and no consumer markup/path/event/style surface. |
| T-05-11-SP | Spoofing | visual provenance and family consistency | mitigate | Versioned authorship record, one Lucide boundary, three Rivallo proofs, and human originality/non-imitation review. |
| T-05-11-SC | Tampering | package supply chain | mitigate | Install no dependency and retain the already approved Lucide package as the sole generic family. |
| T-05-11-RP | Repudiation | optical approval | mitigate | Browser evidence is structural only; 05-10 remains the dated human decision for optical quality. |
</threat_model>

<verification>
Run the two focused DOM suites, UI Lab Chromium suite, typecheck, lint, desktop build, then `pnpm quality:clean`. Confirm the exact Git porcelain baseline is unchanged. Automated success closes the technical portion of the UAT gap; optical/originality acceptance remains in 05-10.
</verification>

<success_criteria>
- The failed UAT icon-foundation gap has concrete package, Lab, DOM, and browser evidence ready for renewed human review.
- Generic icons still come only from the curated Lucide boundary; football icons still come only from Rivallo-authored SVG geometry.
- The three football proofs share and expose one enforced grammar, carry a documented semantic version increment, and are inspectable at 16/20/24px on both graphite surface levels.
- Representative navigation and domain-extension evidence demonstrates scale without creating product screens or an exhaustive icon inventory.
- `pnpm quality:clean` passes without modifying the tracked worktree or installing dependencies.
</success_criteria>

## Source audit

| Source | ID | Constraint | Task | Status |
|---|---|---|---|---|
| GOAL | — | Implement the approved icon policy and UI Lab foundation | 1–2 | COVERED |
| REQUIREMENTS | UI-01 | UI Lab documents responsive visual-system examples | 2 | COVERED |
| UAT | Gap 1 | Premium, coherent, extensible icon foundation | 1–2 | COVERED |
| CONTEXT | D-06–D-09 | Lucide-only generic family, original SVGs, labels/accessibility, static icons | 1–2 | COVERED |
| CONTEXT | D-10 | No literal mascot or final mark | 1–2 | FENCED |
| RESEARCH/PATTERNS | — | Typed registry, safe SVG surface, 16/20/24 optical review and no mixed family | 1–2 | COVERED |
| DEFERRED | — | Product menus/screens, pitch, exhaustive domain inventory, final logo/mascot/name | 1–2 | FENCED |

<output>Create `05-11-SUMMARY.md` when complete.</output>
