---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: '12'
type: execute
wave: 12
depends_on: [05-10]
files_modified:
  - apps/desktop/src/ui/DenseTable/DenseTable.css
  - apps/desktop/src/ui/primitives/primitives.css
  - browser-tests/ui-lab.spec.ts
  - .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [UI-01]
must_haves:
  truths:
    - 'D-14: DenseTable has finite, intentional horizontal overflow at 1366x768, 1920x1080, and 2560x1080 instead of an intrinsic million-pixel layout.'
    - 'Headers, representative first/last cells, and row actions are visible or reachable through the labelled native ScrollArea in every target viewport.'
    - 'Compact and comfortable density retain the 12px/14px operational type floor; the correction does not hide content by shrinking typography.'
  artifacts:
    - path: 'apps/desktop/src/ui/DenseTable/DenseTable.css'
      provides: 'Bounded table sizing with explicit finite column geometry and native overflow.'
    - path: 'apps/desktop/src/ui/primitives/primitives.css'
      provides: 'A generic ScrollArea content wrapper that does not amplify intrinsic width.'
    - path: 'browser-tests/ui-lab.spec.ts'
      provides: 'Chromium regression for finite geometry, visible table structure, and reachable actions at all three target viewports.'
    - path: '.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md'
      provides: 'Nyquist rows for the two DenseTable gap-closure tasks.'
  key_links:
    - from: 'apps/desktop/src/ui/DenseTable/DenseTable.css'
      to: 'apps/desktop/src/ui/primitives/primitives.css'
      via: 'The table owns its finite intrinsic width while ScrollArea owns only bounded overflow.'
    - from: 'browser-tests/ui-lab.spec.ts'
      to: 'apps/desktop/src/ui/DenseTable/DenseTable.css'
      via: 'Browser assertions derive a reasonable upper bound from active column geometry and exercise both left and right edges.'
---

<objective>
Close the rejected DenseTable width/overflow gap with a bounded CSS contract and real three-viewport browser evidence.

Purpose: restore a trustworthy dense-data surface before any screen contract or table-view architecture depends on it.
Output: corrected DenseTable/ScrollArea sizing, Chromium regression coverage, and validation-map evidence.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VERIFICATION.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UAT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-10-VISUAL-REVIEW.md
@apps/desktop/src/ui/DenseTable/DenseTable.tsx
@apps/desktop/src/ui/DenseTable/fixtures.ts
@apps/desktop/src/ui/primitives/layout.tsx

<interfaces>
`DenseTableColumn.width` is a required positive finite pixel width. The evidence fixture totals 1,156px across data columns, plus the existing 56px selection column and 220px actions column. `ScrollArea` renders a labelled native overflow region containing `.rv-scroll-area__content`; no div-grid or table framework is introduced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the DenseTable and ScrollArea width contract</name>
  <files>apps/desktop/src/ui/DenseTable/DenseTable.css, apps/desktop/src/ui/primitives/primitives.css, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
  <read_first>apps/desktop/src/ui/DenseTable/DenseTable.css, apps/desktop/src/ui/primitives/primitives.css, apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/fixtures.ts, apps/desktop/src/ui/primitives/layout.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VERIFICATION.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
  <action>Apply the verified root-cause correction for D-14: remove the two-level intrinsic-width amplification produced by the table's percentage width combined with a max-content minimum and the ScrollArea content wrapper's max-content minimum. Let `.rv-scroll-area` remain the bounded native overflow owner (`max-width: 100%`, `overflow: auto`); make `.rv-scroll-area__content` participate in the available inline size without inventing a second scrolling layer. Make `.rv-dense-table` occupy at least the available width while its `<colgroup>` finite widths determine the intentional overflow when the columns need more room. Preserve `table-layout: fixed`, sticky headers, the toolbar's left-edge behavior, 32px/40px row geometry, explicit widths, ellipsis/tooltips, and the 12px/14px type floor. Do not solve the defect by hiding required columns, shrinking text, removing actions, adding a custom scrollbar, or changing DenseTable into a div grid. Update pre-assigned validation row `05-12-01` from `planned` to the exact observed result after running this task's focused command; preserve requirement UI-01, threat T-05-12-DoS, and the pre-assigned CSS/DOM/build evidence, recording exact failure evidence instead of marking green if the command does not pass.</action>
  <verify>
    <automated>pnpm exec vitest run apps/desktop/src/ui/DenseTable/DenseTable.test.tsx &amp;&amp; pnpm --filter @rivallo/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - DenseTable and ScrollArea no longer compose percentage width with nested max-content minimums that can expand the layout to the browser clamp.
    - The table still honors finite column widths, fills a wider available region, and overflows only inside the labelled native ScrollArea when its declared columns exceed the viewport.
    - Compact/comfortable density, sticky header, selection, truncation, and actions retain their existing semantics and type sizes.
    - Focused DenseTable tests and the desktop production build exit zero.
  </acceptance_criteria>
  <done>The width owner and overflow owner are unambiguous, finite, semantic, and preserve the approved dense desktop typography.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove finite and reachable table geometry in Chromium</name>
  <files>browser-tests/ui-lab.spec.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
  <read_first>browser-tests/ui-lab.spec.ts, playwright.config.ts, apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/fixtures.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
  <behavior>
    - Every Playwright desktop project observes finite positive `clientWidth` and `scrollWidth` for the labelled DenseTable ScrollArea; `scrollWidth` stays within the sum of rendered finite column widths plus a small border/rounding tolerance rather than a magic browser clamp.
    - At scroll origin the table, first sortable header, first row identifier, and scroll affordance are visible; after scrolling to the inline end, the actions header and at least one primary/overflow row action are visible and keyboard reachable.
    - The test exercises compact and comfortable density without computed table text dropping below their declared 12px and 14px sizes.
  </behavior>
  <action>Add a Playwright test titled exactly `keeps DenseTable width finite and reaches both horizontal edges` to the existing Chromium suite so the title-filtered command runs it in all three configured projects. Open the DenseTable category, select the full priority fixture, and measure the ScrollArea, table, headers, representative cells, and action controls with `getBoundingClientRect`, `clientWidth`, and `scrollWidth`. Derive the reasonable maximum from the active `<col>` widths plus border/rounding tolerance; require all measurements to be finite and far below the browser clamp. Assert the first header/cell is visible at `scrollLeft = 0`; set the native region to its maximum horizontal offset and assert the actions header and a real row action become visible, focusable, and operable. Repeat the size-floor assertion after changing density. Update pre-assigned validation row `05-12-02` from `planned` to the exact observed result after running the title-filtered command; preserve requirement UI-01, threat T-05-12-SP, and the pre-assigned browser evidence, recording exact failure evidence instead of marking green if the command does not pass. Confirm the map still contains all eight pre-assigned gap-task IDs `05-12-01`, `05-12-02`, `05-13-01`, `05-13-02`, `05-14-01`, `05-14-02`, `05-15-01`, and `05-16-01`; preserve each later row as `planned` (and `05-16-01` as `human-pending`) until its owning plan executes, and keep the sign-off total at exactly 36. Keep screenshots/reports in the existing ignored Playwright output and do not add dependencies or artifact publication.</action>
  <verify>
    <automated>pnpm exec playwright test browser-tests/ui-lab.spec.ts --grep "keeps DenseTable width finite and reaches both horizontal edges"</automated>
  </verify>
  <acceptance_criteria>
    - The geometry regression runs at 1366x768, 1920x1080, and 2560x1080 and would fail against the prior 1,000,000px behavior.
    - The maximum-width assertion is derived from rendered finite columns; it is not a loose constant that permits pathological overflow.
    - Both inline edges expose their required headers/cells/actions through native scrolling, and the action at the far edge can receive keyboard focus.
    - Compact and comfortable computed type stay at or above 12px and 14px respectively.
    - The title-filtered three-project Playwright command exits zero; full repeated clean-worktree evidence is deferred to the final evidence-assembly plan.
  </acceptance_criteria>
  <done>Real Chromium evidence closes the layout blind spot that allowed the prior million-pixel table to pass static checks.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

- A bounded DenseTable/ScrollArea CSS sizing contract.
- Three-viewport Playwright evidence for finite width, visible structure, reachable actions, and typography floors.
- Validation rows 05-12-01 and 05-12-02.
  </artifacts_this_phase_produces>

<threat_model>

| Threat ID       | Category          | Component                   | Disposition | Mitigation Plan                                                                                 |
| --------------- | ----------------- | --------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-05-12-DoS     | Denial of service | DenseTable intrinsic sizing | mitigate    | Finite column-derived width and a bounded overflow owner prevent pathological layout expansion. |
| T-05-12-SP      | Spoofing          | Browser evidence            | mitigate    | Assert visible content and operable actions at both scroll edges, not only a numeric width.     |
| T-05-12-TM      | Tampering         | Quality/evidence outputs    | mitigate    | Reuse writer-free quality and ignored Playwright paths; compare exact porcelain twice.          |
| </threat_model> |

<verification>
Run the focused DenseTable DOM suite and desktop build, then the title-filtered Playwright test across all three Chromium projects. Inspect the new assertions against the pre-fix CSS once during RED to prove they detect the reported failure; Plan 05-15 owns the single full `pnpm quality:clean` evidence run.
</verification>

<success_criteria>

- DenseTable width is finite and intentional at all three target viewports.
- Headers, cells, and actions are visible or reachable through the labelled native ScrollArea.
- The correction preserves semantic table markup, sticky headers, density, focus, and the operational type floor.
- No dependency, product screen, persistence, table framework, or Table View Engine implementation is introduced.
  </success_criteria>

## Source audit

| Source            | ID                          | Constraint                                                                       | Task | Status  |
| ----------------- | --------------------------- | -------------------------------------------------------------------------------- | ---- | ------- |
| GOAL              | -                           | Accessible primitives and responsive UI Lab foundation                           | 1-2  | COVERED |
| REQUIREMENTS      | UI-01                       | Dense tables and responsive examples are documented and executable               | 2    | COVERED |
| VERIFICATION      | Gap 1                       | Correct million-pixel width and add three-viewport reachable-content regression  | 1-2  | COVERED |
| UAT               | Gap 1                       | Bounded table, sane scroll width, visible/reachable cells and actions            | 1-2  | COVERED |
| CONTEXT           | D-14                        | Semantic DenseTable with priority/visibility and intentional overflow            | 1-2  | COVERED |
| CONTEXT           | D-16-D-18                   | Deterministic service-independent Lab and target evidence                        | 2    | COVERED |
| UI-SPEC           | DenseTable/viewport         | Preserve 12px floor, native table/scroll, sticky headers and three presets       | 1-2  | COVERED |
| RESEARCH/PATTERNS | DenseTable/browser evidence | Native table plus labelled overflow, no div-grid, and real Playwright regression | 1-2  | COVERED |
| DEFERRED          | -                           | Full personalization, persistence, product data and screens                      | 1-2  | FENCED  |

<output>Create `05-12-SUMMARY.md` when complete.</output>
