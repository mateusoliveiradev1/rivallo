---
phase: 05-design-tokens-icon-policy-and-ui-primitives
plan: '14'
type: execute
wave: 14
depends_on: [05-13]
files_modified:
  - packages/icons/src/football-icons.tsx
  - packages/icons/src/Icon.test.tsx
  - packages/icons/AUTHORSHIP.md
  - apps/desktop/src/ui-lab/specimens.tsx
  - apps/desktop/src/ui-lab/UiLab.css
  - apps/desktop/src/ui-lab/UiLab.test.tsx
  - browser-tests/ui-lab.spec.ts
  - .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [UI-01]
must_haves:
  truths:
    - 'D-06-D-09: Lucide remains the sole generic family while Rivallo football nouns remain original, versioned SVGs with visible-label fallbacks.'
    - 'Ball and goal-frame use dedicated, visibly simpler 16px optical geometry rather than only scaling the 24-unit master.'
    - 'The goal-frame symbol means the physical football goal/frame only; it does not claim goal scored, shots on target, match objectives, or a universal action.'
    - 'The UI Lab shows bounded contextual use in AppShell chrome, command bar, table/state, and module-entry compositions without becoming a real menu, dashboard, or product screen.'
    - 'D-01-D-05 and D-20: contextual specimens preserve sober graphite, strong hierarchy, restrained semantic colour, text labels, and the non-imitation fence.'
  artifacts:
    - path: 'packages/icons/src/football-icons.tsx'
      provides: 'Size-aware ball and goal-frame optical variants with precise semantic metadata.'
    - path: 'packages/icons/src/Icon.test.tsx'
      provides: 'Executable proof that 16px variants are distinct, safe, bounded, versioned, and registry-complete.'
    - path: 'apps/desktop/src/ui-lab/specimens.tsx'
      provides: 'Four bounded contextual strips explaining Lucide versus Rivallo ownership and text fallback.'
    - path: 'browser-tests/ui-lab.spec.ts'
      provides: 'Three-viewport visibility/reachability and 16px rendering evidence for the contextual icon specimens.'
    - path: '.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md'
      provides: 'Nyquist rows for the icon optical and contextual-evidence tasks.'
  key_links:
    - from: 'packages/icons/src/football-icons.tsx'
      to: 'apps/desktop/src/ui-lab/specimens.tsx'
      via: 'The Lab consumes the real registry and size-aware component, never copied SVG paths or duplicated metadata.'
    - from: 'apps/desktop/src/ui-lab/specimens.tsx'
      to: 'browser-tests/ui-lab.spec.ts'
      via: 'Stable context and 16px evidence hooks prove where each family belongs in every target viewport.'
---

<objective>
Close the rejected 16px and contextual icon gap without expanding the icon inventory or implementing product navigation.

Purpose: make the football symbols optically credible at compact desktop size and make the generic-versus-domain family boundary understandable inside bounded football-management compositions.
Output: dedicated 16px ball/goal geometry, clarified goal semantics, contextual UI Lab evidence, tests, and validation rows.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@PRODUCT.md
@DESIGN.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-CONTEXT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VERIFICATION.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UAT.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-11-SUMMARY.md
@.planning/todos/pending/2026-07-15-design-initial-menu-and-football-icon-system.md
@.impeccable/critique/2026-07-15T19-21-58Z__apps-desktop-src-ui-lab-specimens-tsx.md
@packages/icons/src/Icon.tsx
@packages/icons/src/football-icons.tsx
@apps/desktop/src/ui-lab/specimens.tsx

<interfaces>
`FootballIcon` currently accepts `name`, approved `size` 16/20/24, optional class, and decorative-or-labelled accessibility props. `footballIconMetadata` and `footballIconGrammar` are exported and consumed by the Lab/tests. The public proof inventory remains exactly `football-ball`, `goal-frame`, and `training-cone`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated 16px ball and goal-frame optical variants</name>
  <files>packages/icons/src/football-icons.tsx, packages/icons/src/Icon.test.tsx, packages/icons/AUTHORSHIP.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
  <read_first>packages/icons/src/football-icons.tsx, packages/icons/src/Icon.test.tsx, packages/icons/AUTHORSHIP.md, packages/icons/src/Icon.tsx, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VERIFICATION.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-10-VISUAL-REVIEW.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</read_first>
  <behavior>
    - Rendering `football-ball` at 16 uses a compact optical master with fewer interior seams than the 20/24 master while keeping the ball silhouette and one readable central football cue.
    - Rendering `goal-frame` at 16 uses a compact silhouette dominated by top bar and posts, with at most one restrained depth/net cue; it contains no dense window-like grid.
    - Rendering either icon at 20 or 24 retains the approved master geometry; `training-cone` remains unchanged and no fourth football icon is added.
    - Metadata names the goal-frame as the physical football goal/frame and explicitly excludes scored-goal, shot, objective, and generic-action meanings.
    - Every size variant remains fixed local React geometry, currentColor, static, within the optical safe zone/detail ceiling, and subject to the existing injection/attribute/accessibility fences.
  </behavior>
  <action>Write failing contract tests first. Replace the one-geometry-per-name internal map with a typed size-aware entry that always owns a master and may own a compact-16 geometry; keep the public `FootballIconProps` and exact three-name registry stable. Create a compact 16px ball variant by reducing interior seam count and emphasizing the outer circle plus one central football construction cue. Create a compact 16px goal variant by emphasizing the physical crossbar/posts silhouette and eliminating the repeated horizontal/vertical net grid that resembles a window; keep no more than one restrained depth or net cue. Select the compact variant only for size 16 and retain the existing master for 20/24. Update the goal metadata/visible meaning to `Estrutura física do gol (trave)` and document prohibited interpretations; bump only the changed ball/goal patch or minor versions consistently in metadata and authorship history. Extend tests to prove distinct 16 versus 20/24 geometry, exact inventory, safe bounds/detail ceiling, registry-to-DOM metadata parity, and the narrowed semantic meaning. Update pre-assigned validation row `05-14-01` from `planned` to the exact observed result after running the focused icon-test command; preserve requirement UI-01, threat T-05-14-TM, and the pre-assigned icon test/registry/authorship evidence, recording exact failure evidence instead of marking green if the command does not pass. Do not add a dependency, fill, animation, second family, extra football symbol, crest, mark, logo, mascot, or copied path.</action>
  <verify>
    <automated>pnpm exec vitest run packages/icons/src/Icon.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ball and goal-frame render different simplified geometry at 16px; 20px and 24px retain their master geometry.
    - The 16px ball has fewer interior seams and the 16px goal has no repeated window-like grid.
    - Goal metadata and authorship documentation define only the physical goal/frame meaning and list the excluded interpretations.
    - The football registry remains exactly three original entries; training cone and the generic Lucide boundary are unchanged.
    - The focused icon test exits zero with no new dependency; desktop build and the final quality aggregate provide the later integration checks.
  </acceptance_criteria>
  <done>The two rejected compact symbols have size-specific, versioned, semantically precise optical masters while the public family stays bounded and safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Explain and prove icon-family use in bounded product contexts</name>
  <files>apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx, browser-tests/ui-lab.spec.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-VALIDATION.md</files>
  <read_first>apps/desktop/src/ui-lab/specimens.tsx, apps/desktop/src/ui-lab/UiLab.css, apps/desktop/src/ui-lab/UiLab.test.tsx, browser-tests/ui-lab.spec.ts, packages/icons/src/index.ts, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md#icon-contract, .planning/todos/pending/2026-07-15-design-initial-menu-and-football-icon-system.md</read_first>
  <behavior>
    - One AppShell-chrome strip uses Lucide semantic navigation icons with visible labels and explains that it is a composition example, not the initial or in-game menu.
    - One command-bar strip uses Lucide for universal verbs such as search and column configuration, with icon plus label and no football SVG used as an action verb.
    - One table/state strip uses Lucide for sort/status/action semantics and keeps explicit text; colour and icon never become the sole signal.
    - One module-entry strip uses the real Rivallo ball, physical goal-frame, and training-cone nouns with visible labels, explicit permitted/prohibited meanings, and text fallback where ambiguity remains.
    - The contextual strips remain readable and reachable at 1366x768, 1920x1080, and 2560x1080 and demonstrate the real 16px optical variants without creating destinations or product behavior.
  </behavior>
  <action>Restructure the Icons category so contextual use precedes or clearly accompanies the technical catalog. Add exactly four bounded, non-navigating specimen strips: AppShell chrome, command bar, table/state, and football module entry. Reuse the real `Icon`, `FootballIcon`, shared Button/Status where appropriate, and exported metadata; every sample must show family, permitted use, prohibited use, visible label requirement, and fallback. Use stable `data-icon-context` hooks. The AppShell sample must explicitly say it is not the initial menu or a production destination. The command and table samples keep universal verbs/states in Lucide. The domain sample shows the 16px ball and physical goal-frame plus training cone with text, explicitly stating that the goal-frame is a physical noun and never means goal scored, shot, or objective. Preserve the technical 16/20/24 catalog and extension rubric but visually separate QA evidence from usage explanation. CSS uses existing semantic tokens, graphite surfaces, restrained borders, 6-12px radii, fixed operational type, and no decorative glass, wide shadows, nested-card wall, neon, casino cues, or new palette. DOM tests assert exactly four contexts, real registry consumption, visible labels/fallbacks, exact goal meaning, and absence of real links/destinations/extra SVGs. Add a Playwright test titled exactly `explains Lucide and Rivallo icon ownership in four bounded contexts`; at all three projects, require the four contexts and their 16px SVGs to be inside finite visible containers and keyboard-reachable controls to retain visible labels. Update pre-assigned validation row `05-14-02` from `planned` to the exact observed result after running the focused DOM/icon, title-filtered browser, and desktop-build command; preserve requirement UI-01, threat T-05-14-SP, and the pre-assigned Lab/browser evidence, recording exact failure evidence instead of marking green if the command does not pass.</action>
  <verify>
    <automated>pnpm exec vitest run apps/desktop/src/ui-lab/UiLab.test.tsx packages/icons/src/Icon.test.tsx &amp;&amp; pnpm exec playwright test browser-tests/ui-lab.spec.ts --grep "explains Lucide and Rivallo icon ownership in four bounded contexts" &amp;&amp; pnpm --filter @rivallo/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - UI Lab contains exactly four bounded contexts covering AppShell, command bar, table/state, and module entry.
    - Lucide owns universal navigation/actions/states; Rivallo owns only the three approved football nouns; all ambiguous meanings retain visible text.
    - The physical goal-frame meaning and prohibited interpretations are explicit in DOM-visible copy and tests.
    - Contexts remain finite, legible, and reachable in all three target viewports without creating product routes, destinations, dashboard, pitch, menu, or new football SVGs.
    - Focused DOM/icon tests, Chromium projects, and desktop build exit zero.
  </acceptance_criteria>
  <done>Mateus can see both what the icons look like and where each family belongs, with compact optical evidence embedded in honest non-product contexts.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

- Dedicated 16px optical variants for ball and physical goal-frame.
- Versioned semantic clarification and authorship history for the goal-frame.
- Four bounded family-usage contexts in the UI Lab with DOM and Chromium evidence.
- Validation rows 05-14-01 and 05-14-02.
  </artifacts_this_phase_produces>

<threat_model>

| Threat ID       | Category    | Component                         | Disposition | Mitigation Plan                                                                                                          |
| --------------- | ----------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-05-14-SP      | Spoofing    | Icon meaning and family ownership | mitigate    | Visible labels, explicit permitted/prohibited meanings, metadata tests, and human review prevent ambiguous substitution. |
| T-05-14-TM      | Tampering   | SVG geometry                      | mitigate    | Fixed local React geometry, size-aware registry, safe-zone/detail tests, and no consumer path/markup surface.            |
| T-05-14-SC      | Tampering   | Icon supply chain                 | mitigate    | Add no package; retain the approved Lucide dependency and Rivallo-authored SVGs only.                                    |
| T-05-14-RP      | Repudiation | Optical/context approval          | mitigate    | Automated bounds/semantics evidence feeds a separate dated Mateus review; tests do not claim visual quality.             |
| </threat_model> |

<verification>
Run the focused icon suite RED against the current scaled masters, implement the size-aware variants, then run focused icon/Lab DOM tests, the title-filtered browser proof across all three Chromium projects, and the desktop build. Automation proves geometry, bounds, semantics, and reachability; Mateus retains optical approval authority, and Plan 05-15 owns the single full `pnpm quality:clean` evidence run.
</verification>

<success_criteria>

- Ball and physical goal-frame are materially clearer at 16px without adding decorative complexity.
- The generic-versus-domain family boundary is understandable in four bounded contexts.
- Contextual evidence preserves the approved sober, premium, graphite-first direction and visible labels.
- No speculative inventory, production menu, dashboard, pitch, training screen, logo, mascot, final name, or dependency is introduced.
  </success_criteria>

## Source audit

| Source            | ID                       | Constraint                                                                              | Task                    | Status  |
| ----------------- | ------------------------ | --------------------------------------------------------------------------------------- | ----------------------- | ------- |
| GOAL              | -                        | Implement icon policy and UI Lab foundation                                             | 1-2                     | COVERED |
| REQUIREMENTS      | UI-01                    | UI Lab documents responsive visual-system examples                                      | 2                       | COVERED |
| VERIFICATION      | Gap 2                    | Dedicated 16px ball/goal treatment, contextual family examples, renewed human review    | 1-2; review in 05-15/16 | COVERED |
| UAT               | Gap 2                    | Strong, understandable icons in AppShell/command/table-state/module contexts            | 1-2                     | COVERED |
| CONTEXT           | D-01-D-05                | Preserve graphite, hierarchy, semantic colour and non-colour communication              | 2                       | COVERED |
| CONTEXT           | D-06-D-09                | Lucide-only generic family, original football SVGs, labels/accessibility, static icons  | 1-2                     | COVERED |
| CONTEXT           | D-10                     | No mascot/final mark                                                                    | 1-2                     | FENCED  |
| CONTEXT           | D-16-D-18                | Deterministic UI Lab and three-viewport evidence                                        | 2                       | COVERED |
| CONTEXT           | D-20                     | Directional evidence only; no copied or real product screen                             | 2                       | FENCED  |
| RESEARCH/PATTERNS | Icon boundary            | Typed original SVG registry, 16/20/24 evidence, one generic family and no copied assets | 1-2                     | COVERED |
| TODO              | Initial menu/icon system | Keep real menu and future inventory in Phase 6/owning screens                           | 2                       | FENCED  |

<output>Create `05-14-SUMMARY.md` when complete.</output>
