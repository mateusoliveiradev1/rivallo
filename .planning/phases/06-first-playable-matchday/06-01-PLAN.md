---
phase: 06-first-playable-matchday
plan: 01
type: implementation
wave: 1
mode: mvp
requirements: [GAME-01, UI-02, DATA-03]
files_modified:
  - crates/domain/src/matchday.rs
  - crates/application/src/matchday.rs
  - crates/platform/src/matchday.rs
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src/matchday/MatchdayScreen.tsx
  - apps/desktop/src/matchday/matchday.css
  - apps/desktop/src/App.tsx
autonomous: true
---

<objective>
Ship one executable matchday slice through the existing Tauri, React and Rust boundaries.

Purpose: replace foundation-only progress with a real football-management interaction that Mateus can run and judge.
</objective>

<tasks>

<task type="tdd">
  <name>1. Define and test the smallest domain-owned matchday</name>
  <action>Add fictional squad state, lineup validation, tactical choices, deterministic match events, result and record updates in the framework-free domain crate.</action>
  <verify>Rust domain tests prove default XI validity, invalid selection rejection, deterministic simulation and record progression.</verify>
</task>

<task type="auto">
  <name>2. Expose one application service and persistent outer adapter</name>
  <action>Add a repository port and matchday service in application, then an atomic JSON file adapter and coordinator in platform without adding a registry package.</action>
  <verify>Application tests prove load/save and the platform boundary round-trips state without moving rules outside domain.</verify>
</task>

<task type="auto">
  <name>3. Wire the real desktop bridge</name>
  <action>Register Tauri commands for loading state, saving XI/tactics and playing the next match. Persist under the application data directory.</action>
  <verify>Tauri compiles with serializable command inputs/outputs and preserves existing lifecycle ownership behavior.</verify>
</task>

<task type="tdd">
  <name>4. Build the real matchday workspace</name>
  <action>Replace the ready-state placeholder with an AppShell, squad/XI workspace, tactical controls and result surface using the existing design tokens, primitives and icon boundary.</action>
  <verify>Component tests cover loading, lineup changes, save, simulation result, error feedback and keyboard-native controls.</verify>
</task>

<task type="auto">
  <name>5. Prove the first playable</name>
  <action>Run all quality gates, build the desktop application, inspect the real surface at target desktop widths and record honest limitations.</action>
  <verify>`pnpm quality` and `pnpm desktop:build` pass from the integrated tree.</verify>
</task>

</tasks>

<success_criteria>

- A real user can change the XI, choose tactics, play a match, inspect the outcome and resume state.
- Every completed task expands an executable user path; no task delivers only speculative infrastructure.
- The UI Lab remains development-only and no longer blocks product validation.
  </success_criteria>
