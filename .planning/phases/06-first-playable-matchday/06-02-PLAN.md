---
phase: 06-first-playable-matchday
plan: '02'
type: execute
wave: 2
depends_on: [06-01]
product_review_remediation: true
baseline: 38c2bff
files_modified:
  - apps/desktop/src/main.tsx
  - apps/desktop/src/assets.d.ts
  - apps/desktop/src/assets/flags/PROVENANCE.md
  - apps/desktop/src/assets/flags/*.svg
  - apps/desktop/src/ui/primitives/disclosure.tsx
  - apps/desktop/src/ui/primitives/primitives.css
  - apps/desktop/src/ui/primitives/composites.test.tsx
  - apps/desktop/src/ui/Nationality/country-catalog.ts
  - apps/desktop/src/ui/Nationality/country-catalog.test.ts
  - apps/desktop/src/ui/Nationality/NationalityDisplay.tsx
  - apps/desktop/src/ui/Nationality/NationalityDisplay.css
  - apps/desktop/src/ui/Nationality/NationalityDisplay.test.tsx
  - apps/desktop/src/ui/Nationality/index.ts
  - apps/desktop/src/ui/DenseTable/DenseTable.tsx
  - apps/desktop/src/matchday/MatchdayScreen.tsx
  - apps/desktop/src/matchday/SquadWorkspace.tsx
  - apps/desktop/src/matchday/WindowControls.tsx
  - apps/desktop/src/matchday/MatchdayScreen.test.tsx
  - apps/desktop/src/matchday/matchday.css
  - browser-tests/matchday.spec.ts
autonomous: false
gap_closure: true
requirements: [SM-01]
must_haves:
  truths:
    - 'The committed 38c2bff AppShell, Elenco/Táticas structure, navigation, local-career flow, design tokens, icons, Tauri/Rust boundaries and public behavior remain intact.'
    - 'Changing density never leaves the adjacent column disclosure, portal, overlay, pointer block or scroll lock in a zombie state; close, Escape and outside click work repeatedly, with focus restored unless the user deliberately moved it to another control.'
    - 'Supplemental tooltips work consistently by keyboard and pointer in the real product, without replacing explicit labels or changing accessible names.'
    - 'Current player nationalities render a deterministic local flag/full-name presentation with accessible unknown and asset-failure fallbacks.'
    - 'The exercised Elenco/Táticas path emits no relevant uncaught page error or console error and all pre-existing regression paths remain green.'
    - 'Only SM-1 and indispensable compatibility seams are implemented; SM-2 through SM-6 remain prospective and no Table View Engine, new preference-migration architecture, route/state migration or sports-domain formula is introduced.'
  artifacts:
    - path: 'apps/desktop/src/ui/primitives/disclosure.tsx'
      provides: 'One stable tooltip-provider boundary and controlled Popover behavior with focus restoration.'
    - path: 'apps/desktop/src/matchday/SquadWorkspace.tsx'
      provides: 'Incrementally repaired product density/column controls without replacing the squad table.'
    - path: 'apps/desktop/src/ui/Nationality/NationalityDisplay.tsx'
      provides: 'Reusable accessible nationality presentation with local-asset and unknown fallbacks.'
    - path: 'apps/desktop/src/matchday/MatchdayScreen.test.tsx'
      provides: 'Component-level reproduction and regression proof for the product surface.'
    - path: 'browser-tests/matchday.spec.ts'
      provides: 'Real-browser repeated disclosure, persistence, tooltip, flag, console and non-regression evidence.'
  key_links:
    - from: 'apps/desktop/src/main.tsx'
      to: 'apps/desktop/src/ui/primitives/disclosure.tsx'
      via: 'The normal product and UI Lab share one TooltipProvider policy without changing entry routing.'
    - from: 'apps/desktop/src/matchday/SquadWorkspace.tsx'
      to: 'apps/desktop/src/ui/primitives/disclosure.tsx'
      via: 'The existing columns control uses the controlled Popover rather than an uncontrolled native details element.'
    - from: 'apps/desktop/src/matchday/SquadWorkspace.tsx'
      to: 'apps/desktop/src/ui/Nationality/NationalityDisplay.tsx'
      via: 'Squad cells and the contextual dossier reuse one nationality display contract.'
    - from: 'apps/desktop/src/ui/DenseTable/DenseTable.tsx'
      to: 'apps/desktop/src/ui/Nationality/NationalityDisplay.tsx'
      via: 'The Lab foundation delegates nationality display instead of maintaining a competing flag implementation.'
    - from: 'apps/desktop/src/matchday/MatchdayScreen.test.tsx'
      to: 'browser-tests/matchday.spec.ts'
      via: 'Component behavior is repeated against real portal/focus/layout behavior and console capture in a browser.'
---

<objective>
Stabilize the real Phase 6 Elenco surface after product review without redesigning or advancing the sports-management roadmap.

Purpose: correct the reproduced density/disclosure regression, restore product tooltips and nationality flags, install a failing console/page-error guard, and establish zero-error regression evidence while preserving the working first-playable.

Output: a narrowly changed, tested desktop surface plus honest evidence that SM-1 works and SM-2 through SM-6 remain unimplemented.
</objective>

<execution_context>
@../../../.codex/gsd-core/workflows/execute-plan.md
@../../../.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@PRODUCT.md
@DESIGN.md
@docs/adr/ADR-0002-tauri-react-rust.md
@docs/adr/ADR-0003-independent-domain-core.md
@docs/adr/ADR-0005-authority-by-mode.md
@docs/adr/ADR-0009-impeccable-design-system.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-TABLE-VIEW-ENGINE-CONTRACT.md
@.planning/phases/06-first-playable-matchday/06-01-PLAN.md
@.planning/phases/06-first-playable-matchday/06-02-AUDIT.md
@.planning/SPORTS-MANAGEMENT-EVOLUTION.md
</context>

<scope_fence>

Allowed: fix the existing controls/components in place; introduce only a shared tooltip provider, a controlled Popover seam and a shared nationality presentation/catalog when they directly solve SM-1; add local flag assets with provenance; add regression tests and console guards.

Forbidden: redesigning AppShell/Elenco/Táticas; changing navigation or routes; changing tokens/brand/logo; replacing the product table or implementing the Table View Engine; adding column order/width/pinning/presets/saved views or a new migration/repository architecture; installing TanStack Router/Query, Zustand, Zod or a drag library; moving state architecture broadly; changing Rust match rules; creating player/coach routes, free formations, new tactical semantics, ratings formulas, dynamics, training or dynamic potential; implementing any part of SM-2 through SM-6 beyond the compatibility seams named above. The existing v2/v3 fallback may retain valid stable column IDs and an intentionally empty optional-column selection; this is compatibility repair, not the future SM-2 migration system.
</scope_fence>

<pre_flight_gate>

Before editing code:

1. Read every file named by the first task and inspect `rtk git status --short`; preserve all unrelated user/agent changes.
2. Confirm `38c2bff` is the factual audit baseline with `rtk git show --stat --oneline 38c2bff`.
3. Run the current targeted component and browser tests once and record failures without changing expectations to hide them.
4. Reproduce the exact sequence: open columns disclosure, change density, close by explicit action/Escape/outside click, reopen repeatedly, navigate Elenco/Táticas and reload.
5. Capture `pageerror` and `console.error`; name the concrete error before fixing it. If the reported break cannot be reproduced, stop broad changes and retain only a failing regression test built from observable evidence.

Passing pre-flight authorizes only the bounded tasks below. It does not authorize a table migration or redesign.
</pre_flight_gate>

<tasks>

<task type="tdd">
  <name>Task 1: Lock the product-review defects in regression tests</name>
  <files>apps/desktop/src/matchday/MatchdayScreen.test.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, browser-tests/matchday.spec.ts</files>
  <read_first>.planning/phases/06-first-playable-matchday/06-02-AUDIT.md, apps/desktop/src/matchday/MatchdayScreen.test.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, browser-tests/matchday.spec.ts, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/ui/primitives/disclosure.tsx, apps/desktop/src/matchday/matchday.css</read_first>
  <action>Add the smallest tests that fail against the diagnosed baseline: open the columns disclosure, change each density, close with the visible close action, Escape and outside pointer, verify trigger focus returns for internal/Escape dismissal and that a deliberately focused outside control retains focus, repeat open/close several times, navigate away/back and reload, and prove no invisible element intercepts a known table action. Add browser listeners that collect uncaught page errors and relevant console errors and fail with their exact text. Preserve every existing assertion; do not weaken timing, accessibility names or layout expectations merely to make the new test pass.</action>
  <verify>
    <automated>rtk pnpm exec vitest run apps/desktop/src/matchday/MatchdayScreen.test.tsx apps/desktop/src/ui/primitives/composites.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The test names describe the user-observable regression and exercise the real product control, not only a toy primitive.
    - Escape, outside click, visible close, appropriate focus restoration/preservation, repeat cycles, navigation/reload and pointer reachability are asserted.
    - Console/page-error collection reports the original message and rejects new relevant errors.
    - Existing Elenco/Táticas tests remain present and unchanged in meaning.
  </acceptance_criteria>
  <done>The regression is reproducible and the suite has an objective red-to-green target.</done>
</task>

<task type="tdd">
  <name>Task 2: Repair density and columns disclosure without replacing the table</name>
  <files>apps/desktop/src/ui/primitives/disclosure.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/matchday/MatchdayScreen.test.tsx, apps/desktop/src/matchday/matchday.css, browser-tests/matchday.spec.ts</files>
  <read_first>apps/desktop/src/ui/primitives/disclosure.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/matchday/MatchdayScreen.tsx, apps/desktop/src/matchday/matchday-ui.ts, apps/desktop/src/matchday/matchday.css, apps/desktop/src/ui/primitives/primitives.css, DESIGN.md, .planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-UI-SPEC.md</read_first>
  <action>Replace only the uncontrolled product `&lt;details&gt;` seam with the existing Radix Popover boundary, extending that primitive minimally so the owning screen can control open state, trigger content/class/label and focus restoration. Coordinate one open state so a density action closes the columns disclosure before applying the existing preference. Keep compact/standard/comfortable values, table markup, visible-column semantics, token values, toolbar hierarchy and persistence key behavior intact. Use existing layer tokens; do not add overlay/scroll lock to this non-modal popover. Avoid global CSS rewrites.</action>
  <verify>
    <automated>rtk pnpm exec vitest run apps/desktop/src/ui/primitives/composites.test.tsx apps/desktop/src/matchday/MatchdayScreen.test.tsx &amp;&amp; rtk pnpm exec playwright test browser-tests/matchday.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three density options change the same existing row geometry and persist through reload.
    - Density selection closes an open columns disclosure deterministically.
    - Close control and Escape close the popover and return focus to its trigger; outside pointer dismissal preserves a deliberate focus target and otherwise falls back to the trigger.
    - Repeated cycles leave no mounted open content, overlay, body scroll lock or pointer interception.
    - The real squad table, filters, sorting, column visibility, row inspector and save actions retain their behavior and names.
  </acceptance_criteria>
  <done>The reproduced density/disclosure sequence is green without a table-engine migration.</done>
</task>

<task type="tdd">
  <name>Task 3: Restore one consistent tooltip boundary in the real product</name>
  <files>apps/desktop/src/main.tsx, apps/desktop/src/ui/primitives/disclosure.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, apps/desktop/src/matchday/MatchdayScreen.tsx, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/matchday/WindowControls.tsx, apps/desktop/src/matchday/MatchdayScreen.test.tsx, apps/desktop/src/matchday/matchday.css</files>
  <read_first>apps/desktop/src/main.tsx, apps/desktop/src/ui/primitives/disclosure.tsx, apps/desktop/src/ui/primitives/actions.tsx, apps/desktop/src/ui/primitives/composites.test.tsx, apps/desktop/src/matchday/MatchdayScreen.tsx, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/matchday/matchday.css</read_first>
  <action>Mount one shared Radix Tooltip Provider at the desktop surface boundary while retaining a safe local fallback for isolated component tests. Use a consistent non-zero delay and portal layer. Migrate only non-obvious density/column controls and supplemental abbreviations/statuses involved in SM-1 from `title` to the shared Tooltip/IconButton pattern. Preserve explicit accessible names separately, support focus/hover/Escape, and wrap disabled triggers when supplemental explanation is required. Do not add tooltips to fully explicit text or duplicate every native title in the product.</action>
  <verify>
    <automated>rtk pnpm exec vitest run apps/desktop/src/ui/primitives/composites.test.tsx apps/desktop/src/matchday/MatchdayScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - One product-level provider owns delay/skip-delay policy; isolated renders still work.
    - Affected icon controls retain stable accessible names independent of tooltip text.
    - Tooltips open on focus and hover, close on Escape, do not contain interactive content and do not cover the invoking control.
    - No blanket tooltip conversion, navigation rewrite or token change occurs.
  </acceptance_criteria>
  <done>Necessary product tooltips are restored through one tested boundary.</done>
</task>

<task type="tdd">
  <name>Task 4: Add local nationality flags with accessible fallbacks</name>
  <files>apps/desktop/src/assets.d.ts, apps/desktop/src/assets/flags/PROVENANCE.md, apps/desktop/src/assets/flags/*.svg, apps/desktop/src/ui/Nationality/country-catalog.ts, apps/desktop/src/ui/Nationality/country-catalog.test.ts, apps/desktop/src/ui/Nationality/NationalityDisplay.tsx, apps/desktop/src/ui/Nationality/NationalityDisplay.css, apps/desktop/src/ui/Nationality/NationalityDisplay.test.tsx, apps/desktop/src/ui/Nationality/index.ts, apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/matchday/MatchdayScreen.test.tsx</files>
  <read_first>crates/domain/src/matchday.rs, apps/desktop/src/matchday/types.ts, apps/desktop/src/matchday/SquadWorkspace.tsx, apps/desktop/src/ui/DenseTable/DenseTable.tsx, apps/desktop/src/ui/DenseTable/fixtures.ts, apps/desktop/src/assets.d.ts, docs/adr/ADR-0008-hosting-edge-assets.md</read_first>
  <action>Create a small presentation catalog for the ISO-like three-letter codes already emitted by the current fixture, mapping them to full Portuguese names, stable two-letter asset keys and committed local SVGs. Add provenance/license notes. Build one NationalityDisplay that keeps a readable code when an asset is unknown or fails, exposes the full name accessibly/through the shared tooltip, aligns in dense table cells and permits future multiple items without changing the current Player contract. Reuse it in the real squad table and contextual dossier; have DenseTable delegate to it rather than retaining competing failure logic. Do not use emoji or a network URL, and do not change Rust serialization in this slice.</action>
  <verify>
    <automated>rtk pnpm exec vitest run apps/desktop/src/ui/Nationality apps/desktop/src/ui/DenseTable/DenseTable.test.tsx apps/desktop/src/matchday/MatchdayScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every nationality code present in the current committed squad resolves to the correct local asset and full name.
    - Unknown, malformed and image-error paths keep readable non-colour text and do not throw.
    - Keyboard users can discover the full name; decorative image semantics do not duplicate speech.
    - The real squad row and dossier share the same component; the UI Lab has no competing flag behavior.
    - No remote flag dependency, emoji-only implementation, player-contract rewrite or dual-nationality fiction is introduced.
  </acceptance_criteria>
  <done>Current nationalities render consistently and safely across the touched product contexts.</done>
</task>

<task type="auto">
  <name>Task 5: Guard console/page errors and run the full non-regression gates</name>
  <files>apps/desktop/src/matchday/MatchdayScreen.test.tsx, browser-tests/matchday.spec.ts, only the smallest already-listed source file that owns each captured error</files>
  <read_first>browser-tests/matchday.spec.ts, apps/desktop/src/matchday/MatchdayScreen.test.tsx, package.json, scripts/run-quality.mjs, scripts/build-desktop.mjs, .planning/phases/06-first-playable-matchday/06-02-AUDIT.md</read_first>
  <action>Install browser guards that fail on each relevant console/page error. If a guard captures an error, fix its source rather than its message or listener; if none is captured, record honestly that zero relevant errors were observed. Re-run the complete existing Elenco/Táticas paths including filters, sort, player focus, XI add/remove, reserve swap, tactical keyboard movement, save, play/result, preferences, fullscreen target viewports and repeated disclosure use. Inspect the final diff against the scope fence; revert no unrelated user change. Record exact commands/results in the eventual 06-02 summary only after they run. Do not mark SM-2 through SM-6 complete.</action>
  <verify>
    <automated>rtk pnpm quality &amp;&amp; rtk pnpm exec playwright test browser-tests/matchday.spec.ts &amp;&amp; rtk pnpm desktop:build &amp;&amp; rtk git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Browser guards fail on relevant page/console errors; the accepted flows observe zero, and no broad suppressor or unreviewed allowlist exists.
    - Typecheck, lint, format, component tests, browser matchday tests, Rust checks and desktop build pass from the integrated tree.
    - Existing public routes/entry behavior, Tauri commands, Rust rules, tokens, navigation, AppShell and product interactions have no regression.
    - The final changed-file set is limited to the frontmatter list or a documented smaller owner file; package manifests, routes, Rust domain and global design tokens are unchanged.
    - Documentation and delivery language state honestly that only SM-1 was implemented.
  </acceptance_criteria>
  <done>The integrated SM-1 slice is mechanically green, scope-bounded and ready for human product review.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 6: Mateus verifies the repaired real Elenco surface</name>
  <files>none unless concrete review evidence creates a later gap plan</files>
  <read_first>.planning/phases/06-first-playable-matchday/06-02-AUDIT.md, .planning/SPORTS-MANAGEMENT-EVOLUTION.md, browser-tests/matchday.spec.ts</read_first>
  <what-built>A narrow stabilization of the existing real Elenco surface: controlled density/columns behavior, consistent product tooltips, local nationality flags/fallback and console-regression coverage.</what-built>
  <how-to-verify>Open the normal fullscreen Tauri application, not `/__ui-lab`. On Elenco, open Colunas; select each density; close by the visible close action, Escape and outside click; repeat; confirm the table remains reachable and the preference survives Elenco→Táticas→Elenco and restart. Focus/hover the non-obvious controls and nationality code, inspect flags/fallback, then exercise row selection and Salvar escalação.</how-to-verify>
  <action>Pause for Mateus after all automated gates pass. Ask only whether the reproduced regression is fixed without visual/behavioral regression. Treat broader aesthetic or SM-2 through SM-6 requests as input to a new discuss/spec/plan, not permission to expand this plan. An actionable failure must name the control, sequence, viewport and observed behavior.</action>
  <verify>
    <manual>Mateus confirms the listed SM-1 paths in the normal product surface at 1366×768 and 1920×1080, or returns concrete reproducible failure evidence.</manual>
  </verify>
  <acceptance_criteria>
    - Review occurs only after automated gates are green.
    - Approval concerns the repaired SM-1 behavior, not blanket approval of the visual direction or future sports-management systems.
    - Rejection produces concrete evidence suitable for a bounded follow-up plan and leaves 06-02 incomplete.
  </acceptance_criteria>
  <done>The real product surface has an explicit human pass or actionable bounded failure evidence.</done>
</task>

</tasks>

<review_gate>

Before any completion claim:

- inspect `rtk git diff --stat` and `rtk git diff -- apps/desktop/src browser-tests` for scope drift;
- prove package manifests, route/entry semantics, Tauri commands/config, Rust domain, design tokens, brand and navigation were not broadened;
- prove no new table engine, profile route, tactic formula, training model or decorative unavailable action was added;
- run `rtk git diff --check`;
- require Task 5 automated gates and Task 6 human review;
- create `06-02-SUMMARY.md` only after execution is genuinely complete; do not modify historical `06-01-SUMMARY.md`, `06-VERIFICATION.md`, `.planning/STATE.md` or closed phase records as part of this planning task.
  </review_gate>

<verification>

Minimum command set from repository root:

```powershell
rtk pnpm exec vitest run apps/desktop/src/ui/primitives/composites.test.tsx apps/desktop/src/ui/Nationality apps/desktop/src/ui/DenseTable/DenseTable.test.tsx apps/desktop/src/matchday/MatchdayScreen.test.tsx
rtk pnpm exec playwright test browser-tests/matchday.spec.ts
rtk pnpm quality
rtk pnpm desktop:build
rtk git diff --check
```

No command result may be inferred from a prior commit or another branch. Skipped browser cases must be explained and cannot cover an SM-1 acceptance path.
</verification>

<success_criteria>

- The reported density/disclosure break is reproducibly fixed and cannot leave stale interaction layers.
- Product tooltips and flags work accessibly in the actual Elenco workspace.
- Relevant console/page errors are a failing automated gate and are absent in the accepted flow.
- The first-playable, navigation, table, inspector, tactics and persistence paths retain their established behavior.
- The diff is incremental, reversible and limited to SM-1.
- SM-2 through SM-6 remain explicitly unimplemented and retain their future owners/dependencies.
  </success_criteria>

<artifacts_this_plan_produces>

- Green regression evidence for density/disclosure/tooltips/flags/console behavior.
- Reusable but bounded Tooltip/Popover/Nationality seams required by SM-1.
- Human product-surface verdict after automated verification.
- `06-02-SUMMARY.md` only when the execution and review are complete.

</artifacts_this_plan_produces>

<output>Create `06-02-SUMMARY.md` only after all automated gates and the human checkpoint complete.</output>
