---
phase: 06.1-sm-2-table-view-engine-and-durable-views
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/application/Cargo.toml
  - crates/application/src/table_view.rs
  - crates/application/src/lib.rs
autonomous: true
requirements: [SM-02, DATA-01]
must_haves:
  truths:
    - "D-07/D-09: Rust application code owns the repository port and validates system-default, user-owned, and shared-read-only lifecycle mutations before any durable save."
    - "D-10: a source-version/fingerprint receipt makes the legacy v2→v3→v4 import idempotent and confirms durable acceptance before the frontend may retire table fields."
    - "D-11: ownerScope: local-fixed is explicit in every accepted repository state and migration receipt; DATA-01 is addressed only at this adapter-owned preference-boundary seam, not as SQLite/career/cache/queue completion."
    - "D-20: load, invalid, unavailable, migrated, recovered, and save-failure outcomes are typed product states rather than adapter strings or silent defaults."
  artifacts:
    - path: "crates/application/src/table_view.rs"
      provides: "Application-owned table-view models, repository port, lifecycle service, typed errors/outcomes, and legacy import receipt policy."
      exports: ["TableViewRepository", "TableViewService", "TableViewServiceError", "TableViewRepositoryState", "TableViewLoadOutcome", "LegacyTableViewImport", "LegacyImportReceipt"]
    - path: "crates/application/src/lib.rs"
      provides: "Public application re-exports used by platform and Tauri without exposing filesystem authority."
  key_links:
    - from: "crates/application/src/table_view.rs"
      to: "crates/platform/src/table_view.rs"
      via: "The future platform adapter implements `TableViewRepository`; application service never imports platform or Tauri."
    - from: "crates/application/src/table_view.rs"
      to: "apps/desktop/src-tauri/src/main.rs"
      via: "Serde-ready application DTOs cross only through thin host conversions and typed command results."
---

<objective>
Create the Rust application repository port and lifecycle service that make durable table views authoritative outside React per D-07 and D-09 through D-11.

Purpose: SM-02 and the DATA-01 preference seam require validated repository policy, provenance, fallback, and import receipts before filesystem or Tauri code is added.
Output: a tested inward application contract with no filesystem, Tauri, SQLite, identity, query, cache, or sync dependency.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-TABLE-VIEW-ENGINE-CONTRACT.md
@.planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-CONTEXT.md
@.planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-RESEARCH.md
@.planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-PATTERNS.md
@crates/application/src/matchday.rs
@crates/application/src/persistence.rs
@crates/application/src/lib.rs
@crates/application/Cargo.toml
</context>

## Wave 0 test-first contract

Both application tasks must begin with failing in-memory port/service cases before production behavior; the repository contract and lifecycle policy are frozen by those tests for downstream platform work.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the bounded Rust model and injected repository contract</name>
  <files>crates/application/Cargo.toml, crates/application/src/table_view.rs, crates/application/src/lib.rs</files>
  <read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-TABLE-VIEW-ENGINE-CONTRACT.md#versioned-persistence-migration-and-recovery, .planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-PATTERNS.md#pattern-assignment-3--application-repository-port-and-service, crates/application/src/matchday.rs, crates/application/src/persistence.rs, crates/application/src/lib.rs, crates/application/Cargo.toml</read_first>
  <behavior>
    - Application values represent stable table/schema/owner/view/baseline IDs, provenance, normalized columns/sorts/typed filters/data window, active/default view references, envelope metadata, recovery outcome, and legacy receipt without filesystem concepts.
    - The validator accepts only `squad.primary`, schema 1, `local-fixed`, supported provenance/mutability, finite bounded values, unique IDs, valid active/default references, and bounded labels/views/clauses/payload metadata.
    - `TableViewRepository` exposes load and atomic-save intent plus typed repository results while hiding paths, bytes, JSON, quarantine filenames, Tauri, and OS errors.
    - The application crate uses only its existing inward dependency plus the exact already-resolved serde version needed for DTO derives; no new package/version or lockfile inventory is introduced.
  </behavior>
  <action>Write failing in-memory contract tests first, then add `table_view.rs` beside—not inside—`persistence.rs` or `matchday.rs`. Define application-owned value types, explicit bounds, provenance and owner enums, repository state, load/recovery outcome, typed service error, and injected repository trait. Validate every untrusted DTO field before mutation or save, with stable reason codes separated from Portuguese presentation copy. Add the exact already-locked serde dependency only if required for the application DTO derives, pinning the existing resolved version and leaving `Cargo.lock` package inventory unchanged. Re-export the public contract from `lib.rs`; import no filesystem, serde_json, Tauri, Tokio, SQLite, network, cache, query, or platform type.</action>
  <acceptance_criteria>
    - In-memory tests prove accepted bounded state and rejection of wrong table/schema/owner/provenance, duplicate IDs, invalid references, non-finite geometry, excessive collections, and incompatible typed filter values.
    - The repository trait and public errors contain no path, JSON, Tauri, SQLite, network, or OS-specific type.
    - Cargo metadata shows no new resolved package/version; only an existing exact serde package may become a direct application dependency.
  </acceptance_criteria>
  <verify>
    <automated>rtk pnpm rust:test &amp;&amp; rtk pnpm rust:architecture</automated>
  </verify>
  <done>The application crate owns a bounded, serializable, filesystem-neutral table-view repository contract with typed invalid/unavailable/recovery outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lifecycle policy, safe fallback, and idempotent legacy receipts</name>
  <files>crates/application/src/table_view.rs, crates/application/src/lib.rs</files>
  <read_first>.planning/phases/05-design-tokens-icon-policy-and-ui-primitives/05-TABLE-VIEW-ENGINE-CONTRACT.md#saved-views-provenance-dirty-state-and-reset, .planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-UI-SPEC.md#saved-view-lifecycle, .planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-RESEARCH.md#legacy-transaction, crates/application/src/matchday.rs</read_first>
  <behavior>
    - First load seeds or returns the valid system baseline while preserving a typed unavailable/recovered/migrated result from the repository.
    - System-default can activate/duplicate/set-default but not rename/delete; user-owned supports activate/create/duplicate/rename/delete/set-default/reset/save; shared-read-only can activate/duplicate but not mutate.
    - Deleting the active or default user view resolves and saves a safe fallback in the same accepted transaction; failed persistence leaves the prior baseline and dirty proposal recoverable.
    - A legacy import is keyed by source version and bounded fingerprint, is accepted at most once, returns a durable receipt, and never claims success before repository save succeeds.
    - The service always validates `local-fixed` ownership and exposes a later owner-migration seam without implementing career identity or storage migration.
  </behavior>
  <action>Write failing `MemoryRepository` service tests before implementing `TableViewService`. Add load-or-seed, validate-and-save, import, and complete provenance-aware lifecycle methods with deterministic stable IDs supplied by validated requests, safe active/default fallback, and typed outcomes for invalid, unavailable, migrated, recovered, and persistence failure states. Make the v2→v3→v4 legacy receipt idempotent by source version plus bounded fingerprint and store it in the same repository transaction as the imported normalized intent. Do not delete browser keys, inspect files, invent sharing transport, perform identity migration, or collapse recovery causes into free-form adapter messages.</action>
  <acceptance_criteria>
    - Tests cover every allowed and forbidden system/user/read-only operation, dirty save failure, active/default deletion fallback, reset, and subsequent load.
    - Duplicate StrictMode-style import requests return the stored receipt without creating another view; a failed save returns no confirmed receipt.
    - Every successful state remains `tableId: squad.primary`, `schemaVersion: 1`, `ownerScope: local-fixed`; the public API leaves Phase 9 ownership replacement possible without changing table semantics.
  </acceptance_criteria>
  <verify>
    <automated>rtk pnpm rust:test &amp;&amp; rtk pnpm rust:fmt &amp;&amp; rtk pnpm rust:clippy &amp;&amp; rtk pnpm rust:architecture</automated>
  </verify>
  <done>The application service authoritatively accepts only valid provenance/lifecycle transitions, persists safe fallbacks, and confirms legacy import only through a durable idempotent receipt.</done>
</task>

</tasks>

## Artifacts this phase produces

- `crates/application/src/table_view.rs` — new repository-neutral model, `TableViewRepository`, `TableViewService`, typed errors/outcomes, lifecycle policy, and legacy import receipt.
- `crates/application/src/lib.rs` — new public re-exports for the table-view application boundary.
- `crates/application/Cargo.toml` — at most one direct reference to the exact serde package/version already present in the lockfile; no new resolved dependency.

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Platform repository result → application service | File-derived state and recovery metadata are untrusted until application invariants and owner/table/schema bounds pass. |
| Tauri lifecycle/import request → repository mutation | Local IPC callers can submit malformed provenance, identities, labels, state, or replayed legacy imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-061-02-T | Tampering | `TableViewService` mutation methods | mitigate | Validate the complete proposed repository state and mutability rules before one repository save; no partial mutation. |
| T-061-02-S | Spoofing | provenance and `ownerScope` | mitigate | Closed enums, exact `local-fixed` acceptance, stable IDs, and operation matrices prevent callers claiming system/read-only mutation authority. |
| T-061-02-R | Repudiation | legacy import receipt / recovery outcome | mitigate | Store source version plus bounded fingerprint and typed outcome atomically so retries and failure paths are auditable without payload logging. |
| T-061-02-D | Denial of Service | labels/views/filters/payload metadata | mitigate | Explicit byte/count/depth/length bounds reject oversized state before repository work. |
| T-061-02-E | Elevation of Privilege | Phase 9 owner/storage capabilities | mitigate | The application exposes only `local-fixed` preference authority; no career identity, SQLite, query, cache, queue, or sync operation exists. |
</threat_model>

<verification>
Run focused application tests, Rust formatting, warnings-denied Clippy, and the architecture fence. Inspect Cargo metadata to prove no new resolved package/version and no outward dependency entered the application boundary.
</verification>

<success_criteria>
- Rust application code, not React, owns repository validation, saved-view mutability, fallback, and import receipt policy.
- DATA-01 coverage is explicitly limited to an adapter-owned preference seam that Phase 9 can replace.
- No filesystem, Tauri, SQLite, career identity, cache/offline/sync, query, grouping, or new registry package appears in the application module.
</success_criteria>

<output>
Create `.planning/phases/06.1-sm-2-table-view-engine-and-durable-views/06.1-02-SUMMARY.md` when complete.
</output>
