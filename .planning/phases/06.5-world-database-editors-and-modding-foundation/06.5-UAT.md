---
status: complete
phase: 06.5-world-database-editors-and-modding-foundation
source: 06.5-SUMMARY.md
started: 2026-07-18T07:00:00-03:00
updated: 2026-07-18T07:08:00-03:00
---

# Fase 06.5 — UAT funcional

## Current Test

[testing complete]

## Ambiente reproduzível

O fluxo aprovado foi executado no host Tauri real, diretamente em `/data-editor`. No ambiente
local preparado para a UAT, o editor pode ser reaberto no PowerShell com:

```powershell
powershell.exe -ExecutionPolicy Bypass -File "$env:APPDATA\com.rivallo.desktop\authoring\uat-06.5\start-data-editor.ps1"
```

A URL servida pelo Vite pode renderizar a interface no navegador, mas não é suficiente para esta
UAT: validação, catálogo e exportação usam o bridge nativo `invoke` do Tauri e o repositório local
de pacotes. A demonstração autoritativa deve usar a janela desktop.

Os insumos locais de autoria ficaram em
`%APPDATA%\com.rivallo.desktop\authoring\uat-06.5`. O catálogo exportado fica em
`%APPDATA%\com.rivallo.desktop\data-packages\<packageId>`. Esses diretórios são dados locais de
desenvolvimento e não fazem parte do build público.

## Autoridade observada

| Campo              | Valor aprovado                |
| ------------------ | ----------------------------- |
| Pacote-base ativo  | `official.rivallo.foundation` |
| `packageId`        | `official.rivallo.foundation` |
| Versão             | `1.0.0`                       |
| `schemaVersion`    | `1`                           |
| Algoritmo          | `fnv1a64`                     |
| `worldFingerprint` | `e581b5521d09cf7b`            |

O editor mostrou o pacote oficial como `Base ativa` e os mesmos metadados no painel
`Snapshot ativo`.

## Roteiro aprovado

1. Abrir o launcher Tauri diretamente em `/data-editor`.
2. Confirmar o pacote-base e os cinco campos da tabela de autoridade.
3. Colar `manifest.json`, manter `data/world.json` vazio e carregar `patches-valid.json` em
   `data/patches.json`.
4. Validar o mod `community.uat.player-identity`, que troca `rv-01` para camisa `99` no Elenco e
   nome conhecido `C. Brandão UAT` no perfil.
5. Exportar o candidato válido.
6. Confirmar a atualização imediata do catálogo local e o estado `Disponível, inativo`.
7. Confirmar a mensagem explícita de que exportar não ativou nenhuma carreira.
8. Preservar a carreira ativa, com escalação, Elenco, Táticas, variações, perfis e visualizações
   salvas intactos.

O destino esperado da exportação é
`%APPDATA%\com.rivallo.desktop\data-packages\community.uat.player-identity`. A exportação
recalcula o checksum do payload, grava atomicamente e não muda o conjunto de pacotes ativos nem o
fingerprint da carreira.

## Caso negativo determinístico

O candidato equivalente a `patches-broken-reference.json` aponta `identity.clubId` para
`club.missing`. O teste automatizado
`world::tests::mod_candidate_is_resolved_against_the_active_base_before_validation_passes`
resolve o mod contra a base ativa e exige:

- relatório `valid=false`;
- código `world.broken_club_reference`;
- campo `identity.clubId`;
- referência `club.missing`.

O mesmo serviço valida antes de chamar `save_package`; o teste
`world::tests::invalid_candidate_is_never_persisted` comprova que candidato inválido mantém o
contador de persistência em zero. `world::tests::catalog_marks_only_repository_active_packages_as_active`
comprova que o catálogo não promove pacote disponível a ativo. Os quatro testes
`rivallo-application::world::tests` passaram no encerramento. Como validação/exportação não possui
comando de carreira, o caso inválido não altera save, snapshot, Elenco, Táticas ou perfis.

## Tests

### 1. Abrir o editor Tauri

expected: A janela desktop abre diretamente em `/data-editor` e carrega o catálogo local.
result: pass
evidence: UAT manual aprovada no executável Tauri real.

### 2. Identificar a autoridade ativa

expected: O editor mostra pacote-base, packageId, versão, schemaVersion, fingerprint e algoritmo.
result: pass
evidence: UAT manual confirmou `official.rivallo.foundation` 1.0.0, schema 1 e `fnv1a64:e581b5521d09cf7b`.

### 3. Validar o patch de jogador

expected: Manifesto e patch válidos são aceitos sem criar autoridade esportiva paralela.
result: pass
evidence: UAT manual aprovou o candidato `community.uat.player-identity`.

### 4. Exportar e atualizar o catálogo

expected: O pacote é materializado atomicamente e aparece no catálogo sem reabrir o editor.
result: pass
evidence: UAT manual confirmou pacote exportado e catálogo local atualizado.

### 5. Manter o pacote exportado inativo

expected: A interface informa que exportar não ativa nenhuma carreira.
result: pass
evidence: Mensagem explícita e separação 06.5/06.6 aprovadas manualmente.

### 6. Bloquear referência quebrada

expected: `club.missing` bloqueia com código, campo e referência estruturados.
result: pass
evidence: Teste Rust focado verde e asserções exatas do diagnóstico.

### 7. Não persistir candidato inválido

expected: Uma falha de validação ocorre antes de qualquer gravação no catálogo.
result: pass
evidence: `invalid_candidate_is_never_persisted` verde, com `save_count=0`.

### 8. Preservar a carreira local

expected: Escalação, Elenco, Táticas, variações, perfis e visualizações permanecem intactos.
result: pass
evidence: Carreira ativa preservada na UAT e regressões completas verdes no commit corretivo.

### 9. Respeitar a fronteira da fase

expected: A 06.5 edita, valida, exporta e cataloga; não seleciona nem ativa pacotes em carreira.
result: pass
evidence: Fronteira aprovada manualmente e `load_active_packages()` permanece restrito à base oficial.

## Fora do escopo aprovado

Recarga explícita de uma carreira com outro conjunto, reflexo de mod no Elenco/perfil,
ativação/desativação e mudança/restauração de fingerprint não são gaps desta UAT. Esses seis
passos dependem da seleção e do snapshot de Nova Carreira, pertencentes à Fase 06.6 e ainda não
iniciados.

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
