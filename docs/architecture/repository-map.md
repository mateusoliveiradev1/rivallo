# Repository Map

Planned directories: `apps/desktop` (Tauri/React), `apps/api` (axum), `apps/data-studio` (future README); `crates/rivallo-{domain,application,simulation,sync,persistence-sqlite,persistence-postgres,api,observability,test-support}`; `packages/{ui,icons,design-tokens,api-client,config,test-fixtures}`; `infra/{opentofu-or-terraform,docker,environments}`; and `docs/`.

Create only the directories needed by each approved phase; future areas get a responsibility README, not empty folders.
