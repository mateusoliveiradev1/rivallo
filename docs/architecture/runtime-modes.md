# Runtime Modes

Local career can be fully offline. Online league can render cached data while disconnected, queue permitted commands, and reconcile later; it must label stale/sync/conflict states and must not claim an unconfirmed competitive change succeeded.
