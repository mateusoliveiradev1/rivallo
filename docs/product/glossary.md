# Glossary

**Aggregate:** versioned domain entity that accepts commands. **Projection:** queryable read model. **Authoritative:** final source of truth for a mode. **Command:** idempotent intent sent for validation. **Outbox/inbox:** durable delivery boundaries. **Local career:** offline mode backed by SQLite. **Online league:** shared mode backed by the Rust service and PostgreSQL.
